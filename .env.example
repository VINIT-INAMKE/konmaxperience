# Konma Xperience OS
# Environment variables are split across projects:
#   - backend/.env.example  — API server config (database, JWT, email)
#   - frontend/.env.example — Web app config (API URL, JWT secret)
#
# Copy the relevant .env.example to .env in each project directory.
claude --resume 17a8787f-d737-4386-871a-61b84a63f029