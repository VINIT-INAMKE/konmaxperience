---
phase: 18-data-export
plan: 06
type: execute
wave: 2
depends_on: ["18-01"]
files_modified:
  - backend/src/exports/builders/operations.builder.ts
  - backend/src/tasks/tasks.service.ts
  - backend/src/exports/exports.module.ts
autonomous: true
requirements:
  - EXPORT-08
must_haves:
  truths:
    - "Tasks export produces XLSX with title, domain, status, priority, xp, valid_xp, owner, quest, due_date, completed_at"
    - "KPIs export produces XLSX with name, description, unit, target_value, current_value, status, domain"
    - "Decision Log export produces XLSX with title, decision_type, context, proposed_by, impact_scope, final_decision, status"
    - "Leaderboard export produces XLSX with name, role, xp_total, level, streak_days"
    - "All 4 exports also produce valid CSV"
  artifacts:
    - path: "backend/src/exports/builders/operations.builder.ts"
      provides: "TasksExportBuilder, KpisExportBuilder, DecisionLogExportBuilder, LeaderboardExportBuilder"
      contains: "TasksExportBuilder"
  key_links:
    - from: "backend/src/exports/builders/operations.builder.ts"
      to: "backend/src/tasks/tasks.service.ts"
      via: "findAllForExport method"
      pattern: "findAllForExport"
    - from: "backend/src/exports/builders/operations.builder.ts"
      to: "backend/src/kpis/kpis.service.ts"
      via: "findAll or findAllForExport"
      pattern: "findAll"
---

<objective>
Build export builders for 4 operations/intelligence report types: Tasks, KPIs, Decision Log, Leaderboard. This completes all 22 export builders.

Purpose: Operations exports support team performance review, KPI tracking, and decision audit trails.
Output: Final 4 export types functional. All 22 report types now covered.
</objective>

<execution_context>
@D:/aditee/konmaxperience/.claude/get-shit-done/workflows/execute-plan.md
@D:/aditee/konmaxperience/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/18-data-export/18-CONTEXT.md
@.planning/phases/18-data-export/18-RESEARCH.md
@.planning/phases/18-data-export/18-01-SUMMARY.md

<interfaces>
From backend/src/exports/exports.service.ts:
```typescript
export interface ExportBuilder {
  buildXlsx(data: unknown[]): Promise<Buffer>;
  buildCsv(data: unknown[]): Promise<Buffer>;
  fetchData(dateFrom?: string, dateTo?: string, filters?: string): Promise<unknown[]>;
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Tasks + KPIs export builders</name>
  <files>backend/src/tasks/tasks.service.ts, backend/src/kpis/kpis.service.ts, backend/src/exports/builders/operations.builder.ts, backend/src/exports/exports.module.ts</files>
  <read_first>
    - backend/src/tasks/tasks.service.ts (full file — findMany, understand Task model with owner, quest relations)
    - backend/src/kpis/kpis.service.ts (full file — findAll, understand KPI model fields)
    - backend/src/tasks/tasks.module.ts
    - backend/src/kpis/kpis.module.ts
    - backend/prisma/schema.prisma (Task, Kpi models — all fields)
    - backend/src/exports/exports.service.ts (ExportBuilder interface)
  </read_first>
  <action>
1. **Add findAllForExport to TasksService**:
   ```typescript
   async findAllForExport(): Promise<Task[]> {
     return this.prisma.task.findMany({
       orderBy: { created_at: 'desc' },
       include: {
         owner: { select: { name: true } },
         quest: { select: { title: true } },
       },
     });
   }
   ```
   Adapt relation names to actual schema.

2. **Add findAllForExport to KpisService** (if not already unbounded):
   ```typescript
   async findAllForExport(): Promise<Kpi[]> {
     return this.prisma.kpi.findMany({
       orderBy: { name: 'asc' },
     });
   }
   ```

3. **Create operations.builder.ts** at `backend/src/exports/builders/operations.builder.ts`:

   **TasksExportBuilder:**
   - fetchData: calls `tasksService.findAllForExport()`
   - XLSX sheet: 'Tasks'
   - Columns: `Title` (30), `Domain` (16), `Status` (14), `Priority` (12), `XP` (8), `Valid XP` (8), `Owner` (20), `Quest` (24), `Due Date` (16, 'YYYY-MM-DD'), `Completed At` (22, 'YYYY-MM-DD HH:MM:SS')
   - `valid_xp` column: check if field is `valid_xp` or `is_valid` + `xp` — read the Task model in schema.prisma carefully

   **KpisExportBuilder:**
   - fetchData: calls `kpisService.findAllForExport()`
   - XLSX sheet: 'KPIs'
   - Columns: `Name` (24), `Description` (40), `Unit` (10), `Target Value` (14, '#,##0.00'), `Current Value` (14, '#,##0.00'), `Status` (12), `Domain` (16)

4. **Register both in ExportsModule**: Import `TasksModule`, `KpisModule`. Add builders as providers. Register for `tasks` and `kpis`.
  </action>
  <verify>
    <automated>cd backend && npx tsc --noEmit && echo "Tasks/KPIs builders compile"</automated>
  </verify>
  <acceptance_criteria>
    - grep "TasksExportBuilder" backend/src/exports/builders/operations.builder.ts returns a match
    - grep "KpisExportBuilder" backend/src/exports/builders/operations.builder.ts returns a match
    - grep "findAllForExport" backend/src/tasks/tasks.service.ts returns a match
    - grep "TasksModule\|KpisModule" backend/src/exports/exports.module.ts returns matches
    - npx tsc --noEmit exits 0
  </acceptance_criteria>
  <done>Tasks and KPIs builders produce XLSX and CSV, both registered in ExportsModule</done>
</task>

<task type="auto">
  <name>Task 2: Decision Log + Leaderboard export builders</name>
  <files>backend/src/exports/builders/operations.builder.ts, backend/src/exports/exports.module.ts</files>
  <read_first>
    - backend/src/decisions/decisions.service.ts (full file — findAll, understand Decision model fields and relations)
    - backend/src/leaderboard/leaderboard.service.ts (full file — understand leaderboard query, User model xp fields)
    - backend/src/decisions/decisions.module.ts
    - backend/src/leaderboard/leaderboard.module.ts
    - backend/prisma/schema.prisma (Decision, User models)
    - backend/src/exports/builders/operations.builder.ts (extend file from Task 1)
  </read_first>
  <action>
1. **Add to operations.builder.ts** (same file as Task 1):

   **DecisionLogExportBuilder:**
   - fetchData: calls `decisionsService.findAll()` (if already unbounded) or add `findAllForExport()` if paginated
   - Read the DecisionsService to see if findAll() already returns all records
   - If paginated, add findAllForExport that removes take/skip
   - XLSX sheet: 'Decision Log'
   - Columns: `Title` (30), `Decision Type` (16), `Context` (40), `Proposed By` (20), `Impact Scope` (16), `Final Decision` (40), `Status` (12), `Created At` (22, 'YYYY-MM-DD HH:MM:SS')
   - Include proposer relation: `proposed_by_user: { select: { name: true } }`

   **LeaderboardExportBuilder:**
   - fetchData: calls leaderboardService method or direct Prisma query for active users sorted by xp:
     ```typescript
     return this.prisma.user.findMany({
       where: { status: 'active' },
       orderBy: { xp_total: 'desc' },
       include: { role: { select: { name: true } } },
     });
     ```
   - XLSX sheet: 'Leaderboard'
   - Columns: `Name` (20), `Role` (20), `XP Total` (10), `Level` (8), `Streak Days` (12)
   - Inject PrismaService directly if LeaderboardService does not expose a suitable method

2. **Register both in ExportsModule**: Import `DecisionsModule`, `LeaderboardModule`. Add builders as providers. Register for `decision_log` and `leaderboard`.

3. **Verify all 22 builders registered**: After this task, the ExportsModule should import all necessary domain modules and have all 22 builders registered with ExportsService. Count them:
   - Plan 02: orders, revenue_summary, top_items, channel_breakdown, recipe_costs (5)
   - Plan 03: inventory_levels, stock_movements, purchase_orders, vendor_pricing (4)
   - Plan 04: waste_log, prep_batches, ingredients, vendors, recipes (5)
   - Plan 05: menu_items, feedback, events, event_guest_lists (4)
   - Plan 06: tasks, kpis, decision_log, leaderboard (4)
   - Total: 22
  </action>
  <verify>
    <automated>cd backend && npx tsc --noEmit && echo "All 22 builders compile"</automated>
  </verify>
  <acceptance_criteria>
    - grep "DecisionLogExportBuilder" backend/src/exports/builders/operations.builder.ts returns a match
    - grep "LeaderboardExportBuilder" backend/src/exports/builders/operations.builder.ts returns a match
    - grep "DecisionsModule\|LeaderboardModule" backend/src/exports/exports.module.ts returns matches
    - npx tsc --noEmit exits 0
  </acceptance_criteria>
  <done>Decision Log and Leaderboard builders produce XLSX and CSV. All 22 export builders now registered in ExportsModule. Full backend export infrastructure complete.</done>
</task>

</tasks>

<verification>
- `cd backend && npx tsc --noEmit` passes
- All 4 builders (tasks, kpis, decision_log, leaderboard) registered
- ExportsModule imports all required domain modules
</verification>

<success_criteria>
4 operations/intelligence export types produce valid XLSX and CSV. All 22 export types from the spec are now covered with registered builders. Backend compiles.
</success_criteria>

<output>
After completion, create `.planning/phases/18-data-export/18-06-SUMMARY.md`
</output>
