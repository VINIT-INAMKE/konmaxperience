---
phase: 17-search-preview-and-content-seeding
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/prisma/seed.ts
autonomous: true
requirements: [SEED-01, SEED-02, SEED-03, SEED-04]

must_haves:
  truths:
    - "Running prisma db seed creates 12 guide sections with 39+ guide pages"
    - "Every major feature area has a section: Kitchen, POS, Inventory, Recipes, Missions, Evidence, Governance, Analytics, Notifications, Events, Feedback, Admin"
    - "Each section has 2-5 pages with real step-by-step walkthroughs (not placeholder text)"
    - "Each section's role_codes matches the roles that actually use those features"
    - "No stale role codes (PRODUCTION_LEAD, FRONTEND_EXPERIENCE_LEAD) appear anywhere in seed"
    - "All seeded content uses valid Tiptap JSON format renderable by GuideProseRenderer"
    - "Admin can open any seeded page in the CMS editor and modify it"
    - "Seed is idempotent — running it twice produces the same result"
  artifacts:
    - path: "backend/prisma/seed.ts"
      provides: "12 guide sections with 39+ pages of real walkthrough content"
      contains: "guideSections"
  key_links:
    - from: "backend/prisma/seed.ts"
      to: "backend/src/guides/guides.service.ts"
      via: "Tiptap JSON content format compatible with GuideProseRenderer"
      pattern: "type.*doc.*content"
    - from: "backend/prisma/seed.ts"
      to: "backend/src/types/roles.ts"
      via: "role_codes using RoleCode enum values"
      pattern: "FOUNDER_ADMIN.*TECH_LEAD.*BACKEND_LEAD"
---

<objective>
Expand seed.ts from 5 sections / 8 pages to 12 sections / 39+ pages of real guide content generated from the actual codebase.

Purpose: On fresh deployment, staff immediately have comprehensive step-by-step walkthroughs for every feature area, mapped to the correct roles. This is a real deliverable — actual training material, not placeholder content.
Output: Fully expanded seed.ts with 12 sections, 39+ pages of Tiptap JSON content, correct role-to-section mapping, proper read time calculations, all seeded as published.
</objective>

<execution_context>
@D:/aditee/konmaxperience/.claude/get-shit-done/workflows/execute-plan.md
@D:/aditee/konmaxperience/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-search-preview-and-content-seeding/17-CONTEXT.md
@.planning/phases/17-search-preview-and-content-seeding/17-RESEARCH.md

<interfaces>
<!-- The executor MUST read the actual source files to generate accurate content -->

Existing seed pattern from backend/prisma/seed.ts:
```typescript
// Each section follows this structure:
{
  title: 'Section Name',
  slug: 'section-name',
  description: 'Short description of the section.',
  icon: 'LucideIconName',
  accent_color: '#HexColor',
  role_codes: ['FOUNDER_ADMIN', 'TECH_LEAD', ...],
  sort_order: N,
  status: 'published',
  pages: [
    {
      title: 'Page Title',
      slug: 'page-slug',
      summary: 'One-line summary of the page.',
      content: JSON.stringify({type:'doc',content:[...]}),
      sort_order: N,
      status: 'published',
    },
  ],
}
```

Tiptap JSON node types used in existing seed:
```
{type:'paragraph', content:[{type:'text', text:'...'}]}
{type:'heading', attrs:{level:2}, content:[{type:'text', text:'...'}]}
{type:'bulletList', content:[{type:'listItem', content:[{type:'paragraph', content:[...]}]}]}
{type:'orderedList', content:[{type:'listItem', content:[{type:'paragraph', content:[...]}]}]}
{type:'text', text:'...', marks:[{type:'bold'}]}  // bold text
{type:'text', text:'...', marks:[{type:'italic'}]}  // italic text
```

Authoritative role codes (from backend/src/types/roles.ts):
```
FOUNDER_ADMIN, FRONTEND_LEAD, BACKEND_LEAD, BI_LEAD,
PROCUREMENT_LEAD, TALENT_LEAD, TECH_LEAD, DESIGN_OUTREACH_LEAD
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Read codebase and plan section content</name>
  <read_first>
    backend/prisma/seed.ts
    backend/src/types/roles.ts
    backend/src/types/permissions.ts
    backend/src/kitchen/kds/kds.controller.ts
    backend/src/kitchen/prep-batches/prep-batches.controller.ts
    backend/src/kitchen/waste/waste.controller.ts
    backend/src/orders/orders.controller.ts
    backend/src/inventory/inventory.controller.ts
    backend/src/procurement/procurement.controller.ts
    backend/src/recipes/recipes.controller.ts
    backend/src/menu/menu.controller.ts
    backend/src/missions/missions.controller.ts
    backend/src/quests/quests.controller.ts
    backend/src/tasks/tasks.controller.ts
    backend/src/evidence/evidence.controller.ts
    backend/src/approvals/approvals.controller.ts
    backend/src/decisions/decisions.controller.ts
    backend/src/analytics/analytics.controller.ts
    backend/src/notifications/notifications.service.ts
    backend/src/events/events.controller.ts
    backend/src/feedback/feedback.controller.ts
    backend/src/users/users.controller.ts
    backend/src/settings/settings.controller.ts
    backend/src/delegations/delegations.service.ts
    backend/src/kpis/kpis.controller.ts
    backend/src/leaderboard/leaderboard.service.ts
    frontend/app/(ops)/operations/kitchen/kds/page.tsx
    frontend/app/(ops)/operations/kitchen/prep-batches/page.tsx
    frontend/app/(ops)/operations/kitchen/waste/page.tsx
    frontend/app/(ops)/pos/page.tsx
    frontend/app/(ops)/operations/inventory/page.tsx
    frontend/app/(ops)/operations/procurement/page.tsx
    frontend/app/(ops)/operations/recipes/page.tsx
    frontend/app/(ops)/operations/menu/page.tsx
    frontend/app/(ops)/missions/page.tsx
    frontend/app/(ops)/boards/missions/page.tsx
    frontend/app/(ops)/boards/evidence/page.tsx
    frontend/app/(ops)/approvals/page.tsx
    frontend/app/(ops)/decisions/page.tsx
    frontend/app/(ops)/intelligence/analytics/page.tsx
    frontend/app/(ops)/dashboard/page.tsx
    frontend/app/(ops)/notifications/page.tsx
    frontend/app/(ops)/operations/events/page.tsx
    frontend/app/(ops)/operations/feedback/page.tsx
    frontend/app/(ops)/admin/users/page.tsx
    frontend/app/(ops)/admin/settings/page.tsx
    frontend/app/(ops)/admin/permissions/page.tsx
    frontend/app/(ops)/admin/delegations/page.tsx
    frontend/app/(ops)/kpis/page.tsx
    frontend/app/(ops)/leaderboard/page.tsx
    frontend/app/(ops)/readiness/page.tsx
  </read_first>
  <files>
    (no files modified — this is a read-only research task that feeds Task 2)
  </files>
  <action>
    Read ALL the files listed in read_first. This is the research step that generates the knowledge needed for writing accurate walkthrough content in Task 2.

    For each feature area, note:
    - What the frontend page looks like (components used, layout, user interactions)
    - What API endpoints exist (controller methods, DTOs)
    - What workflow steps a user follows (e.g., create a prep batch: select recipe -> enter qty -> review deduction -> confirm)
    - What important tips or gotchas exist (e.g., FIFO deduction, stock validation, approval flows)

    Build a mental map of all 12 sections and their pages. The 12 sections and their target page counts:

    1. **Kitchen Operations** (5 pages) — expand existing 3 pages, add Waste Logging and Expiry Management
    2. **POS & Orders** (4 pages) — expand existing 2 pages, add Delivery Management and Daily Revenue Summary
    3. **Inventory & Procurement** (3 pages) — expand existing 1 page, add Purchase Orders and Vendor Management
    4. **Recipes & Menu** (3 pages) — expand existing 1 page, add Menu Items and Cost Analysis
    5. **Missions & Tasks** (4 pages) — expand existing 1 page, add Quests, Task Workflow, and XP/Leaderboard
    6. **Evidence & Approvals** (3 pages) — NEW: Uploading Evidence, Approval Queue, Override Workflow
    7. **Governance & Decisions** (3 pages) — NEW: Decision Log, Creating Decisions, Blockers
    8. **Analytics & Dashboard** (3 pages) — NEW: Dashboard Overview, Analytics Reports, KPIs
    9. **Notifications** (2 pages) — NEW: How Notifications Work, Managing Your Notifications
    10. **Events & Bookings** (3 pages) — NEW: Creating Events, Managing Bookings, Public Event Page
    11. **Customer Feedback** (2 pages) — NEW: How Feedback Works, Reviewing Feedback
    12. **Admin & System Guide** (4 pages) — NEW: User Management, Roles & Permissions, System Settings, Delegations

    Total: 39 pages minimum.

    DO NOT write any code yet. This task is purely about reading the codebase to understand the features.
  </action>
  <verify>
    <automated>echo "Read-only research task — no files modified"</automated>
  </verify>
  <acceptance_criteria>
    - All files in read_first have been read by the executor
    - Executor has sufficient knowledge to write accurate walkthroughs in Task 2
  </acceptance_criteria>
  <done>
    Executor has read all relevant backend controllers, services, and frontend pages across all 12 feature areas and is ready to generate accurate walkthrough content.
  </done>
</task>

<task type="auto">
  <name>Task 2: Expand seed.ts with 12 sections and 39+ pages of real content</name>
  <read_first>
    backend/prisma/seed.ts
    backend/src/types/roles.ts
    backend/src/guides/guides.service.ts
  </read_first>
  <files>
    backend/prisma/seed.ts
  </files>
  <action>
    Rewrite the `guideSections` array in `backend/prisma/seed.ts` with all 12 sections. Keep ALL existing non-guide seed code (roles, users, readiness meters, zones, brands, channels, unit conversions, system settings) UNCHANGED. Only modify the guide sections/pages block.

    **CRITICAL FIXES before expanding:**

    1. **Fix stale role codes (per Research Pitfall 4):**
       - Replace ALL instances of `'PRODUCTION_LEAD'` with `'BACKEND_LEAD'`
       - Replace ALL instances of `'FRONTEND_EXPERIENCE_LEAD'` with `'FRONTEND_LEAD'`

    2. **Fix read time calculation:**
       Replace `Math.ceil(JSON.stringify(page.content).length / 1000)` with a proper word-count function. Add this helper before `main()`:

       ```typescript
       function computeReadTime(content: string): number {
         const text = content.replace(/"text":"([^"]+)"/g, '$1 ');
         const words = text.split(/\s+/).filter(Boolean).length;
         return Math.max(1, Math.round(words / 200));
       }
       ```

       Then in the guide seeding loop, use: `estimated_read_time: computeReadTime(page.content)`.

    **SECTION SPECIFICATIONS (per D-10, D-11, D-12, D-13, D-14):**

    All content MUST be real, accurate walkthroughs based on what you learned from reading the actual codebase in Task 1. Use Tiptap JSON format with: paragraph, heading (level 2 and 3), bulletList, orderedList, bold marks, italic marks. DO NOT use placeholder text like "Lorem ipsum" or "TODO". Every page should read like real training documentation.

    **Section 1: Kitchen Operations** (expand from 3 to 5 pages)
    - slug: `kitchen-operations`, icon: `ChefHat`, accent_color: `#FF6B35`
    - role_codes: `['FOUNDER_ADMIN', 'TECH_LEAD', 'BACKEND_LEAD', 'PROCUREMENT_LEAD']`
    - sort_order: 1
    - Keep existing 3 pages (Getting Started, Creating Prep Batches, Using the KDS) but REFRESH content based on actual code
    - Add page 4: **Logging Waste** — slug: `logging-waste` — covers the waste log form, waste categories (EXPIRED, OVERPRODUCTION, DAMAGED, OTHER), zone selection, linking to ingredients, reviewing waste reports
    - Add page 5: **Expiry Management** — slug: `expiry-management` — covers how the expiry cron flags items approaching expiry, expiry alerts on dashboard, how to act on expiring stock

    **Section 2: POS & Orders** (expand from 2 to 4 pages)
    - slug: `pos-orders`, icon: `ShoppingCart`, accent_color: `#6366F1`
    - role_codes: `['FOUNDER_ADMIN', 'TECH_LEAD', 'FRONTEND_LEAD']`
    - sort_order: 2
    - Keep existing 2 pages (Taking an Order, Processing Payments) but REFRESH
    - Add page 3: **Managing Deliveries** — slug: `managing-deliveries` — delivery queue table, delivery statuses (PENDING, DISPATCHED, DELIVERED), driver assignment, tracking
    - Add page 4: **Daily Revenue Summary** — slug: `daily-revenue-summary` — revenue breakdown by payment method, daily totals, order count, how to use the POS dashboard

    **Section 3: Inventory & Procurement** (expand from 1 to 3 pages)
    - slug: `inventory-procurement`, icon: `Package`, accent_color: `#10B981`
    - role_codes: `['FOUNDER_ADMIN', 'TECH_LEAD', 'PROCUREMENT_LEAD']`
    - sort_order: 3
    - Keep existing 1 page (Checking Stock Levels) but REFRESH
    - Add page 2: **Creating Purchase Orders** — slug: `creating-purchase-orders` — PO creation flow, selecting vendor, adding line items with ingredient/qty/unit, submission, receiving PO, partial receiving
    - Add page 3: **Managing Vendors** — slug: `managing-vendors` — vendor list, adding vendors with contact info, vendor pricing (ingredient prices per vendor), comparing vendor prices

    **Section 4: Recipes & Menu** (expand from 1 to 3 pages)
    - slug: `recipes-menu`, icon: `BookOpen`, accent_color: `#EC4899`
    - role_codes: `['FOUNDER_ADMIN', 'TECH_LEAD', 'BACKEND_LEAD', 'BI_LEAD']`
    - sort_order: 4
    - Keep existing 1 page (Creating a Recipe) but REFRESH
    - Add page 2: **Managing Menu Items** — slug: `managing-menu-items` — creating menu items linked to recipes, pricing, category assignment, channel-specific modifiers, activating/deactivating items
    - Add page 3: **Understanding Recipe Costs** — slug: `understanding-recipe-costs` — cost calculator, how BOM costs roll up, impact of vendor price changes, food cost percentage

    **Section 5: Missions & Tasks** (expand from 1 to 4 pages)
    - slug: `missions-tasks`, icon: `Target`, accent_color: `#F59E0B`
    - role_codes: ALL 8 roles
    - sort_order: 5
    - Keep existing 1 page (How Missions Work) but REFRESH
    - Add page 2: **Working with Quests** — slug: `working-with-quests` — quest lifecycle (open->active->review->complete), quest owners, adding tasks to quests, quest progress tracking
    - Add page 3: **Completing Tasks** — slug: `completing-tasks` — task workflow (todo->in_progress->blocked->done), attaching evidence, getting approval, ad-hoc tasks vs core tasks, task types and XP weights
    - Add page 4: **XP & Leaderboard** — slug: `xp-and-leaderboard` — how XP is earned, task type multipliers (CORE=100%, ADHOC=70%, IMPROVEMENT=80%), level progression, leaderboard page, team rankings

    **Section 6: Evidence & Approvals** (NEW, 3 pages)
    - slug: `evidence-approvals`, icon: `CheckCircle`, accent_color: `#8B5CF6`
    - role_codes: `['FOUNDER_ADMIN', 'TECH_LEAD', 'FRONTEND_LEAD', 'BACKEND_LEAD', 'PROCUREMENT_LEAD']`
    - sort_order: 6
    - Page 1: **Uploading Evidence** — slug: `uploading-evidence` — what counts as evidence (photos, screenshots, documents), upload via R2 presigned URLs, attaching to tasks, evidence status (PENDING, APPROVED, REJECTED)
    - Page 2: **The Approval Queue** — slug: `approval-queue` — approval queue page, reviewing evidence, approving/rejecting with comments, batch approvals, what happens when evidence is approved (task status update)
    - Page 3: **Override Workflow** — slug: `override-workflow` — when to use overrides, override with admin PIN, audit trail for overrides, who can override

    **Section 7: Governance & Decisions** (NEW, 3 pages)
    - slug: `governance-decisions`, icon: `Scale`, accent_color: `#0EA5E9`
    - role_codes: `['FOUNDER_ADMIN', 'TECH_LEAD', 'FRONTEND_LEAD', 'BACKEND_LEAD', 'BI_LEAD', 'DESIGN_OUTREACH_LEAD']`
    - sort_order: 7
    - Page 1: **The Decision Log** — slug: `decision-log` — what decisions are tracked, decision statuses (PROPOSED, APPROVED, REJECTED, DEFERRED), browsing the decision list, filtering
    - Page 2: **Creating a Decision** — slug: `creating-a-decision` — decision form fields (title, description, context, options, impact), assigning stakeholders, decision categories
    - Page 3: **Managing Blockers** — slug: `managing-blockers` — admin blockers page, blocking items, escalation, resolution tracking

    **Section 8: Analytics & Dashboard** (NEW, 3 pages)
    - slug: `analytics-dashboard`, icon: `BarChart3`, accent_color: `#14B8A6`
    - role_codes: `['FOUNDER_ADMIN', 'TECH_LEAD', 'BI_LEAD']`
    - sort_order: 8
    - Page 1: **Dashboard Overview** — slug: `dashboard-overview` — dashboard page layout, KPI alert cards, low stock alerts, readiness strip, quick navigation
    - Page 2: **Analytics Reports** — slug: `analytics-reports` — analytics page, revenue trends, order volume, recipe cost analysis table, date range filtering
    - Page 3: **Working with KPIs** — slug: `working-with-kpis` — KPI cards, creating KPIs (name, target, actual, unit), KPI categories, tracking progress, alerts when behind target

    **Section 9: Notifications** (NEW, 2 pages)
    - slug: `notifications`, icon: `Bell`, accent_color: `#F97316`
    - role_codes: ALL 8 roles
    - sort_order: 9
    - Page 1: **How Notifications Work** — slug: `how-notifications-work` — notification types (task assigned, evidence approved/rejected, quest status change, KPI alert, approval required), real-time bell icon count, notification list page
    - Page 2: **Managing Your Notifications** — slug: `managing-notifications` — marking as read, notification cleanup, notification preferences (if any), understanding priority levels

    **Section 10: Events & Bookings** (NEW, 3 pages)
    - slug: `events-bookings`, icon: `Calendar`, accent_color: `#D946EF`
    - role_codes: `['FOUNDER_ADMIN', 'TECH_LEAD', 'FRONTEND_LEAD', 'DESIGN_OUTREACH_LEAD']`
    - sort_order: 10
    - Page 1: **Creating Events** — slug: `creating-events` — event form (title, description, date, capacity, price, location), event types, publishing events to public page
    - Page 2: **Managing Bookings** — slug: `managing-bookings` — booking list sheet, booking statuses (CONFIRMED, CANCELLED), capacity tracking, guest information
    - Page 3: **The Public Event Page** — slug: `public-event-page` — how the public /events page works, event cards, availability badges, capacity indicators, booking form for guests

    **Section 11: Customer Feedback** (NEW, 2 pages)
    - slug: `customer-feedback`, icon: `MessageSquare`, accent_color: `#EF4444`
    - role_codes: `['FOUNDER_ADMIN', 'TECH_LEAD', 'FRONTEND_LEAD']`
    - sort_order: 11
    - Page 1: **How Feedback Works** — slug: `how-feedback-works` — feedback link shared with customers (per order), star rating input (1-5), text feedback, thank-you page after submission, public /feedback/{orderId} route
    - Page 2: **Reviewing Feedback** — slug: `reviewing-feedback` — feedback list page, filtering by date/rating, viewing individual feedback details, using feedback to improve operations

    **Section 12: Admin & System Guide** (NEW, 4 pages, per D-12)
    - slug: `admin-system-guide`, icon: `Shield`, accent_color: `#64748B`
    - role_codes: `['FOUNDER_ADMIN', 'TECH_LEAD']`
    - sort_order: 12
    - Page 1: **User Management** — slug: `user-management` — user list page, creating users, assigning roles, resetting passwords, user status (active/suspended)
    - Page 2: **Roles & Permissions** — slug: `roles-permissions` — permission matrix page, understanding the 8 roles, what each permission grants (VIEW_ROLE_SCOPED, CREATE_TASK, MANAGE_INVENTORY, etc.), how role_codes control guide visibility
    - Page 3: **System Settings** — slug: `system-settings` — settings page, leaderboard toggle, system-level configuration, managing brands, channels, zones
    - Page 4: **Delegations** — slug: `delegations` — delegation list, creating delegations (delegate permissions temporarily), delegation expiry, use cases for delegations

    **FINAL STEPS in seed.ts:**

    1. Update the guide seeding loop to use `computeReadTime(page.content)` instead of `Math.ceil(JSON.stringify(page.content).length / 1000)`.

    2. Update the console.log at the end to show the correct count:
    ```typescript
    console.log('  - 12 guide sections with 39+ pages');
    ```

    3. Ensure ALL guide content uses `JSON.stringify({type:'doc', content:[...]})` format for the content field.

    4. Double-check that NO section uses `PRODUCTION_LEAD` or `FRONTEND_EXPERIENCE_LEAD` — only the 8 RoleCode enum values.
  </action>
  <verify>
    <automated>cd backend && npx tsc --noEmit --project tsconfig.json</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "Kitchen Operations" backend/prisma/seed.ts
    - grep -q "Evidence & Approvals" backend/prisma/seed.ts
    - grep -q "Governance & Decisions" backend/prisma/seed.ts
    - grep -q "Analytics & Dashboard" backend/prisma/seed.ts
    - grep -q "Notifications" backend/prisma/seed.ts
    - grep -q "Events & Bookings" backend/prisma/seed.ts
    - grep -q "Customer Feedback" backend/prisma/seed.ts
    - grep -q "Admin & System Guide" backend/prisma/seed.ts
    - grep -q "computeReadTime" backend/prisma/seed.ts
    - grep -v "PRODUCTION_LEAD" backend/prisma/seed.ts (must NOT contain stale code)
    - grep -v "FRONTEND_EXPERIENCE_LEAD" backend/prisma/seed.ts (must NOT contain stale code)
    - grep -q "BACKEND_LEAD" backend/prisma/seed.ts
    - grep -q "FRONTEND_LEAD" backend/prisma/seed.ts
    - grep -c "slug:" backend/prisma/seed.ts returns 50+ (12 sections + 39+ pages)
  </acceptance_criteria>
  <done>
    seed.ts contains 12 guide sections with 39+ pages of real walkthrough content. All role codes are correct (no stale codes). Read time calculation uses word count. Content uses valid Tiptap JSON format. Seed is idempotent via deleteMany + create pattern.
  </done>
</task>

</tasks>

<verification>
1. `cd backend && npx tsc --noEmit` compiles without errors
2. `cd backend && npx prisma db seed` runs successfully and seeds all 12 sections
3. No instances of `PRODUCTION_LEAD` or `FRONTEND_EXPERIENCE_LEAD` in seed.ts
4. `grep -c "title:" backend/prisma/seed.ts` shows 50+ entries (sections + pages)
5. Manual: Open seeded guide page in CMS editor — content loads and is editable
6. Manual: Navigate /guide as BACKEND_LEAD — Kitchen Operations section is visible
</verification>

<success_criteria>
- 12 guide sections seeded covering all major feature areas (SEED-01)
- Each section has 2-5 pages with real step-by-step walkthroughs from actual codebase (SEED-02)
- Sections mapped to correct roles per RBAC permissions (SEED-03)
- All content stored as Tiptap JSON, fully editable in CMS (SEED-04)
- No stale role codes in any section
- Seed is idempotent (runs multiple times without errors)
- Read time calculated from word count, not JSON string length
</success_criteria>

<output>
After completion, create `.planning/phases/17-search-preview-and-content-seeding/17-03-SUMMARY.md`
</output>
