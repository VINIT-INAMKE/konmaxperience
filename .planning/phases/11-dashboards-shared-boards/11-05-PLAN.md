---
phase: 11-dashboards-shared-boards
plan: 05
type: execute
wave: 2
depends_on:
  - 11-01
files_modified:
  - frontend/app/(ops)/boards/missions/page.tsx
  - frontend/app/(ops)/boards/quests/page.tsx
  - frontend/app/(ops)/boards/wins/page.tsx
  - frontend/app/(ops)/boards/evidence/page.tsx
  - frontend/components/ops/boards/MissionCard.tsx
  - frontend/components/ops/boards/QuestKanbanColumn.tsx
  - frontend/components/ops/boards/QuestKanbanCard.tsx
  - frontend/components/ops/boards/WinsTimeline.tsx
  - frontend/components/ops/boards/EvidenceFeedCard.tsx
autonomous: true
requirements:
  - DASH-06

must_haves:
  truths:
    - "Mission board shows responsive card grid with MagicCard, progress bars, and click-to-detail"
    - "Quest board shows 3-column kanban (Not Started / In Progress / Completed) with filter by mission and assignee"
    - "Wins timeline shows vertical feed of completed quests and validated tasks in reverse chronological order"
    - "Evidence feed shows scrollable card list with thumbnails, status badges, and filter by status"
    - "All boards accessible to all authenticated users (no permission gate)"
  artifacts:
    - path: "frontend/app/(ops)/boards/missions/page.tsx"
      provides: "Mission board page"
      contains: "Mission Board"
    - path: "frontend/app/(ops)/boards/quests/page.tsx"
      provides: "Quest board kanban page"
      contains: "Quest Board"
    - path: "frontend/app/(ops)/boards/wins/page.tsx"
      provides: "Wins timeline page"
      contains: "Wins & Milestones"
    - path: "frontend/app/(ops)/boards/evidence/page.tsx"
      provides: "Evidence feed page"
      contains: "Evidence Feed"
    - path: "frontend/components/ops/boards/MissionCard.tsx"
      provides: "Mission card with MagicCard"
      contains: "MagicCard"
    - path: "frontend/components/ops/boards/QuestKanbanColumn.tsx"
      provides: "Kanban column component"
      contains: "min-w-"
    - path: "frontend/components/ops/boards/WinsTimeline.tsx"
      provides: "Timeline feed component"
      contains: "quest_completed"
  key_links:
    - from: "frontend/app/(ops)/boards/missions/page.tsx"
      to: "/missions"
      via: "apiClient.get('/missions')"
      pattern: "apiClient.get.*missions"
    - from: "frontend/app/(ops)/boards/wins/page.tsx"
      to: "/analytics/wins"
      via: "apiClient.get('/analytics/wins')"
      pattern: "analytics/wins"
    - from: "frontend/app/(ops)/boards/evidence/page.tsx"
      to: "/evidence/feed"
      via: "apiClient.get('/evidence/feed')"
      pattern: "evidence/feed"
---

<objective>
Build 4 shared board pages: mission board (card grid), quest board (kanban), wins timeline (vertical feed), and evidence feed (scrollable cards). All accessible to every authenticated user.

Purpose: DASH-06 requires shared team visibility into missions, quests, wins, and evidence — celebrating progress and enabling cross-team awareness without drilling into individual management pages.

Output: 4 new board pages under /boards/*, 5 new board components.
</objective>

<execution_context>
@D:/aditee/konmaxperience/.claude/get-shit-done/workflows/execute-plan.md
@D:/aditee/konmaxperience/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-dashboards-shared-boards/11-CONTEXT.md
@.planning/phases/11-dashboards-shared-boards/11-RESEARCH.md
@.planning/phases/11-dashboards-shared-boards/11-UI-SPEC.md
@.planning/phases/11-dashboards-shared-boards/11-01-SUMMARY.md

<interfaces>
<!-- Existing types and new analytics types -->

From frontend/lib/types/analytics.ts (from Plan 01):
```typescript
export interface WinsEntry {
  id: string;
  type: 'quest_completed' | 'task_validated';
  title: string;
  actor_name: string;
  actor_role: string;
  timestamp: string;
}
export interface EvidenceFeedEntry {
  id: string; task_id: string; uploaded_by: string;
  type: 'photo' | 'doc' | 'video' | 'link' | 'note';
  url: string; notes: string | null;
  approval_status: 'pending' | 'approved' | 'rejected';
  created_at: string;
  task?: { title: string };
  uploader_user?: { id: string; name: string };
}
```

From existing frontend types:
```typescript
// Mission (from missions CRUD pages)
interface Mission {
  id: string; name: string; description: string;
  phase: string; status: string; progress: number;
  owner_user_id: string; deadline: string | null;
  owner?: { id: string; name: string };
}
// Quest (from quests CRUD pages)
interface Quest {
  id: string; title: string; description: string;
  mission_id: string; status: string; progress: number;
  owner_user_id: string;
  owner?: { id: string; name: string };
  _count?: { tasks: number };
  mission?: { name: string };
}
```

Backend endpoints:
- GET /missions -> Mission[] (existing)
- GET /quests -> Quest[] (existing)
- GET /analytics/wins?limit=20&cursor=ISO -> WinsEntry[] (Plan 01)
- GET /evidence/feed?status=X&limit=20&cursor=ISO -> EvidenceFeedEntry[] (Plan 01)
- GET /users -> User[] (existing, for assignee filter)

MagicCard usage:
```typescript
import { MagicCard } from '@/components/ui/magic-card';
// <MagicCard gradientColor="#1a1a2e"> established in Phase 2
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Mission board + Quest board pages and components</name>
  <files>
    frontend/app/(ops)/boards/missions/page.tsx,
    frontend/app/(ops)/boards/quests/page.tsx,
    frontend/components/ops/boards/MissionCard.tsx,
    frontend/components/ops/boards/QuestKanbanColumn.tsx,
    frontend/components/ops/boards/QuestKanbanCard.tsx
  </files>
  <read_first>
    frontend/app/(ops)/missions/page.tsx,
    frontend/app/(ops)/missions/[id]/page.tsx,
    frontend/components/ui/magic-card.tsx,
    frontend/components/ui/animated-circular-progress-bar.tsx,
    frontend/lib/types/analytics.ts,
    frontend/lib/api-client.ts
  </read_first>
  <action>
    **1. Create `frontend/components/ops/boards/MissionCard.tsx`:**
    - `'use client'` component accepting a mission object prop (type inline or from types)
    - Uses `MagicCard` with `gradientColor="#1a1a2e"` (established project pattern)
    - Layout inside card:
      - Mission name: 20px font-bold (heading)
      - Phase label: 12px font-bold text-muted-foreground (e.g. "Foundation Phase")
      - Progress bar: shadcn `Progress` component with `value={mission.progress}`
      - Progress text: 14px font-bold (e.g. "72%") beside progress bar
      - Owner name: 14px text-muted-foreground (e.g. `mission.owner?.name ?? 'Unassigned'`)
      - Deadline: 14px text-muted-foreground, formatted with `format(new Date(mission.deadline), 'MMM d, yyyy')` from date-fns, or "No deadline" if null
    - Wrapped in `Link` from `next/link` to `/missions/${mission.id}`
    - Card padding: 16px (p-4)

    **2. Create `frontend/app/(ops)/boards/missions/page.tsx`:**
    - `'use client'` page
    - Query: `useQuery({ queryKey: ['missions'], queryFn: () => apiClient.get<any[]>('/missions') })`
    - Page heading: "Mission Board"
    - Filter: optional status select (All / active / completed / draft) using client-side filtering
    - Grid: `grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 xl:grid-cols-4 gap-6`
    - Map missions to `<MissionCard>` components
    - Empty state: "No missions yet" subtitle "Create a mission to get started."
    - Loading: grid of 4 Skeleton cards
    - BlurFade page wrapper

    **3. Create `frontend/components/ops/boards/QuestKanbanCard.tsx`:**
    - `'use client'` component accepting quest prop
    - Card with 16px padding, white/card background
    - Quest title: 14px font-bold (truncate to 1 line)
    - Progress bar: shadcn Progress with `value={quest.progress}`
    - Owner: Avatar (shadcn) with AvatarFallback showing initials + owner name text 12px
    - Task count: 12px text-muted-foreground (e.g. "5/8 tasks" using `_count?.tasks` if available, else just progress %)
    - Click: Link to `/missions/${quest.mission_id}` (quest drill-down goes through mission detail)

    **4. Create `frontend/components/ops/boards/QuestKanbanColumn.tsx`:**
    - `'use client'` component accepting `title: string`, `quests: Quest[]`, `accentClass: string` (for left border color)
    - Column layout:
      - Header: `flex items-center gap-2 mb-3`
        - Left border accent: 3px solid via `border-l-4 ${accentClass}` on column container (not header)
        - Status name: 12px font-bold uppercase
        - Count badge: `Badge variant="secondary"` showing quest count (e.g. "3 quests")
      - Quest list: `space-y-3` with ScrollArea for overflow if many quests
      - Column min-width: `min-w-[240px]`
    - "In Progress" column: sorted by progress desc (most complete at top per UI-SPEC)

    **5. Create `frontend/app/(ops)/boards/quests/page.tsx`:**
    - `'use client'` page
    - Queries:
      - `useQuery({ queryKey: ['quests'], queryFn: () => apiClient.get<any[]>('/quests') })`
      - `useQuery({ queryKey: ['missions'], queryFn: () => apiClient.get<any[]>('/missions') })` for filter dropdown
      - `useQuery({ queryKey: ['users'], queryFn: () => apiClient.get<any[]>('/users') })` for assignee filter
    - Page heading: "Quest Board"
    - Filter bar: `flex gap-4` with Mission Select (200px) and Assignee Select (200px) — client-side filtering per Research Pattern 6
    - Client-side filter logic:
      ```typescript
      const filtered = quests.filter(q =>
        (!missionFilter || q.mission_id === missionFilter) &&
        (!assigneeFilter || q.owner_user_id === assigneeFilter)
      );
      ```
    - Partition into 3 groups:
      - Not Started: `status === 'not_started'` or `status === 'draft'`
      - In Progress: `status === 'in_progress'` or `status === 'active'`
      - Completed: `status === 'completed'`
    - Kanban layout: `grid grid-cols-1 md:grid-cols-3 gap-6`
    - Column accent classes per UI-SPEC color contract:
      - Not Started: `border-l-muted-foreground/40`
      - In Progress: `border-l-primary`
      - Completed: `border-l-emerald-500`
    - Empty state per column: "No quests" in muted text
    - Page-level empty: "No quests found" subtitle "Try adjusting your filters."
    - BlurFade page wrapper
    - No drag-and-drop (deferred to v2 per CONTEXT.md deferred ideas)
  </action>
  <verify>
    <automated>cd D:/aditee/konmaxperience/frontend && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - frontend/app/(ops)/boards/missions/page.tsx contains `'Mission Board'` and `'/missions'` query and `MissionCard`
    - frontend/app/(ops)/boards/missions/page.tsx contains `grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 xl:grid-cols-4`
    - frontend/components/ops/boards/MissionCard.tsx contains `MagicCard` and `gradientColor` and `"#1a1a2e"` and `Progress`
    - frontend/app/(ops)/boards/quests/page.tsx contains `'Quest Board'` and `'/quests'` and `missionFilter` or `mission_id`
    - frontend/app/(ops)/boards/quests/page.tsx contains `not_started` and `in_progress` and `completed` column partitioning
    - frontend/components/ops/boards/QuestKanbanColumn.tsx contains `min-w-[240px]` and `Badge` and column styling
    - frontend/components/ops/boards/QuestKanbanColumn.tsx contains accent class prop usage (border-l)
    - frontend/components/ops/boards/QuestKanbanCard.tsx contains `Avatar` and `Progress` and quest title
    - `cd D:/aditee/konmaxperience/frontend && npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>Mission board page shows responsive MagicCard grid with progress bars; quest board shows 3-column kanban with client-side filtering by mission and assignee</done>
</task>

<task type="auto">
  <name>Task 2: Wins timeline + Evidence feed pages</name>
  <files>
    frontend/app/(ops)/boards/wins/page.tsx,
    frontend/app/(ops)/boards/evidence/page.tsx,
    frontend/components/ops/boards/WinsTimeline.tsx,
    frontend/components/ops/boards/EvidenceFeedCard.tsx
  </files>
  <read_first>
    frontend/lib/types/analytics.ts,
    frontend/lib/types/evidence.ts,
    frontend/lib/api-client.ts,
    frontend/components/ui/badge.tsx,
    frontend/components/ui/blur-fade.tsx
  </read_first>
  <action>
    **1. Create `frontend/components/ops/boards/WinsTimeline.tsx`:**
    - `'use client'` component accepting `entries: WinsEntry[]`
    - Vertical timeline layout:
      - Container: `relative` with vertical line `absolute left-[11px] top-0 bottom-0 w-px bg-border`
      - Each entry: `relative flex gap-3 pl-8 pb-6`
        - Dot: `absolute left-[7px] top-[6px] size-2 rounded-full` with color based on type:
          - `quest_completed`: `bg-emerald-500`
          - `task_validated`: `bg-primary`
        - Content div:
          - Type label in brackets + title: `[Quest Completed] ${entry.title}` at 14px
          - Actor and time: `@${entry.actor_name} · ${formatDistanceToNow(new Date(entry.timestamp), { addSuffix: true })}` at 14px text-muted-foreground
    - Import `formatDistanceToNow` from `date-fns`

    **2. Create `frontend/app/(ops)/boards/wins/page.tsx`:**
    - `'use client'` page
    - State: `entries: WinsEntry[]` initialized to `[]`, `cursor: string | undefined`, `hasMore: boolean` default true
    - Initial query: `useQuery({ queryKey: ['wins', cursor], queryFn: () => apiClient.get<WinsEntry[]>(\`/analytics/wins?limit=20${cursor ? \`&cursor=${cursor}\` : ''}\`) })`
    - On data loaded: append to entries (not replace, for "load more" pattern)
    - Use a simpler pattern: keep all entries in state, "Load more entries" button sets cursor to last entry's timestamp and fetches more, appends
    - Actually, simpler approach: use `useInfiniteQuery` or manual load-more:
      ```typescript
      const [entries, setEntries] = useState<WinsEntry[]>([]);
      const [cursor, setCursor] = useState<string | undefined>();
      const [hasMore, setHasMore] = useState(true);

      const { isLoading, isFetching } = useQuery({
        queryKey: ['wins', cursor],
        queryFn: () => apiClient.get<WinsEntry[]>(
          `/analytics/wins?limit=20${cursor ? `&cursor=${cursor}` : ''}`
        ),
        // On success, append entries
      });
      ```
    - Use `useEffect` watching query data to append: on new data received, if data.length < 20, setHasMore(false), then setEntries(prev => cursor ? [...prev, ...data] : data)
    - Page heading: "Wins & Milestones"
    - Render `<WinsTimeline entries={entries} />`
    - "Load more entries" Button at bottom (disabled when !hasMore or isFetching), onClick sets cursor to last entry's timestamp
    - Empty state: "No milestones yet" subtitle "Completed quests and validated tasks will appear here."
    - BlurFade page wrapper

    **3. Create `frontend/components/ops/boards/EvidenceFeedCard.tsx`:**
    - `'use client'` component accepting `evidence: EvidenceFeedEntry`
    - Card component with flex row layout:
      - Left: 64x64 thumbnail area (`size-16 rounded-md overflow-hidden flex-shrink-0`)
        - If type === 'photo': `<img src={evidence.url} alt="" className="size-full object-cover" loading="lazy" />`
        - If type === 'video': lucide `Video` icon in 64x64 gray square (`bg-muted flex items-center justify-center`)
        - If type === 'doc': lucide `FileText` icon in gray square
        - If type === 'link': lucide `Link` icon in gray square
        - If type === 'note': lucide `StickyNote` icon in gray square
        - Icon size: `size-6 text-muted-foreground`
      - Right: content area (`flex-1 min-w-0 space-y-1`)
        - Task name: 14px font-bold truncate — `evidence.task?.title ?? 'Unknown task'`
        - Uploader and time: 14px text-muted-foreground — `Uploaded by ${evidence.uploader_user?.name ?? 'Unknown'} · ${formatDistanceToNow(new Date(evidence.created_at), { addSuffix: true })}`
        - Evidence type: 12px text-muted-foreground — `Evidence type: ${evidence.type}`
        - Approval status badge (top-right of card): Badge with colors per UI-SPEC:
          - approved: `bg-emerald-500/15 text-emerald-700`
          - pending: `bg-amber-500/15 text-amber-600`
          - rejected: `bg-destructive/10 text-destructive`
    - Wrapped in Link to `/missions` (task detail is at existing task route — use task_id for navigation if route exists, otherwise link to missions page)

    **4. Create `frontend/app/(ops)/boards/evidence/page.tsx`:**
    - `'use client'` page
    - State: status filter (`'all' | 'pending' | 'approved' | 'rejected'`), entries array, cursor, hasMore
    - Query: `apiClient.get<EvidenceFeedEntry[]>(\`/evidence/feed?limit=20${statusParam}${cursorParam}\`)`
      - statusParam: `status !== 'all' ? \`&status=${status}\` : ''`
    - Reset entries and cursor when status filter changes
    - Page heading: "Evidence Feed"
    - Filter bar: 4 buttons (All / Pending / Approved / Rejected) — client-side via query params to backend feed endpoint
    - Render `<EvidenceFeedCard>` for each entry in `space-y-4`
    - "Load more evidence" Button at bottom
    - Empty state: "No evidence submitted" subtitle "Evidence submitted to tasks will appear here."
    - BlurFade page wrapper
  </action>
  <verify>
    <automated>cd D:/aditee/konmaxperience/frontend && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - frontend/app/(ops)/boards/wins/page.tsx contains `'Wins & Milestones'` and `'/analytics/wins'` and `'Load more entries'`
    - frontend/app/(ops)/boards/wins/page.tsx contains cursor-based pagination logic
    - frontend/components/ops/boards/WinsTimeline.tsx contains `quest_completed` and `task_validated` and `bg-emerald-500` and `bg-primary` and `formatDistanceToNow`
    - frontend/components/ops/boards/WinsTimeline.tsx contains vertical line (`w-px bg-border`)
    - frontend/app/(ops)/boards/evidence/page.tsx contains `'Evidence Feed'` and `'/evidence/feed'` and `'Load more evidence'`
    - frontend/app/(ops)/boards/evidence/page.tsx contains status filter buttons (All, Pending, Approved, Rejected)
    - frontend/components/ops/boards/EvidenceFeedCard.tsx contains `size-16` thumbnail area and `FileText` and `Video` and `Link` and `StickyNote` icon fallbacks
    - frontend/components/ops/boards/EvidenceFeedCard.tsx contains `bg-emerald-500/15` and `bg-amber-500/15` and `bg-destructive/10` badge colors
    - `cd D:/aditee/konmaxperience/frontend && npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>Wins timeline shows vertical feed with colored dots and relative timestamps; evidence feed shows scrollable cards with thumbnails, status badges, and load-more pagination</done>
</task>

</tasks>

<verification>
- `cd D:/aditee/konmaxperience/frontend && npx tsc --noEmit` exits 0
- All 4 board pages render at /boards/missions, /boards/quests, /boards/wins, /boards/evidence
- No permission gate on any board page (all authenticated users per D-18)
</verification>

<success_criteria>
1. Mission board: MagicCard grid with progress bars, click navigates to mission detail
2. Quest board: 3-column kanban (Not Started/In Progress/Completed) with mission and assignee filters
3. Wins timeline: vertical feed with colored dots, type labels, actor names, relative timestamps, load more
4. Evidence feed: card list with 64x64 thumbnails (image or icon fallback), status badges, status filter, load more
5. All boards accessible to all authenticated users
</success_criteria>

<output>
After completion, create `.planning/phases/11-dashboards-shared-boards/11-05-SUMMARY.md`
</output>
