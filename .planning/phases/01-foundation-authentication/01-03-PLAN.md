---
phase: 01-foundation-authentication
plan: 03
type: execute
wave: 3
depends_on: ["01-01", "01-02"]
files_modified:
  - apps/web/middleware.ts
  - apps/web/lib/auth.ts
  - apps/web/lib/api-client.ts
  - apps/web/lib/stores/auth-store.ts
  - apps/web/lib/providers.tsx
  - apps/web/app/layout.tsx
  - apps/web/app/(auth)/layout.tsx
  - apps/web/app/(auth)/login/page.tsx
  - apps/web/app/(auth)/forgot-password/page.tsx
  - apps/web/app/(auth)/set-password/page.tsx
  - apps/web/app/(auth)/reset-password/page.tsx
  - apps/web/app/(ops)/layout.tsx
  - apps/web/app/(ops)/dashboard/page.tsx
  - apps/web/app/(ops)/admin/users/page.tsx
  - apps/web/app/(ops)/admin/permissions/page.tsx
  - apps/web/components/ops/Sidebar.tsx
  - apps/web/components/ops/AdminUserFilter.tsx
  - apps/web/components/ops/CreateUserDialog.tsx
  - apps/web/components/ops/PermissionMatrix.tsx
autonomous: false
requirements:
  - AUTH-01
  - AUTH-02
  - AUTH-04
  - AUTH-05
  - AUTH-06
user_setup:
  - service: mailersend
    why: "Transactional emails for password setup/reset"
    env_vars:
      - name: MAILERSEND_API_KEY
        source: "MailerSend Dashboard -> API Tokens -> Create new token"
      - name: MAILERSEND_FROM_EMAIL
        source: "MailerSend Dashboard -> Domains -> Verified domain sender address"
    dashboard_config:
      - task: "Verify a sender domain or email"
        location: "MailerSend Dashboard -> Domains -> Add domain"
  - service: neon
    why: "PostgreSQL database hosting"
    env_vars:
      - name: DATABASE_URL
        source: "Neon Console -> Project -> Connection Details -> Connection string"
    dashboard_config:
      - task: "Create a Neon project"
        location: "Neon Console -> New Project"
must_haves:
  truths:
    - "User can navigate to /login and see the login form"
    - "User can log in with valid credentials and be redirected to /dashboard"
    - "User who is not logged in is redirected from /dashboard to /login"
    - "Logged-in user session persists across browser refresh (cookie-based)"
    - "User can log out from current device or all devices"
    - "Admin can navigate to /admin/users and see the team member list"
    - "Admin can create a new team member which triggers a password setup email"
    - "Admin can navigate to /admin/permissions and see the permission matrix"
    - "Admin can toggle permissions for non-admin roles and save changes"
    - "Non-admin user does not see Admin nav items in sidebar"
    - "Dashboard shell displays user name, role, and placeholder content"
  artifacts:
    - path: "apps/web/middleware.ts"
      provides: "Edge JWT verification with jose"
      contains: "jwtVerify"
    - path: "apps/web/app/(auth)/login/page.tsx"
      provides: "Login page with email/password form"
      contains: "Sign in"
    - path: "apps/web/app/(ops)/layout.tsx"
      provides: "Ops layout with sidebar"
      contains: "Sidebar"
    - path: "apps/web/app/(ops)/admin/users/page.tsx"
      provides: "Admin user management page"
      contains: "Team"
    - path: "apps/web/app/(ops)/admin/permissions/page.tsx"
      provides: "Admin permission settings page"
      contains: "Permissions"
    - path: "apps/web/components/ops/Sidebar.tsx"
      provides: "Role-scoped navigation sidebar"
      contains: "FOUNDER_ADMIN"
  key_links:
    - from: "apps/web/middleware.ts"
      to: "apps/api/src/auth/auth.controller.ts"
      via: "jose verifies JWT signed by NestJS auth service"
      pattern: "jwtVerify.*JWT_SECRET"
    - from: "apps/web/app/(auth)/login/page.tsx"
      to: "apps/api/src/auth/auth.controller.ts"
      via: "POST /auth/login API call"
      pattern: "auth/login"
    - from: "apps/web/app/(ops)/admin/permissions/page.tsx"
      to: "apps/api/src/roles/roles.controller.ts"
      via: "GET /roles and PATCH /roles/:id/permissions"
      pattern: "roles.*permissions"
    - from: "apps/web/components/ops/Sidebar.tsx"
      to: "apps/web/lib/stores/auth-store.ts"
      via: "reads user role to filter nav items"
      pattern: "useAuthStore"
---

<objective>
Build the complete Next.js 16 frontend: auth pages (login, forgot/set/reset password), edge middleware for JWT verification, ops layout with role-scoped sidebar, admin user management page, admin permission settings page, and dashboard shell.

Purpose: Deliver the user-facing interface that completes the auth flow end-to-end, allows admins to manage team and permissions, and scaffolds the ops layout for all future phases.

Output: Working frontend where users can log in, admins can manage team and permissions, all routes are protected by edge middleware, and the role-scoped sidebar provides navigation structure for the full application.
</objective>

<execution_context>
@D:/aditee/konmaxperience/.claude/get-shit-done/workflows/execute-plan.md
@D:/aditee/konmaxperience/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-authentication/01-CONTEXT.md
@.planning/phases/01-foundation-authentication/01-RESEARCH.md
@.planning/phases/01-foundation-authentication/01-UI-SPEC.md
@.planning/phases/01-foundation-authentication/01-01-SUMMARY.md
@.planning/phases/01-foundation-authentication/01-02-SUMMARY.md
@contextdocs/dev_spec.md

<interfaces>
<!-- Key API contracts from Plan 02 that this plan consumes -->

POST /auth/login — Body: { email, password } → Response: { accessToken, user: { id, name, email, roleCode, roleName } } + sets httpOnly cookies (access_token, refresh_token)
POST /auth/refresh — Reads refresh_token cookie → Response: { accessToken } + sets new access_token cookie
POST /auth/logout — Clears cookies → Response: { message }
POST /auth/logout-all — Clears cookies, revokes all tokens → Response: { message }
POST /auth/forgot-password — Body: { email } → Response: { message }
POST /auth/reset-password — Body: { token, password } → Response: { message }
POST /auth/set-password — Body: { token, password } → Response: { message }
GET /auth/me — Returns UserProfile
GET /users — Returns UserProfile[] (FOUNDER_ADMIN only)
POST /users — Body: { name, email, roleId } → Returns created user (MANAGE_RBAC)
PATCH /users/:id — Body: { name?, status? } → Returns updated user (MANAGE_RBAC)
POST /users/:id/reset-password → Returns { message } (MANAGE_RBAC)
POST /users/:id/deactivate → Returns updated user (MANAGE_RBAC)
GET /roles — Returns Role[] (any authenticated user)
PATCH /roles/:id/permissions — Body: { permissions: string[] } → Returns updated role (MANAGE_RBAC)

From packages/types/src/auth.ts:
```typescript
export interface JwtPayload { userId: string; roleCode: string; }
export interface LoginResponse { accessToken: string; user: { id, name, email, roleCode, roleName } }
```

From packages/types/src/roles.ts:
```typescript
export enum RoleCode { FOUNDER_ADMIN, FRONTEND_LEAD, BACKEND_LEAD, BI_LEAD, PROCUREMENT_LEAD, TALENT_LEAD, TECH_LEAD, DESIGN_OUTREACH_LEAD }
export const ROLE_DISPLAY_NAMES: Record<RoleCode, string>
```

From packages/types/src/permissions.ts:
```typescript
export enum Permission { VIEW_ALL, VIEW_ROLE_SCOPED, ... (15 total) }
export const PERMISSION_DISPLAY_NAMES: Record<Permission, string>
export const PERMISSION_DESCRIPTIONS: Record<Permission, string>
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement edge middleware, auth utilities, API client, and all auth pages</name>
  <files>
    apps/web/middleware.ts,
    apps/web/lib/auth.ts,
    apps/web/lib/api-client.ts,
    apps/web/lib/stores/auth-store.ts,
    apps/web/lib/providers.tsx,
    apps/web/app/layout.tsx,
    apps/web/app/(auth)/layout.tsx,
    apps/web/app/(auth)/login/page.tsx,
    apps/web/app/(auth)/forgot-password/page.tsx,
    apps/web/app/(auth)/set-password/page.tsx,
    apps/web/app/(auth)/reset-password/page.tsx
  </files>
  <read_first>
    apps/web/app/layout.tsx
    apps/web/package.json
    packages/types/src/auth.ts
    packages/types/src/roles.ts
    packages/types/src/permissions.ts
    .planning/phases/01-foundation-authentication/01-UI-SPEC.md
    .planning/phases/01-foundation-authentication/01-RESEARCH.md
  </read_first>
  <action>
1. Create `apps/web/middleware.ts` using jose (NOT jsonwebtoken -- edge runtime incompatible):
   ```typescript
   import { jwtVerify } from 'jose';
   import { NextRequest, NextResponse } from 'next/server';

   const JWT_SECRET = new TextEncoder().encode(process.env.JWT_SECRET!);
   const PUBLIC_PATHS = ['/login', '/forgot-password', '/set-password', '/reset-password'];

   export async function middleware(request: NextRequest) {
     const { pathname } = request.nextUrl;

     if (PUBLIC_PATHS.some(p => pathname.startsWith(p))) {
       return NextResponse.next();
     }

     const token = request.cookies.get('access_token')?.value;
     if (!token) {
       const loginUrl = new URL('/login', request.url);
       loginUrl.searchParams.set('redirect', pathname);
       return NextResponse.redirect(loginUrl);
     }

     try {
       const { payload } = await jwtVerify(token, JWT_SECRET);
       const response = NextResponse.next();
       response.headers.set('x-user-id', payload.userId as string);
       response.headers.set('x-role-code', payload.roleCode as string);
       return response;
     } catch {
       const loginUrl = new URL('/login', request.url);
       loginUrl.searchParams.set('redirect', pathname);
       return NextResponse.redirect(loginUrl);
     }
   }

   export const config = {
     matcher: ['/((?!_next/static|_next/image|favicon.ico|api).*)'],
   };
   ```

2. Create `apps/web/lib/api-client.ts`:
   - Typed fetch wrapper to NestJS API at NEXT_PUBLIC_API_URL
   - `apiClient.get<T>(path)`, `apiClient.post<T>(path, body)`, `apiClient.patch<T>(path, body)`
   - All requests include `credentials: 'include'` to send httpOnly cookies cross-origin
   - On 401 response, attempt silent refresh: call POST /auth/refresh; if refresh succeeds, retry original request once; if refresh fails, clear auth store and redirect to /login
   - Return typed response or throw ApiError with status and message

3. Create `apps/web/lib/stores/auth-store.ts`:
   - Zustand store with persist middleware (sessionStorage)
   - State: `user: { id, name, email, roleCode, roleName } | null`, `isAuthenticated: boolean`
   - Actions: `setUser(user)`, `clearUser()`, `hasPermission(permission: string): boolean` (this queries the API or reads from a cached permissions list fetched at login)
   - On login success, store user data. On logout, clear.

4. Create `apps/web/lib/auth.ts`:
   - `login(email: string, password: string)`: POST /auth/login, on success set user in auth store and return redirect URL (from ?redirect param or default /dashboard)
   - `logout()`: POST /auth/logout, clear auth store, redirect to /login
   - `logoutAll()`: POST /auth/logout-all, clear auth store, redirect to /login
   - `refreshSession()`: POST /auth/refresh -- called by api-client on 401
   - `forgotPassword(email: string)`: POST /auth/forgot-password
   - `resetPassword(token: string, password: string)`: POST /auth/reset-password
   - `setPassword(token: string, password: string)`: POST /auth/set-password

5. Create `apps/web/lib/providers.tsx`:
   - QueryClientProvider from @tanstack/react-query
   - Wrap children with providers
   - Import in root layout

6. Update `apps/web/app/layout.tsx`:
   - Import Geist and Geist_Mono from next/font/google
   - Apply `className={`${geist.variable} ${geistMono.variable} font-sans antialiased`}` to body
   - html with `lang="en"` and `className="dark"` (dark mode default per UI-SPEC)
   - Wrap {children} with Providers component

7. Create `apps/web/app/(auth)/layout.tsx`:
   - Full-viewport centered layout: `min-h-screen flex items-center justify-center`
   - Background: `bg-background`
   - No sidebar, no nav -- completely isolated auth flow

8. Create `apps/web/app/(auth)/login/page.tsx`:
   Follow UI-SPEC Login Page contract exactly:
   - Card component: `max-w-[400px] w-full` padding 24px (p-6), rounded-xl, border, shadow-sm
   - Brand headline: "Welcome back" in Display typography (text-[28px] font-semibold leading-[1.1])
   - Subtitle: "Sign in to Konma Xperience" (text-sm text-muted-foreground, centered)
   - Email Input: label "Email address", type="email", autocomplete="email"
   - Password Input: label "Password", type="password", autocomplete="current-password", show/hide toggle with Eye/EyeOff icons from lucide-react, 44px touch target button, aria-label="Show password" when hidden / aria-label="Hide password" when visible
   - "Forgot password?" link: right-aligned below password, text-sm, accent underline on hover, links to /forgot-password
   - Primary CTA Button: full-width, "Sign in"
   - States: Default (fields empty, CTA enabled), Loading (spinner + "Signing in...", fields disabled), Error (Alert above form: "Incorrect email or password. Check your details and try again."), Field validation (inline below field: "Email is required" / "Password is required" in text-xs text-destructive), Rate-limited (Alert: "Too many attempts. Try again in 5 minutes.")
   - Use react-hook-form with zod resolver for validation
   - On success: redirect to ?redirect param or /dashboard
   - On 401: show error alert
   - On 429: show rate limit alert

9. Create `apps/web/app/(auth)/forgot-password/page.tsx`:
   Follow UI-SPEC Forgot Password contract:
   - Back arrow link to /login: ChevronLeft icon + "Back to sign in"
   - Heading: "Reset your password"
   - Subtitle: "Enter your email and we'll send a reset link."
   - Email Input
   - CTA: "Send reset link"
   - States: Default, Loading (spinner + "Sending..."), Success (replaces form: CheckCircle icon + "Check your email" + "We've sent a reset link to [email]. It expires in 15 minutes." + "Resend email" ghost button enabled after 60s countdown), Error (Alert: "No account found with that email address.")

10. Create `apps/web/app/(auth)/set-password/page.tsx`:
    Follow UI-SPEC Set Password contract:
    - Heading: "Set your password"
    - Subtitle: "Create a password to access Konma Xperience."
    - New Password Input with show/hide toggle (aria-label="Show password"/"Hide password")
    - Confirm Password Input with show/hide toggle
    - Password strength indicator: 3-segment bar (destructive/amber-500/green-500)
    - Password requirements checklist: "At least 8 characters", "At least one number" -- check icon turns accent when met
    - CTA: "Set password and sign in"
    - States: Passwords don't match (inline error), Expired token (full-card error: "This setup link has expired. Ask your admin to resend the invitation."), Used token (full-card error: "This link has already been used. Sign in or reset your password."), Success (redirect to /login with toast: "Password set. You can now sign in.")
    - Read token from URL ?token= query param

11. Create `apps/web/app/(auth)/reset-password/page.tsx`:
    Follow UI-SPEC Reset Password contract:
    - Identical structure to set-password but:
    - Heading: "Choose a new password"
    - Subtitle: "Enter a new password for your account."
    - CTA: "Reset password and sign in"
    - Same states as set-password
  </action>
  <verify>
    <automated>cd D:/aditee/konmaxperience/apps/web && npx next build 2>&1 | tail -5 && echo "BUILD_OK"</automated>
  </verify>
  <acceptance_criteria>
    - apps/web/middleware.ts contains "jwtVerify" from "jose" (NOT jsonwebtoken)
    - apps/web/middleware.ts contains PUBLIC_PATHS array with "/login", "/forgot-password", "/set-password", "/reset-password"
    - apps/web/middleware.ts redirects to /login with ?redirect= param when token missing or invalid
    - apps/web/middleware.ts sets x-user-id and x-role-code headers on successful verification
    - apps/web/lib/api-client.ts contains "credentials: 'include'" for cookie passthrough
    - apps/web/lib/api-client.ts contains 401 retry logic calling /auth/refresh
    - apps/web/lib/stores/auth-store.ts contains "zustand" and "persist" and "sessionStorage"
    - apps/web/app/layout.tsx contains "Geist" font import and "dark" class on html
    - apps/web/app/(auth)/layout.tsx contains "min-h-screen" and "flex" and "items-center" and "justify-center"
    - apps/web/app/(auth)/login/page.tsx contains "Welcome back" and "Sign in" and "Forgot password?"
    - apps/web/app/(auth)/login/page.tsx contains "aria-label" for password toggle (accessibility)
    - apps/web/app/(auth)/login/page.tsx contains "Incorrect email or password" error message
    - apps/web/app/(auth)/forgot-password/page.tsx contains "Reset your password" and "Send reset link"
    - apps/web/app/(auth)/forgot-password/page.tsx contains "Check your email" success state
    - apps/web/app/(auth)/set-password/page.tsx contains "Set your password" and "Set password and sign in"
    - apps/web/app/(auth)/set-password/page.tsx contains password strength indicator (3 segments)
    - apps/web/app/(auth)/reset-password/page.tsx contains "Choose a new password" and "Reset password and sign in"
    - `npx next build` exits 0
  </acceptance_criteria>
  <done>Edge middleware verifies JWT with jose, redirects unauthenticated users to /login. API client with auto-refresh on 401. Zustand auth store persists user session. Login, forgot-password, set-password, and reset-password pages implemented per UI-SPEC with all interaction states, accessibility attributes, and copy.</done>
</task>

<task type="auto">
  <name>Task 2: Implement ops layout with role-scoped sidebar, admin pages, and dashboard shell</name>
  <files>
    apps/web/app/(ops)/layout.tsx,
    apps/web/app/(ops)/dashboard/page.tsx,
    apps/web/app/(ops)/admin/users/page.tsx,
    apps/web/app/(ops)/admin/permissions/page.tsx,
    apps/web/components/ops/Sidebar.tsx,
    apps/web/components/ops/AdminUserFilter.tsx,
    apps/web/components/ops/CreateUserDialog.tsx,
    apps/web/components/ops/PermissionMatrix.tsx
  </files>
  <read_first>
    apps/web/app/layout.tsx
    apps/web/lib/stores/auth-store.ts
    apps/web/lib/api-client.ts
    apps/web/lib/auth.ts
    packages/types/src/roles.ts
    packages/types/src/permissions.ts
    .planning/phases/01-foundation-authentication/01-UI-SPEC.md
    .planning/phases/01-foundation-authentication/01-CONTEXT.md
  </read_first>
  <action>
1. Create `apps/web/components/ops/Sidebar.tsx`:
   Follow UI-SPEC Sidebar contract:
   - Fixed width 240px, bg-card, border-r, full height
   - Top section: navigation items
   - Nav items for ALL roles:
     - Dashboard (LayoutDashboard icon) -> /dashboard
     - Missions (Rocket icon, disabled Badge "Coming soon") -> disabled
   - Nav items for FOUNDER_ADMIN only (under "Admin" heading label):
     - Team (Users icon) -> /admin/users
     - Permissions (Shield icon) -> /admin/permissions
   - Read user roleCode from auth store (useAuthStore)
   - If roleCode === 'FOUNDER_ADMIN', show admin section
   - Active nav item: accent background (bg-primary text-primary-foreground)
   - Inactive: hover bg shifts to muted
   - Bottom section: user avatar (Avatar component with user initials fallback), name, role badge (Badge component), dropdown menu (DropdownMenu) with "Log out" and "Log out everywhere" items
   - "Log out" calls auth.logout(), "Log out everywhere" calls auth.logoutAll()
   - Each nav item: padding md (16px), rounded-md
   - Use lucide-react icons throughout

2. Create `apps/web/app/(ops)/layout.tsx`:
   Follow UI-SPEC Ops Layout contract:
   - Two-column layout: fixed Sidebar (240px) + flex main content area
   - Main content: padding lg (24px = p-6) all sides, max-w-[1200px] for very wide screens
   - Fetch current user on mount via GET /auth/me and populate auth store if not already set
   - If user not in store and API fails, redirect to /login

3. Create `apps/web/app/(ops)/dashboard/page.tsx`:
   Follow UI-SPEC Dashboard Shell contract:
   - Page title: "Dashboard" (text-xl font-semibold)
   - Card with "Dashboard coming in Phase 7" placeholder text
   - Display user's name and role from auth store
   - If FOUNDER_ADMIN: show AdminUserFilter component (Select dropdown "Viewing as: All team members" -- placeholder only, no filter behavior in Phase 1)

4. Create `apps/web/components/ops/AdminUserFilter.tsx`:
   - Select component with placeholder "Viewing as: All team members"
   - Placeholder only -- no filter functionality in Phase 1 (per UI-SPEC)
   - Will be wired to admin user-level filtering in Phase 7

5. Create `apps/web/app/(ops)/admin/users/page.tsx`:
   Follow UI-SPEC Admin User Management contract:
   - Page title: "Team" (text-xl font-semibold)
   - Header row: "Team" left + "Add team member" Button right
   - Fetch users via GET /users using @tanstack/react-query useQuery
   - User table (Table component) with columns:
     - Avatar (initials) + Name
     - Email
     - Role (Badge variant="secondary")
     - Status (Badge: green for active, muted for inactive)
     - Last active (relative time or "-")
     - Actions (DropdownMenu: "Send password reset email", separator, "Deactivate user" in destructive color)
   - Row hover: bg-muted (subtle)
   - Empty state: centered Users icon + "No team members yet" + "Add your first team member to get started." + "Add team member" CTA
   - "Send password reset email" action: POST /users/:id/reset-password, show toast "Password reset email sent to [name]"
   - "Deactivate user" action: show confirmation Dialog ("Deactivate [name]?" body: "They will lose access immediately. You can reactivate them at any time." CTAs: "Keep active" ghost + "Deactivate" destructive), then POST /users/:id/deactivate
   - "Add team member" opens CreateUserDialog

6. Create `apps/web/components/ops/CreateUserDialog.tsx`:
   Follow UI-SPEC Create User modal contract:
   - Dialog with title "Add team member"
   - Form fields: Full name (Input), Email address (Input), Role (Select with all 8 roles by display name from ROLE_DISPLAY_NAMES)
   - Fetch roles via GET /roles for the Select options
   - Footer: "Discard" ghost Button + "Add member" primary Button
   - Use react-hook-form + zod: name @min(2), email @email, roleId @uuid
   - Loading state: "Add member" -> spinner + "Adding..."
   - On success: close dialog + toast "Invitation sent to [name]" + invalidate users query
   - POST /users with { name, email, roleId }

7. Create `apps/web/app/(ops)/admin/permissions/page.tsx`:
   Follow UI-SPEC Admin Permission Settings contract:
   - Page title: "Permissions"
   - Subtitle: "Control what each role can view and do. Changes take effect within 60 seconds." (text-sm text-muted-foreground)
   - Fetch roles via GET /roles
   - Render PermissionMatrix component with roles data

8. Create `apps/web/components/ops/PermissionMatrix.tsx`:
   Follow UI-SPEC Permission Matrix contract:
   - Table with:
     - Column 1: Permission display name (from PERMISSION_DISPLAY_NAMES) + info icon with Tooltip showing enum name and description (from PERMISSION_DESCRIPTIONS)
     - Columns 2-8: one column per non-admin role (7 columns)
     - FOUNDER_ADMIN column: shown with all checkboxes checked and disabled + Lock icon (admin permissions not editable)
   - Each cell: Checkbox centered in 40px x 40px touch target
   - Role headers: Label typography (text-[13px] font-normal), truncated to 2 lines max
   - Column 1 sticky on horizontal scroll
   - Track local state of all permission changes
   - Unsaved changes detection: floating banner at bottom of viewport with "Unsaved changes" text + "Discard" ghost button + "Save changes" primary button
   - "Discard" resets to fetched state
   - "Save changes": for each changed role, PATCH /roles/:id/permissions with the new permissions array. Show spinner + "Saving...". On success: banner disappears + toast "Permissions updated". Invalidate roles query.
   - All 15 permissions as rows, all 8 roles as columns (FOUNDER_ADMIN locked)
  </action>
  <verify>
    <automated>cd D:/aditee/konmaxperience/apps/web && npx next build 2>&1 | tail -5 && echo "FRONTEND_BUILD_OK"</automated>
  </verify>
  <acceptance_criteria>
    - apps/web/components/ops/Sidebar.tsx contains "FOUNDER_ADMIN" check for admin nav section
    - apps/web/components/ops/Sidebar.tsx contains "Log out" and "Log out everywhere" in dropdown menu
    - apps/web/components/ops/Sidebar.tsx contains nav items: "Dashboard", "Missions" (disabled), "Team", "Permissions"
    - apps/web/components/ops/Sidebar.tsx has width 240px (w-60 or w-[240px])
    - apps/web/app/(ops)/layout.tsx contains Sidebar component and flex layout
    - apps/web/app/(ops)/layout.tsx has max-w-[1200px] on main content
    - apps/web/app/(ops)/dashboard/page.tsx contains "Dashboard coming in Phase 7"
    - apps/web/app/(ops)/dashboard/page.tsx contains AdminUserFilter for FOUNDER_ADMIN
    - apps/web/app/(ops)/admin/users/page.tsx contains "Team" heading and "Add team member" button
    - apps/web/app/(ops)/admin/users/page.tsx contains Table with columns for Name, Email, Role, Status, Actions
    - apps/web/app/(ops)/admin/users/page.tsx contains deactivate confirmation Dialog with "They will lose access immediately"
    - apps/web/components/ops/CreateUserDialog.tsx contains "Add team member" title and Select for role assignment
    - apps/web/components/ops/CreateUserDialog.tsx contains "Invitation sent to" toast on success
    - apps/web/app/(ops)/admin/permissions/page.tsx contains "Permissions" title and "Changes take effect within 60 seconds" subtitle
    - apps/web/components/ops/PermissionMatrix.tsx contains all 15 permission display names from PERMISSION_DISPLAY_NAMES
    - apps/web/components/ops/PermissionMatrix.tsx contains "Unsaved changes" floating banner
    - apps/web/components/ops/PermissionMatrix.tsx contains disabled/locked checkboxes for FOUNDER_ADMIN column
    - apps/web/components/ops/PermissionMatrix.tsx contains Tooltip with permission descriptions
    - `npx next build` exits 0
  </acceptance_criteria>
  <done>Ops layout with 240px sidebar, role-scoped nav (admin section visible only to FOUNDER_ADMIN), logout dropdown. Admin user management page with table, create user dialog, deactivate confirmation. Admin permission settings with full 15x8 permission matrix, unsaved changes banner, save with cache invalidation. Dashboard shell with placeholder content and admin user filter.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify complete auth flow end-to-end</name>
  <files>none -- verification checkpoint only</files>
  <action>
    Human verifies the complete authentication and authorization system end-to-end:

    1. Start both apps: `cd apps/api && npm run start:dev` and `cd apps/web && npm run dev`
    2. Navigate to http://localhost:3000 -- should redirect to /login
    3. Log in with seeded admin credentials (founder@konmaxperience.com / KonmaTemp123!) -- should redirect to /dashboard
    4. Verify dashboard shows user name and "Founder/Admin" role
    5. Verify sidebar shows: Dashboard, Missions (disabled), Team, Permissions under Admin heading
    6. Navigate to /admin/users -- verify team member table shows 8 seeded users
    7. Click "Add team member" -- verify create user dialog opens with name, email, role fields
    8. Navigate to /admin/permissions -- verify 15x8 permission matrix displays correctly
    9. Toggle a permission checkbox -- verify "Unsaved changes" banner appears
    10. Click "Save changes" -- verify toast "Permissions updated" appears
    11. Refresh the page -- verify you remain logged in (session persistence)
    12. Click user dropdown in sidebar -- verify "Log out" and "Log out everywhere" options appear
    13. Log out -- verify redirect to /login
    14. Try accessing /dashboard directly while logged out -- verify redirect to /login with ?redirect=/dashboard
    15. Log in again -- verify redirect back to /dashboard (redirect param works)
    16. Log in as a non-admin user -- verify sidebar does NOT show Team or Permissions nav items
  </action>
  <verify>Human confirms all 16 verification steps pass. Type "approved" to confirm the auth flow works, or describe any issues found.</verify>
  <done>All auth flow verification steps confirmed by human: login, session persistence, RBAC, admin features, logout, redirect behavior.</done>
</task>

</tasks>

<verification>
1. `cd apps/web && npx next build` -- frontend builds without errors
2. `cd apps/api && npx jest --passWithNoTests --forceExit` -- all backend tests pass
3. Login flow: unauthenticated user -> /login -> enter credentials -> /dashboard
4. Auth persistence: refresh page while logged in -> still on /dashboard
5. RBAC: non-admin user cannot see /admin/* nav items
6. Admin features: team management table, create user dialog, permission matrix
7. Edge middleware: direct URL access to /dashboard while logged out -> redirect to /login
</verification>

<success_criteria>
- Edge middleware uses jose (not jsonwebtoken) for JWT verification at edge runtime
- Login page matches UI-SPEC: "Welcome back" headline, email/password fields, show/hide password toggle with accessibility labels, "Sign in" CTA
- Forgot/set/reset password pages match UI-SPEC copy and interaction states
- Ops layout: 240px sidebar with role-scoped nav, main content area with 24px padding
- Admin user management: table with avatar, name, email, role badge, status badge, actions dropdown
- Admin permissions: 15x8 matrix with checkboxes, FOUNDER_ADMIN column locked, unsaved changes banner
- Dashboard shell: user name + role display, "Dashboard coming in Phase 7" placeholder
- Sidebar: FOUNDER_ADMIN sees Admin section (Team, Permissions), other roles do not
- Logout: "Log out" (this device) and "Log out everywhere" (all devices) in sidebar dropdown
- Session persistence: cookie-based JWT survives browser refresh
- Redirect: unauthenticated /dashboard access redirects to /login?redirect=/dashboard, successful login redirects back
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-authentication/01-03-SUMMARY.md`
</output>
