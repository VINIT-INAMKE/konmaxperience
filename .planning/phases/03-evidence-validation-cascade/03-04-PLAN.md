---
phase: 03-evidence-validation-cascade
plan: 04
type: execute
wave: 3
depends_on: [03-02, 03-03]
files_modified:
  - frontend/components/ops/evidence/EvidenceItem.tsx
  - frontend/components/ops/evidence/EvidenceSection.tsx
  - frontend/components/ops/evidence/ValidationStatus.tsx
  - frontend/components/ops/evidence/RejectionDialog.tsx
  - frontend/components/ops/approvals/ApprovalQueue.tsx
  - frontend/components/ops/approvals/ApprovalItem.tsx
  - frontend/app/(ops)/approvals/page.tsx
  - frontend/app/(ops)/tasks/[id]/page.tsx
  - frontend/components/ops/Sidebar.tsx
  - backend/src/evidence/evidence.controller.ts
  - backend/src/evidence/evidence.service.ts
autonomous: true
requirements: [EVID-02, EVID-03]

must_haves:
  truths:
    - "Reviewer can approve evidence inline on task detail page with cool-mode particle burst"
    - "Reviewer can reject evidence with required rejection reason via dialog"
    - "Self-approval actions are hidden when evidence.uploaded_by === currentUser.id"
    - "Validation checklist shows 3 conditions with green/gray icons for met/unmet"
    - "When task becomes valid: confetti burst + Sonner toast 'Task validated! +N XP' with animations"
    - "Dedicated approval queue page at /approvals shows pending evidence across all tasks"
    - "Sidebar has Approvals link with pending count badge in amber"
    - "Pending items > 24h get amber pulsating ring for urgency"
    - "Quest progress bar updates via React Query invalidation after validation"
  artifacts:
    - path: "frontend/components/ops/evidence/ValidationStatus.tsx"
      provides: "Validation checklist card with animated-circular-progress-bar"
      exports: ["ValidationStatus"]
    - path: "frontend/components/ops/evidence/RejectionDialog.tsx"
      provides: "Dialog for entering rejection reason"
      exports: ["RejectionDialog"]
    - path: "frontend/app/(ops)/approvals/page.tsx"
      provides: "Approval queue page at /approvals"
      contains: "ApprovalQueue"
    - path: "frontend/components/ops/approvals/ApprovalQueue.tsx"
      provides: "Full approval queue list with filters"
      exports: ["ApprovalQueue"]
    - path: "frontend/components/ops/Sidebar.tsx"
      provides: "Approvals nav link with pending count badge"
      contains: "Approvals"
  key_links:
    - from: "frontend/components/ops/evidence/EvidenceItem.tsx"
      to: "/evidence/:id/approve"
      via: "apiClient.post on approve button click"
      pattern: "evidence.*approve"
    - from: "frontend/components/ops/evidence/EvidenceItem.tsx"
      to: "/evidence/:id/reject"
      via: "apiClient.post on reject submit"
      pattern: "evidence.*reject"
    - from: "frontend/app/(ops)/tasks/[id]/page.tsx"
      to: "frontend/components/ops/evidence/ValidationStatus.tsx"
      via: "React component import"
      pattern: "ValidationStatus"
    - from: "frontend/app/(ops)/approvals/page.tsx"
      to: "frontend/components/ops/approvals/ApprovalQueue.tsx"
      via: "React component import"
      pattern: "ApprovalQueue"
    - from: "frontend/components/ops/Sidebar.tsx"
      to: "/approvals"
      via: "NavItem href"
      pattern: "/approvals"
---

<objective>
Frontend approval UI (inline approve/reject on task detail), validation status display, celebration animations, approval queue page, and sidebar update.

Purpose: Completes the evidence-to-validation user journey — reviewers can approve/reject evidence, task owners see their validation checklist progress, and validated tasks trigger celebrations. The approval queue gives reviewers a dedicated inbox for pending evidence.
Output: Working inline approval actions, validation checklist, confetti celebrations, approval queue page, sidebar with approvals badge
</objective>

<execution_context>
@D:/aditee/konmaxperience/.claude/get-shit-done/workflows/execute-plan.md
@D:/aditee/konmaxperience/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-evidence-validation-cascade/03-CONTEXT.md
@.planning/phases/03-evidence-validation-cascade/03-RESEARCH.md
@.planning/phases/03-evidence-validation-cascade/03-UI-SPEC.md
@.planning/phases/03-evidence-validation-cascade/03-02-SUMMARY.md
@.planning/phases/03-evidence-validation-cascade/03-03-SUMMARY.md

<interfaces>
<!-- From Plan 02 — backend approval endpoint contracts -->

POST /evidence/:id/approve
- Permission: APPROVE_EVIDENCE
- Returns: { valid: boolean, valid_xp: number }
- 403 if self-approval attempted

POST /evidence/:id/reject
- Permission: APPROVE_EVIDENCE
- Body: { notes: string } (rejection reason, required)
- Returns: void

GET /tasks/:taskId/evidence (from Plan 01)
- Returns: Evidence[] with uploader and reviewer includes

<!-- From Plan 03 — frontend components already created -->

From frontend/lib/types/evidence.ts:
```typescript
export type EvidenceType = 'photo' | 'doc' | 'video' | 'link' | 'note';
export type EvidenceApprovalStatus = 'pending' | 'approved' | 'rejected';
export interface Evidence {
  id: string; task_id: string; uploaded_by: string;
  uploader?: { id: string; name: string };
  type: EvidenceType; url: string; notes: string | null;
  approval_status: EvidenceApprovalStatus;
  reviewed_by: string | null;
  reviewer?: { id: string; name: string } | null;
  reviewed_at: string | null; created_at: string;
}
```

From frontend/components/ops/evidence/EvidenceItem.tsx (created in Plan 03):
- Already renders type icon + filename + status badge
- Has onApprovalAction prop placeholder for this plan to add approve/reject buttons

From frontend/app/(ops)/tasks/[id]/page.tsx (modified in Plan 03):
- Has evidence query, EvidenceUploadZone, EvidenceList
- This plan adds: ValidationStatus, inline approval actions, confetti/toast on validation

Existing components available:
- animated-circular-progress-bar (Phase 2)
- number-ticker (Phase 2)
- confetti (Phase 2)
- interactive-hover-button (installed in Plan 03)
- cool-mode (installed in Plan 03)
- shimmer-button (installed in Plan 03)
- pulsating-button (installed in Plan 03)
- shine-border (installed in Plan 03)
- text-animate (installed in Plan 03)
- hyper-text (installed in Plan 03)
- blur-fade (Phase 2)
- animated-list (Phase 2)

From frontend/lib/types/permissions.ts:
```typescript
// Check user role permissions — the auth store has roleCode
// APPROVE_EVIDENCE holders can approve/reject
```

From frontend/components/ops/Sidebar.tsx:
```typescript
// mainNav and adminNav arrays — add Approvals to mainNav
// NavItem interface: { label, href, icon, disabled?, badge? }
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Validation status card, inline approval actions on evidence items, rejection dialog, and celebration animations</name>
  <files>
    frontend/components/ops/evidence/ValidationStatus.tsx,
    frontend/components/ops/evidence/RejectionDialog.tsx,
    frontend/components/ops/evidence/EvidenceItem.tsx,
    frontend/components/ops/evidence/EvidenceSection.tsx,
    frontend/app/(ops)/tasks/[id]/page.tsx
  </files>
  <read_first>
    frontend/app/(ops)/tasks/[id]/page.tsx,
    frontend/components/ops/evidence/EvidenceItem.tsx,
    frontend/components/ops/evidence/EvidenceList.tsx,
    frontend/components/ops/evidence/EvidenceUploadZone.tsx,
    frontend/lib/types/evidence.ts,
    frontend/lib/stores/auth-store.ts,
    frontend/components/ui/animated-circular-progress-bar.tsx,
    frontend/components/ui/interactive-hover-button.tsx,
    frontend/components/ui/cool-mode.tsx,
    frontend/components/ui/shine-border.tsx,
    frontend/components/ui/text-animate.tsx,
    frontend/components/ui/number-ticker.tsx,
    frontend/components/ui/confetti.tsx,
    .planning/phases/03-evidence-validation-cascade/03-UI-SPEC.md (Validation Checklist section, Evidence Item Layout section, Toast Notification Contract)
  </read_first>
  <action>
    1. Create `frontend/components/ops/evidence/ValidationStatus.tsx`:
       - Props: `task: Task`, `evidence: Evidence[]`
       - Compute 3 conditions:
         a. statusDone = task.status === 'done'
         b. hasApprovedEvidence = evidence.some(e => e.approval_status === 'approved')
         c. approvalsSatisfied = task.requires_approval ? true : true (simplified — approvals are checked server-side; frontend shows basic check)
            NOTE: For v1, show the third condition as met if hasApprovedEvidence is true (since the Approval model is entity-level, the frontend cannot easily check all required approvals; the server-side validateTask is authoritative)
       - metCount = number of met conditions out of 3
       - If task.valid === true: show green "Valid" badge + "Task validated" text (Label, green-400) + animated-circular-progress-bar at 100%
       - If task.valid === false: show "Validation required" heading + checklist:
         - Each row: CheckCircle (green-400) when met, Circle (muted-foreground) when unmet
         - Text: "Status is Done", "At least one evidence approved", "All required approvals satisfied"
         - Color: text-green-400 when met, text-muted-foreground when unmet
         - animated-circular-progress-bar showing metCount/3 * 100
       - Checklist container: role="list", each item: role="listitem"
       - Icons: aria-hidden="true" (text carries meaning)
       - animated-circular-progress-bar: aria-label="Validation conditions met: N of 3"
       - Card with --card bg, border, rounded-lg, padding md (16px)

    2. Create `frontend/components/ops/evidence/RejectionDialog.tsx`:
       - Props: `open: boolean`, `onOpenChange: (open: boolean) => void`, `onReject: (notes: string) => void`, `isSubmitting: boolean`
       - Dialog (modal) — uses Dialog from shadcn
       - Title: "Reject evidence"
       - Body: "Tell the task owner what needs to be corrected."
       - Required textarea, placeholder: "e.g. The photo doesn't clearly show the completed setup. Please retake with better lighting."
       - Footer: "Keep pending" ghost button (closes dialog) + "Reject evidence" destructive button (calls onReject with notes text)
       - While submitting: button shows spinner + "Rejecting...", textarea disabled
       - Textarea has associated Label via htmlFor

    3. Update `frontend/components/ops/evidence/EvidenceItem.tsx`:
       - Add props: `canApprove: boolean` (determined by parent — true when user has APPROVE_EVIDENCE permission AND evidence.approval_status === 'pending' AND evidence.uploaded_by !== currentUser.id)
       - When canApprove is true, show action buttons:
         a. "Approve" — InteractiveHoverButton (green hover state), wrapped in CoolMode for particle burst on click
         b. "Reject" — InteractiveHoverButton (red hover state), opens RejectionDialog
       - Approve mutation: `apiClient.post(\`/evidence/${evidence.id}/approve\`)` — returns { valid, valid_xp }
         - On success: toast.success("Evidence approved."), call onApprovalAction
         - If response.valid && valid was previously false: this is the validation transition
       - Reject mutation: `apiClient.post(\`/evidence/${evidence.id}/reject\`, { notes })` — on success: toast.error("Evidence rejected."), call onApprovalAction
       - ShineBorder wrapper: show for 3 seconds after evidence transitions to approved (use state + setTimeout)
       - Loading states: Approve button -> spinner + "Approving...", disabled

    4. Create `frontend/components/ops/evidence/EvidenceSection.tsx`:
       - Consolidation component — wraps ValidationStatus + EvidenceUploadZone + link/note forms + EvidenceList
       - Props: `task: Task`, `isOwn: boolean`, `isAdmin: boolean`
       - Internal state: showLinkForm, showNoteForm
       - Evidence query: `useQuery({ queryKey: ['evidence', task.id], queryFn: () => apiClient.get<Evidence[]>(\`/tasks/${task.id}/evidence\`) })`
       - Determine canApprove per evidence item: user has APPROVE_EVIDENCE perm (check roleCode against known roles — for simplicity, leads and admin have this perm; OR just pass it as a prop from parent)
         NOTE: Since permission checking on frontend is role-based and APPROVE_EVIDENCE is a backend permission, use a simple heuristic: isAdmin || (user.roleCode !== task.owner's roleCode) — OR better, add a `permissions` array to the auth store user. For now, assume only admin and leads can approve. Check if user.roleCode is not the task owner's role to show approve buttons. The backend enforces the real permission.
       - On approval action: invalidate evidence query + tasks query + quests query
       - Track previous task.valid state. When task.valid transitions false->true after evidence query invalidation:
         - Fire confetti: `confetti({ particleCount: 80, spread: 80, origin: { y: 0.6 } })`
         - Fire toast: `toast.success("Task validated! +{task.valid_xp} XP")` — use custom JSX description if possible to include text-animate and number-ticker, otherwise use plain text (Sonner supports JSX descriptions)
       - Section header: "Evidence" heading (text-xl font-semibold) + "N items" count badge (text-[13px] muted)

    5. Update `frontend/app/(ops)/tasks/[id]/page.tsx`:
       - Replace the inline evidence section code (added in Plan 03) with the new EvidenceSection component
       - Import EvidenceSection
       - Pass task, isOwn, isAdmin props
       - Also add to the task header: if task.valid === true, show a green "Valid" badge (text-green-400 bg-green-950) alongside the existing status/type/priority badges
       - Remove any evidence query and evidence-related state that was inlined in Plan 03 — now handled by EvidenceSection
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "ValidationStatus" frontend/components/ops/evidence/EvidenceSection.tsx
    - grep -q "EvidenceSection" frontend/app/(ops)/tasks/[id]/page.tsx
    - grep -q "confetti" frontend/components/ops/evidence/EvidenceSection.tsx
    - grep -q "toast.success.*Task validated\|Task validated" frontend/components/ops/evidence/EvidenceSection.tsx
    - grep -q "InteractiveHoverButton\|interactive-hover-button" frontend/components/ops/evidence/EvidenceItem.tsx
    - grep -q "CoolMode\|cool-mode" frontend/components/ops/evidence/EvidenceItem.tsx
    - grep -q "ShineBorder\|shine-border" frontend/components/ops/evidence/EvidenceItem.tsx
    - grep -q "animated-circular-progress-bar\|AnimatedCircularProgressBar" frontend/components/ops/evidence/ValidationStatus.tsx
    - grep -q "CheckCircle\|Circle" frontend/components/ops/evidence/ValidationStatus.tsx
    - grep -q "RejectionDialog\|Reject evidence" frontend/components/ops/evidence/RejectionDialog.tsx
    - grep -q "evidence.*approve" frontend/components/ops/evidence/EvidenceItem.tsx
    - grep -q "evidence.*reject" frontend/components/ops/evidence/EvidenceItem.tsx
    - grep -q "Valid" frontend/app/(ops)/tasks/[id]/page.tsx
  </acceptance_criteria>
  <done>Validation checklist shows 3 conditions with progress ring. Inline approve/reject buttons on evidence items (hidden for self-uploads). Rejection requires reason via dialog. Confetti + toast fire on task validation. Evidence approved items get temporary shine-border. Task header shows "Valid" badge when validated.</done>
</task>

<task type="auto">
  <name>Task 2: Approval queue page, sidebar Approvals link with pending count badge</name>
  <files>
    frontend/components/ops/approvals/ApprovalItem.tsx,
    frontend/components/ops/approvals/ApprovalQueue.tsx,
    frontend/app/(ops)/approvals/page.tsx,
    frontend/components/ops/Sidebar.tsx
  </files>
  <read_first>
    frontend/components/ops/Sidebar.tsx,
    frontend/lib/types/evidence.ts,
    frontend/lib/api-client.ts,
    frontend/components/ui/pulsating-button.tsx,
    frontend/components/ui/shimmer-button.tsx,
    frontend/components/ui/blur-fade.tsx,
    frontend/components/ui/animated-list.tsx,
    frontend/components/ops/evidence/RejectionDialog.tsx (just created),
    .planning/phases/03-evidence-validation-cascade/03-UI-SPEC.md (Approval Queue Page section)
  </read_first>
  <action>
    1. Create `frontend/components/ops/approvals/ApprovalItem.tsx`:
       - Props: `evidence: Evidence & { task?: { id: string; title: string; quest?: { id: string; title: string }; mission?: { id: string; title: string } } }`, `onAction: () => void`
       - Card (--card bg, border, rounded-lg, padding md 16px) layout:
         - Row 1: Task title (Body 14px, weight 600) — Quest name (Label, muted) — Mission name (Label, muted)
         - Row 2: Type icon 16px + Filename/URL (Label 14px) — Uploader avatar + name (Label, muted) — "Submitted: {relative time}" (Label, muted)
         - Row 3: Actions — "Approve" ShimmerButton (small, green shimmer, wrapped in CoolMode) + "Reject" InteractiveHoverButton (red)
       - Approve mutation: `apiClient.post(\`/evidence/${evidence.id}/approve\`)` — toast.success("Evidence approved.") + onAction()
       - Reject: opens RejectionDialog, on submit: `apiClient.post(\`/evidence/${evidence.id}/reject\`, { notes })` — toast.error("Evidence rejected.") + onAction()
       - Pending > 24h detection: `Date.now() - new Date(evidence.created_at).getTime() > 24 * 60 * 60 * 1000`
         - If true: wrap action area with PulsatingButton amber ring (use pulseColor amber variant)
         - Tooltip on hover: "Pending for {N} days"
       - AvatarCircles showing uploader avatar (DiceBear) + reviewer avatar if reviewed
       - Loading states: Approve -> spinner + "Approving...", button disabled

    2. Create `frontend/components/ops/approvals/ApprovalQueue.tsx`:
       - Fetches pending evidence: `useQuery({ queryKey: ['approvals', 'pending'], queryFn: () => apiClient.get<(Evidence & {...})[]>('/tasks/evidence/pending') })`
         NOTE: This requires a backend endpoint that returns all pending evidence across tasks. Since Plan 01/02 did not add this specific endpoint, the frontend should call a general endpoint. Two options:
         a. Add a new backend endpoint (but that's not in this plan's files_modified)
         b. Use existing GET /tasks/:id/evidence with a filter — but this is per-task
         DECISION: This plan adds a simple GET /evidence?status=pending endpoint by modifying the evidence controller/service. Add it to files_modified as a deviation noted in the summary.
         Actually, to keep this frontend-only, we can work with what we have. Fetch all tasks first, then fetch evidence per task. BUT that's N+1 and terrible.
         BETTER: Add `GET /evidence?status=pending` to the EvidenceController in Task 1 action (add to evidence.controller.ts). This is a minor backend addition that belongs here since the approval queue page needs it.
       - Actually, implement this: Query `apiClient.get<Evidence[]>('/evidence?status=pending')` — this endpoint needs to be added. Add `backend/src/evidence/evidence.controller.ts` and `backend/src/evidence/evidence.service.ts` to the read_first and note the minor backend addition.
       - Filter bar: task name search input (Search icon) + quest select dropdown — both inline, right-aligned, gap-md
       - Client-side filtering of the fetched results
       - Sorted: oldest pending first (most urgent at top)
       - AnimatedList wrapping all ApprovalItems
       - BlurFade wrapping entire list container
       - Loading: 3 skeleton ApprovalItem cards
       - Empty: centered CheckCircle (48px, green-400) + "No pending approvals" (Heading 20px) + "You're all caught up." (Body, muted)
       - Error: Alert "Could not load approvals. Try refreshing."
       - No results after filter: "No approvals match your filter." (Body, muted, centered)
       - On any approval/reject action: invalidate ['approvals', 'pending'] query

    3. Create `frontend/app/(ops)/approvals/page.tsx`:
       - Page header: "Approvals" heading (20px) + subtitle "Evidence waiting for your review." (Body, muted) + count badge "N pending" (amber)
       - Renders ApprovalQueue component
       - BlurFade wraps page content

    4. Update `frontend/components/ops/Sidebar.tsx`:
       - Add "Approvals" to mainNav array (below "Missions"):
         ```
         { label: 'Approvals', href: '/approvals', icon: <CheckCircle className="size-4" /> }
         ```
         Import CheckCircle from lucide-react
       - Add pending approvals count badge: fetch count via `useQuery({ queryKey: ['approvals', 'count'], queryFn: () => apiClient.get<{ count: number }>('/evidence/pending-count') })`
         If endpoint doesn't exist, use the pending evidence list length from ['approvals', 'pending'] query cache, or just show no badge initially.
         SIMPLER: Just add the nav item without dynamic badge for now. The backend can provide count later. Show a static badge style that reads from cache.
         ACTUALLY: Add badge prop with pending count. Fetch evidence pending count in sidebar on mount. If the GET /evidence?status=pending endpoint exists (added above), use it and show `.length` as badge.
       - Badge color: amber (text-amber-400 bg-amber-950) — consistent with UI-SPEC

    5. BACKEND ADDITION (minor, needed for approval queue):
       Add to `backend/src/evidence/evidence.controller.ts`:
       - `GET /evidence` with optional query param `status` — returns evidence with task, uploader, reviewer includes
       - Protected by @RequiresPermission(Permission.APPROVE_EVIDENCE)
       - Apply buildScopeFilter: scoped users see only evidence for tasks in their domain, admin sees all
       - Include task with quest and mission title for display in approval queue

       Add to `backend/src/evidence/evidence.service.ts`:
       - `findAll(filters: { status?: string }, scopeFilter: Record<string, unknown>)`: returns evidence with task/quest/mission includes
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit && cd ../backend && npx tsc --noEmit && echo "Both compile OK"</automated>
  </verify>
  <acceptance_criteria>
    - test -f frontend/app/(ops)/approvals/page.tsx
    - grep -q "ApprovalQueue" frontend/app/(ops)/approvals/page.tsx
    - grep -q "Approvals" frontend/components/ops/Sidebar.tsx
    - grep -q "/approvals" frontend/components/ops/Sidebar.tsx
    - grep -q "ShimmerButton\|shimmer-button" frontend/components/ops/approvals/ApprovalItem.tsx
    - grep -q "CoolMode\|cool-mode" frontend/components/ops/approvals/ApprovalItem.tsx
    - grep -q "PulsatingButton\|pulsating-button" frontend/components/ops/approvals/ApprovalItem.tsx
    - grep -q "RejectionDialog" frontend/components/ops/approvals/ApprovalItem.tsx
    - grep -q "No pending approvals\|all caught up" frontend/components/ops/approvals/ApprovalQueue.tsx
    - grep -q "BlurFade\|blur-fade" frontend/components/ops/approvals/ApprovalQueue.tsx
    - grep -q "AnimatedList\|animated-list" frontend/components/ops/approvals/ApprovalQueue.tsx
    - grep -q "evidence.*status.*pending\|GET.*evidence" backend/src/evidence/evidence.controller.ts
  </acceptance_criteria>
  <done>Approval queue page at /approvals shows all pending evidence with approve/reject actions. Sidebar has "Approvals" link. Pending items >24h show amber pulsating ring. Empty state shows "all caught up" message. GET /evidence?status=pending backend endpoint added for queue data.</done>
</task>

</tasks>

<verification>
- `cd frontend && npx tsc --noEmit` — zero errors
- `cd backend && npx tsc --noEmit` — zero errors
- `cd backend && npx jest --forceExit` — all tests pass
- /approvals page renders
- Sidebar shows "Approvals" nav item
- Inline approve/reject buttons on evidence items (hidden for own evidence)
- Rejection dialog requires reason text
- Confetti + toast fire when task transitions to valid
- Validation checklist shows 3 conditions with progress ring
</verification>

<success_criteria>
- Reviewer can approve evidence from task detail page — particle burst + toast
- Reviewer can reject evidence with reason — dialog enforces non-empty notes
- Self-approval actions hidden (uploaded_by === currentUser check)
- Validation checklist shows met/unmet conditions with green/gray styling
- Task validation triggers confetti + "Task validated! +N XP" toast
- /approvals page lists all pending evidence with task context
- Pending >24h items have amber pulsating urgency indicator
- Sidebar has "Approvals" link
- All TypeScript compiles, all backend tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/03-evidence-validation-cascade/03-04-SUMMARY.md`
</output>
