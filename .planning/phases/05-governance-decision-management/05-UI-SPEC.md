---
phase: 5
slug: governance-decision-management
status: draft
shadcn_initialized: true
preset: base-nova / neutral / cssVariables
created: 2026-03-21
---

# Phase 5 — UI Design Contract

> Visual and interaction contract for frontend phases. Generated by gsd-ui-researcher, verified by gsd-ui-checker.

---

## Design System

| Property | Value |
|----------|-------|
| Tool | shadcn (base-nova style) |
| Preset | base-nova, neutral base color, CSS variables enabled |
| Component library | base-ui (via shadcn) |
| Icon library | lucide-react |
| Font | Geist Sans (--font-geist-sans) / Geist Mono (--font-geist-mono) |
| MagicUI registry | @magicui → https://magicui.design/r/{name} |

**Source:** components.json + globals.css — carried forward from Phase 4 approved contract. Not re-asked.

---

## Spacing Scale

Declared values (multiples of 4):

| Token | Value | Usage |
|-------|-------|-------|
| xs | 4px | Icon gaps, badge inner padding, inline chip spacing |
| sm | 8px | Compact element spacing, filter pill padding, table cell gaps |
| md | 16px | Default element/section spacing, card padding, form field gaps |
| lg | 24px | Section padding, grid gaps between decision cards |
| xl | 32px | Layout gaps, page section breaks |
| 2xl | 48px | Major section breaks, dashboard column separation |
| 3xl | 64px | Page-level top padding |

Exceptions:
- Override reason textarea: min-height 80px (not a spacing token — component height constraint)
- Delegation date inputs: min-width 140px for date picker usability

**Source:** Carried forward from Phase 4 approved contract.

---

## Typography

| Role | Size | Weight | Line Height |
|------|------|--------|-------------|
| Body | 16px | 400 (regular) | 1.5 |
| Label | 14px | 400 (regular) | 1.4 |
| Heading | 20px | 600 (semibold) | 1.2 |
| Display | 28px | 600 (semibold) | 1.1 |

**Source:** Carried forward from Phase 4 approved contract. Not re-asked.

Governance-specific usage:
- Decision type badge label: 13px regular (tighter than standard label — fits pill without wrapping)
- Decision context text (full detail view): 16px regular, 1.5 line-height (body role — readable prose)
- Override reason text on approval card (audit trail): 13px regular, muted-foreground, italic
- "on behalf of" delegation attribution: 13px regular, muted-foreground — shown as subtitle below approver name
- Delegation date range chip: 13px semibold (both dates, `font-mono` for the date values for alignment)
- Status badge text: 13px semibold (compact — consistent with KPI status badge established in Phase 4)
- Decision list table: Body 16px for decision title, Label 14px for all metadata columns

---

## Color

| Role | Value | Usage |
|------|-------|-------|
| Dominant (60%) | `--background` oklch(0.145 0 0) dark / oklch(1 0 0) light | Page background, decisions page surface |
| Secondary (30%) | `--card` oklch(0.205 0 0) dark / oklch(1 0 0) light | Decision cards, delegation list cards, approval detail sections |
| Accent (10%) | `--primary` oklch(0.922 0 0) dark / oklch(0.205 0 0) light | Reserved elements below |
| Destructive | `--destructive` oklch(0.704 0.191 22.216) dark | Reopen locked decision (admin action) only |

Accent (`--primary`) reserved for:
- "Log Decision" primary action ShimmerButton
- "Override Approval" submit button inside the override Dialog
- Active/selected filter tab underline on the decisions list

**Source:** globals.css CSS variables — carried forward from Phase 4 approved contract.

### Semantic Color Overrides (governance-specific, not in CSS vars)

These are hardcoded Tailwind classes applied within specific governance components only:

| Semantic Role | Value | Applied To |
|--------------|-------|------------|
| Decision proposed | `text-amber-500`, Badge `border-amber-500/40 text-amber-400` | Decision status badge — proposed state |
| Decision approved | `text-green-500`, Badge `border-green-500/40 text-green-400` | Decision status badge — approved state |
| Decision rejected | `text-red-500`, Badge `border-red-500/40 text-red-400` | Decision status badge — rejected state |
| Decision locked (approved) | `text-muted-foreground` + lock icon (lucide `Lock`, 14px) | Title row of approved/locked decisions — edit/delete controls hidden |
| Override badge | `bg-amber-500/10 text-amber-400 border-amber-500/30` | Override attribution chip on approval card ("Overridden by [name]") |
| Delegation active | `bg-blue-500/10 text-blue-400 border-blue-500/30` | Active delegation status chip in delegation management list |
| Delegation expired | `bg-muted text-muted-foreground` | Expired delegation row — visually de-emphasized |
| Decision type — individual | `bg-slate-500/10 text-slate-400` | Decision type badge |
| Decision type — cross-function | `bg-blue-500/10 text-blue-400` | Decision type badge |
| Decision type — strategic | `bg-purple-500/10 text-purple-400` | Decision type badge |
| MagicCard spotlight | gradientColor `#1a1a2e` | Decision cards — consistent with Phase 2-03 decision |

---

## MagicUI Component Inventory

All 15 components are pre-installed at `frontend/components/ui/`. No install commands needed for Phase 5.

| Component | File | Phase 5 Usage |
|-----------|------|---------------|
| `magic-card` | `magic-card.tsx` | Decision cards in list view (each decision row card) |
| `blur-fade` | `blur-fade.tsx` | Page entry animation for /decisions page, decision detail slide-in |
| `shimmer-button` | `shimmer-button.tsx` | "Log Decision" primary action button |
| `animated-list` | `animated-list.tsx` | Decision list items on /decisions page (staggered entry) |
| `interactive-hover-button` | `interactive-hover-button.tsx` | "View Detail" expand trigger on decision card hover |
| `text-animate` | `text-animate.tsx` | "Decision Locked" confirmation text in lock dialog |
| `shine-border` | `shine-border.tsx` | Newly logged decision card (3s glow after creation, then removed) |
| `cool-mode` | `cool-mode.tsx` | Wraps "Override Approval" submit button — particle effect on override |
| `border-beam` | `border-beam.tsx` | Override reason field while admin is typing (active input state) |
| `pulsating-button` | `pulsating-button.tsx` | Approval items older than 24h that are overrideable — override button pulses |
| `number-ticker` | `number-ticker.tsx` | Decision count badge in sidebar nav (animates when new decision logged) |
| `avatar-circles` | `avatar-circles.tsx` | Delegation card — shows from_user and to_user avatars side-by-side |
| `badge` (shadcn, not MagicUI) | `badge.tsx` | Decision type, status, override attribution, delegation status |

Not used in Phase 5 (already installed, not applicable to governance domain):
- `confetti`, `hyper-text`, `animated-circular-progress-bar` — gamification-specific

**Source:** 15 MagicUI components pre-installed (confirmed in Phase 3-03 and Phase 4 records). Only semantically appropriate components assigned. No installs required.

---

## Component Inventory

### New Components (Phase 5 creates these)

| Component | Path | MagicUI Used |
|-----------|------|--------------|
| `DecisionCard` | `components/ops/decisions/DecisionCard.tsx` | `magic-card`, `shine-border` (3s post-create), `interactive-hover-button` |
| `DecisionList` | `components/ops/decisions/DecisionList.tsx` | `animated-list`, `blur-fade` |
| `DecisionForm` | `components/ops/decisions/DecisionForm.tsx` | `shimmer-button` (submit) |
| `DecisionDetail` | `components/ops/decisions/DecisionDetail.tsx` | `blur-fade` (expand animation) |
| `DecisionStatusBadge` | `components/ops/decisions/DecisionStatusBadge.tsx` | shadcn Badge |
| `DecisionTypeBadge` | `components/ops/decisions/DecisionTypeBadge.tsx` | shadcn Badge |
| `OverrideDialog` | `components/ops/approvals/OverrideDialog.tsx` | `cool-mode` (submit wrap), `border-beam` (textarea focus) |
| `DelegationCard` | `components/ops/delegations/DelegationCard.tsx` | `avatar-circles` |
| `DelegationForm` | `components/ops/delegations/DelegationForm.tsx` | `shimmer-button` (submit) |
| `DelegationList` | `components/ops/delegations/DelegationList.tsx` | shadcn Card list |

### Existing Components (Phase 5 modifies these)

| Component | Modification |
|-----------|-------------|
| `ApprovalItem.tsx` | Add "Override" button (admin only, `pulsating-button` when pending 24h+); add override attribution display when override_reason present |
| `ApprovalQueue.tsx` | No structural change — receives modified ApprovalItem |
| `Sidebar.tsx` | Add "Decisions" link under Work section with `ClipboardCheck` icon + `number-ticker` count badge |

### Existing shadcn Components (reused, no changes)

| Component | Phase 5 Usage |
|-----------|--------------|
| `Card`, `CardContent` | Delegation list, decision detail container |
| `Badge` | Decision type, status, delegation status, override attribution |
| `Button` | Filter clear, cancel actions, status change (approve/reject decision) |
| `Dialog` | Override reason dialog, reopen locked decision confirmation |
| `Sheet` | Decision create form (slide-in from right), delegation create form |
| `Tabs` | /decisions page status filter tabs (All, Proposed, Approved, Rejected) |
| `Textarea` | Override reason input, decision context field |
| `Input` | Decision search filter, delegation from/to user fields |
| `Select` | Decision type select, decision status select |
| `Combobox` | Mission link picker, task link picker in decision form |
| `Separator` | Section dividers in decision detail view |
| `Tooltip` | "Locked — approved decisions cannot be edited" on disabled edit action |
| `Sonner` | Toast notifications (decision logged, override applied, delegation created) |
| `ScrollArea` | Decisions list if >10 items |
| `Avatar`, `AvatarFallback` | Delegation card user representations |
| `Popover` | Date range picker for delegation start/end dates |

---

## Page Layout Contracts

### `/decisions` — Decision Log

Layout: Single column, `space-y-6`. No tabs on page level — filter tabs are within the list section.

1. **Page header**: "Decisions" h1 (28px semibold) + ShimmerButton "Log Decision" (right-aligned, visible to users with CREATE_DECISION permission).
2. **Filter bar**: shadcn Tabs with 4 tabs: "All", "Proposed", "Approved", "Rejected". Active tab has `--primary` underline. Right side: search Input `placeholder="Search decisions..."` (w-64). Tabs and search on same row, space-between layout.
3. **Decision list** (`DecisionList`): `AnimatedList` with `delay={50}`. Each entry = `DecisionCard`. List fills remaining page height with `ScrollArea` if >10 cards.
4. **Empty state**: Centered within list area. Icon: lucide `ClipboardList` (48px, muted). Heading "No decisions yet" (20px semibold). Body "Log the first decision to start building your governance trail." (14px muted). ShimmerButton "Log Decision" below body if user has CREATE_DECISION permission.

**Decision card layout** (`DecisionCard`, wraps `MagicCard` with `gradientColor="#1a1a2e"`):
- Row 1: Decision title (16px semibold) + lock icon (14px muted, lucide `Lock`) if approved/locked + `DecisionTypeBadge` + `DecisionStatusBadge` — all on same row, flex-wrap
- Row 2: Context preview (truncated to 2 lines, 14px muted) — full text in detail expand
- Row 3: Linked mission/task chips (Badge variant outline, 13px) if present + author chip (AvatarFallback 20px + name, 13px) + relative timestamp (13px muted) — pushed right

On card hover: `InteractiveHoverButton` "View Detail" overlays bottom-right corner.

### Decision Detail Expand

Decision detail is not a separate page. It is an inline expansion that appears directly below the card in the list (same pattern as MeterDetailPanel in Phase 4). Expand/collapse with `BlurFade`. Single panel open at a time.

Detail panel layout (inside shadcn Card with `p-6`):
- **Section "Context"**: Label "Context" (14px semibold, uppercase tracking) + full context text (16px regular, 1.5 line-height)
- **Section "Links"**: Only rendered if linked_mission_id or linked_task_id is set. Shows "Linked Mission: [name]" and/or "Linked Task: [name]" as Badge pills with lucide `ExternalLink` icon (12px), linking to the respective page.
- **Section "History"**: Status change timeline (proposed → approved/rejected). Each event: timestamp (13px mono, muted) + "Status changed to [status]" text. Minimal, no icons.
- **Admin actions** (visible to FOUNDER_ADMIN only): If decision is approved/locked → shadcn Button destructive variant "Reopen Decision". If decision is proposed → Button "Approve" (primary) + Button "Reject" (outline, border-destructive).

### `/approvals` — Approval Queue (modified, not new page)

Existing page receives one addition to each `ApprovalItem`:
- **Override button** (FOUNDER_ADMIN only): `PulsatingButton` if `pending > 24h` (amber pulse `#f59e0b`), standard `InteractiveHoverButton` if pending <= 24h. Label: "Override". Placed at the end of the action buttons row, separated by a `Separator` (vertical) at 16px height.
- **Override attribution display**: When `override_reason` is set on the approval record, render a new row below the actions row: small `AlertCircle` icon (12px, amber) + italic text "Overridden by [override_by name] — [override_reason]" (13px, muted-foreground). Timestamp shown as relative time (13px, muted).

### `/admin/delegations` — Delegation Management (new page under Admin section)

Layout: Single column, `space-y-6`. Accessible via Admin section in sidebar (admin-only route).

1. **Page header**: "Approval Delegations" h1 (28px semibold) + ShimmerButton "Create Delegation" (right-aligned, admin only).
2. **Active delegations**: Subsection label "Active" (14px semibold, uppercase tracking) + list of `DelegationCard` for delegations where `active=true` and `end_date >= today`.
3. **Expired delegations**: Subsection label "Expired" (14px semibold, uppercase tracking, muted) + list of `DelegationCard` (de-emphasized, muted styling). Collapsed behind a "Show expired" toggle (14px muted link) — hidden by default.
4. **Empty state**: Centered. Icon: lucide `UserCheck` (48px, muted). Heading "No active delegations" (20px semibold). Body "Delegations let an admin temporarily grant approval authority to another user." (14px muted). ShimmerButton "Create Delegation" below.

**Delegation card layout** (`DelegationCard`, shadcn Card):
- Row 1: `AvatarCircles` showing both users (from_user + to_user, 28px each, overlapping) + "[from_user name] → [to_user name]" (16px semibold) + active/expired Badge chip.
- Row 2: Date range — lucide `Calendar` icon (14px, muted) + "[start_date] to [end_date]" in `font-mono` 13px semibold + "Created by [admin name]" (13px muted) — pushed right.
- Admin actions (inline, right side): "Deactivate" Button (destructive outline, 13px) for active delegations. No action for expired.

---

## Interaction Contracts

### Log Decision Flow

- **Trigger**: ShimmerButton "Log Decision" on /decisions page header.
- **Panel**: shadcn Sheet (side right, `w-[480px]`). Slides in from right.
- **Form fields**:
  - Title: Input, `placeholder="e.g. Switched to presigned URL pattern for evidence storage"`, required.
  - Type: shadcn Select — options: Individual, Cross-function, Strategic. Required.
  - Context: Textarea, `placeholder="What was decided, why, and by whom?"`, required, min-height 80px.
  - Status: shadcn Select — default "Proposed". Options: Proposed, Approved, Rejected. Required.
  - Linked Mission (optional): Combobox, searches existing missions.
  - Linked Task (optional): Combobox, searches existing tasks.
- **Submit**: ShimmerButton "Log Decision" (`shimmerColor="#4ade80"`). Disabled while submitting (Loader2 icon + "Saving...").
- **On success**: Sheet closes. New decision appears at top of list with `ShineBorder` wrapper active for 3s. Sonner toast: "Decision logged."
- **On error**: Sonner toast: "Failed to log decision. Check your inputs and try again."
- **Cancel**: Ghost Button "Cancel" — no confirmation needed (no destructive data loss for new forms).

### Approve / Reject Decision (admin)

- Approve decision: Button "Approve" (primary, 14px) in detail expand. Click → immediate API call → status changes to "approved", decision locks. Sonner toast: "Decision approved and locked."
- Reject decision: Button "Reject" (outline, border-destructive, 14px) → no confirmation dialog (not destructive in an irreversible sense) → status changes to "rejected". Sonner toast: "Decision rejected."
- Reopen locked decision: Button "Reopen Decision" (destructive variant) in detail expand → Dialog confirmation: "Reopen this decision? Approved decisions are locked for governance integrity. Reopening sets status back to Proposed." → Confirm: Button "Reopen Decision" (destructive). Cancel: Ghost Button "Keep Locked". On confirm: toast "Decision reopened."

### Override Approval

- **Trigger**: "Override" button on an `ApprovalItem` (admin-only, visible on all pending items).
- **Dialog**: shadcn Dialog (not Sheet — override is a decisive action, modal focus is appropriate). Title "Override Approval" (20px semibold). Body: "Bypassing the approval workflow. This action is recorded in the audit trail." (14px muted).
- **Reason field**: Textarea, `placeholder="State the reason for overriding this approval..."`, required, min-height 80px. `BorderBeam` activates on focus — signals the seriousness of this field.
- **Submit**: CoolMode wraps the submit button. ShimmerButton "Override and Approve" (`shimmerColor="#4ade80"`). Disabled until reason field is non-empty (min 10 chars). Disabled during submission (Loader2 + "Overriding...").
- **On success**: Dialog closes. Full validation cascade fires (same as normal approval — XP, quest, mission, readiness). `ApprovalItem` re-renders with override attribution row. Sonner toast: "Approval overridden. Validation cascade triggered."
- **On error**: Sonner toast: "Override failed. Try again or check permissions."
- **Cancel**: Ghost Button "Cancel" — Dialog closes, no action taken.

### Create Delegation

- **Trigger**: ShimmerButton "Create Delegation" on /admin/delegations page.
- **Panel**: shadcn Sheet (side right, `w-[480px]`).
- **Form fields**:
  - From User: Select/Combobox of all users. Label "Delegating From (Absent User)". Required.
  - To User: Select/Combobox of all users (excluding selected from_user). Label "Delegate To". Required.
  - Start Date: Date Input (`type="date"`), required. Label "Start".
  - End Date: Date Input (`type="date"`), required, must be >= start_date. Label "End".
- **Validation**: End date < start date shows inline error below end date field: "End date must be after start date." (13px, destructive color).
- **Submit**: ShimmerButton "Create Delegation". Disabled during submission.
- **On success**: Sheet closes. New delegation card appears in Active section. Sonner toast: "Delegation created. [to_user name] can approve on behalf of [from_user name] until [end_date]."
- **On error**: Sonner toast: "Failed to create delegation. Check the dates and try again."
- **Cancel**: Ghost Button "Cancel".

### Deactivate Delegation

- **Trigger**: "Deactivate" Button on active `DelegationCard`.
- **Confirmation**: No Dialog — this is a low-stakes administrative action. Single click deactivates immediately.
- **On success**: Card moves to Expired section (or is removed if expired list is hidden). Sonner toast: "Delegation deactivated."

### Decisions Sidebar Link

- **Location in Sidebar**: Under "Work" section, between "Approvals" and any following item. Icon: lucide `ClipboardCheck` (16px).
- **Count badge**: `NumberTicker` badge showing open (proposed) decisions count. Only shown when count > 0. Updates when a decision is logged or status changes.

---

## Copywriting Contract

| Element | Copy |
|---------|------|
| Primary CTA — Log Decision | "Log Decision" |
| Primary CTA — Save Override | "Override and Approve" |
| Primary CTA — Create Delegation | "Create Delegation" |
| Primary CTA — Approve Decision | "Approve" |
| Primary CTA — Reject Decision | "Reject" |
| Decisions empty state heading | "No decisions yet" |
| Decisions empty state body | "Log the first decision to start building your governance trail." |
| Decisions empty search result | "No decisions match your filter." |
| Override Dialog title | "Override Approval" |
| Override Dialog body | "Bypassing the approval workflow. This action is recorded in the audit trail." |
| Override reason placeholder | "State the reason for overriding this approval..." |
| Reopen Decision Dialog title | "Reopen this decision?" |
| Reopen Decision Dialog body | "Approved decisions are locked for governance integrity. Reopening sets status back to Proposed." |
| Reopen confirm button | "Reopen Decision" |
| Reopen cancel button | "Keep Locked" |
| Delegation empty state heading | "No active delegations" |
| Delegation empty state body | "Delegations let an admin temporarily grant approval authority to another user." |
| Locked decision tooltip | "Locked — approved decisions cannot be edited or deleted." |
| Override attribution inline | "Overridden by [name] — [reason]" |
| On-behalf attribution inline | "Approved by [delegate name] (on behalf of [from_user name])" |
| Decision locked toast | "Decision approved and locked." |
| Decision rejected toast | "Decision rejected." |
| Decision reopened toast | "Decision reopened." |
| Decision logged toast | "Decision logged." |
| Override success toast | "Approval overridden. Validation cascade triggered." |
| Override error toast | "Override failed. Try again or check permissions." |
| Delegation created toast | "Delegation created. [to_user name] can approve on behalf of [from_user name] until [end_date]." |
| Delegation deactivated toast | "Delegation deactivated." |
| Error — decisions load failed | "Couldn't load decisions. Refresh the page or try again." |
| Error — delegation load failed | "Couldn't load delegations. Refresh the page or try again." |
| Error — decision form failed | "Failed to log decision. Check your inputs and try again." |
| Error — delegation form failed | "Failed to create delegation. Check the dates and try again." |
| Date validation inline error | "End date must be after start date." |
| Show expired delegations toggle | "Show expired" |

**Source:** CONTEXT.md decisions (D-07 through D-17 for override/delegation copy patterns). Governance domain copy derived from requirement descriptions and UX conventions.

---

## Registry Safety

| Registry | Blocks Used | Safety Gate |
|----------|-------------|-------------|
| shadcn official | button, card, badge, dialog, sheet, tabs, textarea, input, select, combobox, separator, tooltip, scroll-area, avatar, popover | not required |
| @magicui (https://magicui.design/r/) | magic-card, blur-fade, shimmer-button, animated-list, interactive-hover-button, text-animate, shine-border, cool-mode, border-beam, pulsating-button, number-ticker, avatar-circles | view passed — no flags — 2026-03-19 (Phase 02-03) and 2026-03-20 (Phase 03-03). All 15 components pre-installed. No new installs for Phase 5. Source files at `frontend/components/ui/*.tsx`. |

**No new third-party registry blocks** are introduced in Phase 5. All MagicUI components already present in the codebase. Registry safety gate is satisfied by the prior-phase install record.

---

## States Inventory

Every UI surface must handle these states explicitly.

| Surface | Loading | Empty | Error | Populated |
|---------|---------|-------|-------|-----------|
| /decisions list | Skeleton cards (3x `h-[100px] bg-muted/50 animate-pulse rounded-lg`) | Empty state (icon + heading + body + CTA) | `AlertCircle` icon + "Couldn't load decisions. Refresh the page or try again." | AnimatedList of DecisionCard |
| Decision detail expand | Skeleton text blocks (2x `h-4 bg-muted/50 animate-pulse rounded`) | n/a (only opens when card has data) | Inline "Couldn't load detail. Try again." (14px muted) | BlurFade-animated detail panel |
| Override Dialog reason field | n/a | Empty field with placeholder | Inline validation "Reason is required (minimum 10 characters)." | Field with BorderBeam on focus |
| /admin/delegations active list | Skeleton cards (2x `h-[80px] bg-muted/50 animate-pulse rounded-lg`) | Empty state (icon + heading + body + CTA) | `AlertCircle` + "Couldn't load delegations. Refresh the page or try again." | DelegationCard list |
| /admin/delegations expired list | Same skeleton | "No expired delegations." (14px muted, no icon) | Same error | De-emphasized DelegationCard list |
| ApprovalItem override button | Loader2 while override in flight | n/a | Sonner toast error | Normal button state |
| Sidebar decisions count | n/a | Badge hidden (count = 0) | Silent (last known count) | NumberTicker badge |

---

## Accessibility Contract

- Decision cards must have `aria-label="Decision: [title], status: [status], type: [type]"` on the outer MagicCard interactive element.
- Override Dialog must trap focus and return focus to the triggering "Override" button on close.
- The reason textarea in Override Dialog must have `aria-required="true"` and `aria-describedby` pointing to the inline validation message when error is present.
- "Locked" decision title icon (lucide `Lock`) must have `aria-label="Locked decision"` — not purely decorative.
- Status badge color is never the sole indicator — all three decision status states also have text labels ("Proposed", "Approved", "Rejected").
- Delegation date inputs must have `aria-describedby` pointing to the inline date validation error element.
- CoolMode particles on Override submit: `aria-hidden="true"` on particle container.
- "Show expired" toggle must be a button with `aria-expanded` state.

---

## Checker Sign-Off

- [ ] Dimension 1 Copywriting: PASS
- [ ] Dimension 2 Visuals: PASS
- [ ] Dimension 3 Color: PASS
- [ ] Dimension 4 Typography: PASS
- [ ] Dimension 5 Spacing: PASS
- [ ] Dimension 6 Registry Safety: PASS

**Approval:** pending
