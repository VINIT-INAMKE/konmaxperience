---
phase: 16-admin-cms
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/components/ops/Sidebar.tsx
  - frontend/app/(ops)/admin/guide/page.tsx
  - frontend/components/ops/guide/admin/GuideSectionList.tsx
  - frontend/components/ops/guide/admin/GuideSectionCard.tsx
  - frontend/components/ops/guide/admin/GuideSectionForm.tsx
  - frontend/components/ops/guide/admin/GuideIconPicker.tsx
  - frontend/components/ops/guide/admin/GuideColorPicker.tsx
  - frontend/components/ops/guide/admin/GuideAdminSkeleton.tsx
autonomous: true
requirements:
  - EDIT-01

must_haves:
  truths:
    - "Admin sees 'Guide Management' in sidebar under Admin section when they have MANAGE_GUIDE permission"
    - "Admin can view all guide sections with their pages in an expandable list at /admin/guide"
    - "Admin can create a new section with title, description, icon, color, and role assignments via Sheet form"
    - "Admin can edit an existing section's properties via the same Sheet form"
    - "Admin can delete a section with confirmation dialog showing page count cascade warning"
    - "Admin can reorder sections and pages via up/down arrow buttons"
    - "Admin can create a new page within a section (navigates to editor route)"
    - "Admin can delete a page with confirmation dialog"
  artifacts:
    - path: "frontend/app/(ops)/admin/guide/page.tsx"
      provides: "Admin guide management page route"
      min_lines: 30
    - path: "frontend/components/ops/guide/admin/GuideSectionList.tsx"
      provides: "Collapsible section list with page sub-lists"
      min_lines: 50
    - path: "frontend/components/ops/guide/admin/GuideSectionForm.tsx"
      provides: "Sheet-based create/edit form for sections"
      min_lines: 80
    - path: "frontend/components/ops/guide/admin/GuideSectionCard.tsx"
      provides: "Admin section card with expand, reorder, edit/delete actions"
      min_lines: 50
  key_links:
    - from: "frontend/components/ops/Sidebar.tsx"
      to: "/admin/guide"
      via: "NavItem with can('MANAGE_GUIDE') guard"
      pattern: "MANAGE_GUIDE.*admin/guide"
    - from: "frontend/components/ops/guide/admin/GuideSectionList.tsx"
      to: "/guide/sections"
      via: "React Query fetch all sections for admin"
      pattern: "apiClient.*guide/sections"
    - from: "frontend/components/ops/guide/admin/GuideSectionForm.tsx"
      to: "/guide/sections"
      via: "POST and PATCH for section create/edit"
      pattern: "apiClient.*(post|patch).*guide/sections"
---

<objective>
Build the admin guide management page at `/admin/guide` with section list, page sub-lists, section CRUD via Sheet forms, page create/delete, and reorder functionality.

Purpose: Provides the admin UI for organizing guide structure (sections and pages) before content editing. This is the "file manager" for the guide CMS.
Output: Working admin page where admins can create, edit, delete, and reorder guide sections and pages. Pages link to the editor (Plan 02).
</objective>

<execution_context>
@D:/aditee/konmaxperience/.claude/get-shit-done/workflows/execute-plan.md
@D:/aditee/konmaxperience/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-admin-cms/16-CONTEXT.md
@.planning/phases/16-admin-cms/16-RESEARCH.md
@.planning/phases/16-admin-cms/16-UI-SPEC.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->
<!-- Executor should use these directly — no codebase exploration needed. -->

From frontend/lib/types/guides.ts:
```typescript
export interface GuideSectionPage {
  id: string;
  title: string;
  slug: string;
  sort_order: number;
  status: 'draft' | 'published';
  summary: string | null;
  estimated_read_time: number | null;
}

export interface GuideSection {
  id: string;
  title: string;
  slug: string;
  description: string | null;
  icon: string | null;
  accent_color: string | null;
  sort_order: number;
  role_codes: string[];
  status: 'draft' | 'published';
  created_at: string;
  updated_at: string;
  pages: GuideSectionPage[];
}
```

From frontend/lib/types/roles.ts:
```typescript
export enum RoleCode {
  FOUNDER_ADMIN = 'FOUNDER_ADMIN',
  FRONTEND_LEAD = 'FRONTEND_LEAD',
  BACKEND_LEAD = 'BACKEND_LEAD',
  BI_LEAD = 'BI_LEAD',
  PROCUREMENT_LEAD = 'PROCUREMENT_LEAD',
  TALENT_LEAD = 'TALENT_LEAD',
  TECH_LEAD = 'TECH_LEAD',
  DESIGN_OUTREACH_LEAD = 'DESIGN_OUTREACH_LEAD',
}

export const ROLE_DISPLAY_NAMES: Record<RoleCode, string> = { ... };
```

From frontend/lib/api-client.ts:
```typescript
export const apiClient = {
  get<T>(path: string): Promise<T>,
  post<T>(path: string, body: any): Promise<T>,
  patch<T>(path: string, body: any): Promise<T>,
  delete<T>(path: string): Promise<T>,
};
```

Backend API endpoints (all require MANAGE_GUIDE permission for write operations):
- GET /guide/sections — returns GuideSection[] (admin sees all, including draft)
- POST /guide/sections — body: { title, description?, icon?, accent_color?, role_codes?, sort_order?, status? }
- PATCH /guide/sections/:id — body: partial of above + slug?
- DELETE /guide/sections/:id — cascades to pages
- POST /guide/pages — body: { section_id, title, content, summary?, sort_order?, status? }
- PATCH /guide/pages/:id — body: { title?, content?, summary?, sort_order?, status?, slug? }
- DELETE /guide/pages/:id

Sidebar admin section pattern (from Sidebar.tsx lines 292-314):
```typescript
const adminNav: NavItem[] = [
  ...(can('MANAGE_RBAC') ? [
    { label: 'Team', href: '/admin/users', icon: <Users className="size-4" /> },
    { label: 'Permissions', href: '/admin/permissions', icon: <Shield className="size-4" /> },
  ] : []),
  ...(can('MANAGE_GUIDE') ? [
    { label: 'Guide Management', href: '/admin/guide', icon: <BookOpen className="size-4" /> },
  ] : []),
  // ... more items
];
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Admin guide management page with section list, expand/collapse, and page sub-lists</name>
  <files>
    frontend/components/ops/Sidebar.tsx,
    frontend/app/(ops)/admin/guide/page.tsx,
    frontend/components/ops/guide/admin/GuideSectionList.tsx,
    frontend/components/ops/guide/admin/GuideSectionCard.tsx,
    frontend/components/ops/guide/admin/GuideAdminSkeleton.tsx
  </files>
  <read_first>
    frontend/components/ops/Sidebar.tsx,
    frontend/app/(ops)/admin/users/page.tsx,
    frontend/lib/types/guides.ts,
    frontend/lib/types/roles.ts,
    frontend/lib/api-client.ts,
    frontend/components/ops/guide/DynamicIcon.tsx,
    frontend/components/ui/border-beam.tsx,
    .planning/phases/16-admin-cms/16-UI-SPEC.md,
    .planning/phases/16-admin-cms/16-RESEARCH.md
  </read_first>
  <action>
**1. Add "Guide Management" to Sidebar admin nav (per D-07).**

In `frontend/components/ops/Sidebar.tsx`, inside the `adminNav` array, add a new entry gated by `MANAGE_GUIDE` permission. Insert it AFTER the existing `can('MANAGE_SYSTEM')` Settings entry block (at the end of adminNav). BookOpen icon is already imported in the Sidebar:

```typescript
...(can('MANAGE_GUIDE')
  ? [{ label: 'Guide Management', href: '/admin/guide', icon: <BookOpen className="size-4" /> }]
  : []),
```

**2. Create `GuideAdminSkeleton.tsx`.**

File: `frontend/components/ops/guide/admin/GuideAdminSkeleton.tsx`

A simple loading skeleton component for the section list. Must be `'use client'`. Renders 4 skeleton cards (`h-14 rounded-xl animate-pulse bg-muted`) in a `space-y-3` container.

**3. Create `GuideSectionCard.tsx`.**

File: `frontend/components/ops/guide/admin/GuideSectionCard.tsx`

Props: `{ section: GuideSection, isExpanded: boolean, onToggle: () => void, isFirst: boolean, isLast: boolean, onMoveUp: () => void, onMoveDown: () => void, onEdit: () => void, onDelete: () => void, onCreatePage: () => void, onEditPage: (pageId: string) => void, onDeletePage: (page: GuideSectionPage) => void, onMovePage: (page: GuideSectionPage, dir: 'up'|'down', pages: GuideSectionPage[]) => void }`

Card layout per UI-SPEC:
- Row: `flex items-center gap-3 px-4 py-3 rounded-xl border bg-card hover:bg-muted/50 cursor-pointer`
- Left side: `DynamicIcon` (from `@/components/ops/guide/DynamicIcon`) showing `section.icon` with `size-5` and `style={{ color: section.accent_color || undefined }}`
- Section title: `text-[16px] font-semibold text-foreground`
- Role badges: Map `section.role_codes` to compact `Badge variant="secondary"` (12px text), show max 3 then `+N more`. Use `ROLE_DISPLAY_NAMES` from `@/lib/types/roles`.
- Status badge: if `section.status === 'draft'` show amber badge (`bg-amber-500/10 text-amber-600 border-amber-500/20`), else green badge (`bg-success/10 text-success border-success/20`). Text: "Draft" or "Published".
- Page count: `{section.pages.length} pages` in `text-[14px] text-muted-foreground`
- Right side (visible on hover via group-hover): Up arrow (`ChevronUp`, disabled/opacity-30 if `isFirst`), Down arrow (`ChevronDown`, disabled if `isLast`), Edit button (pencil icon), Delete button (trash icon, `text-destructive`), ChevronDown/ChevronUp toggle for expand
- Use `group` class on the row for hover reveal of action buttons
- Expanded state: below the row, render page list with `ml-8 space-y-1 py-2`:
  - Each page: `flex items-center gap-2 px-4 py-2 hover:bg-muted/50 rounded-lg`
  - `FileText` icon (size-4, text-muted-foreground), page title (14px), status badge (same color pattern), spacer, up/down arrows for page reorder (same disabled pattern for first/last), Edit button (links to `/admin/guide/pages/${page.id}`), Delete button
  - At bottom of page list: "Add Page" button (`Plus` icon + "Add Page" text, `variant="ghost"`, `size="sm"`)
- Import `BorderBeam` from `@/components/ui/border-beam` and show it on card hover (same pattern as Phase 15 GuideSectionCard)

**4. Create `GuideSectionList.tsx`.**

File: `frontend/components/ops/guide/admin/GuideSectionList.tsx`

`'use client'` component. Props: `{ sections: GuideSection[], onEditSection: (section: GuideSection) => void, onDeleteSection: (section: GuideSection) => void, onCreatePage: (sectionId: string) => void, onEditPage: (pageId: string) => void, onDeletePage: (page: GuideSectionPage & { section_id?: string }) => void }`

State: `expandedSections` as `Set<string>` (section IDs that are expanded).

Reorder logic:
- `moveSection(section, direction)`: finds current index, gets swap target, calls `Promise.all([apiClient.patch('/guide/sections/${section.id}', { sort_order: swapTarget.sort_order }), apiClient.patch('/guide/sections/${swapTarget.id}', { sort_order: section.sort_order })])`, then invalidates `['guide-sections-admin']` query. Show `toast.error` on failure.
- `movePage(page, direction, sectionPages)`: same pattern with `/guide/pages/${page.id}` and `/guide/pages/${swapTarget.id}`.

Render: `space-y-3` container. Map sorted sections to `GuideSectionCard` components. Pass all handler props through.

**5. Create admin guide management page.**

File: `frontend/app/(ops)/admin/guide/page.tsx`

`'use client'` component (needs React Query, state for Sheet). Pattern follows `admin/users/page.tsx`.

- Page header: `flex items-center justify-between` with `<h1 className="text-[24px] font-semibold">Guide Management</h1>` left, `<Button onClick={() => setCreateOpen(true)}><Plus className="size-4 mr-1" /> Add Section</Button>` right
- Subtitle: `<p className="text-[14px] text-muted-foreground">Create and manage guide sections and pages for your team.</p>`
- Data fetching: `useQuery({ queryKey: ['guide-sections-admin'], queryFn: () => apiClient.get<GuideSection[]>('/guide/sections') })`
- Loading state: render `GuideAdminSkeleton`
- Error state: `AlertCircle` icon + "Could not load guide sections. Try refreshing the page."
- Empty state: `BookOpen` icon (size-12, text-muted-foreground) + "No guide sections yet" heading + "Create your first section to start building the team guide." body + "Add Section" button
- Section list: render `GuideSectionList` with sections data and handlers
- Section form Sheet: render `GuideSectionForm` (created in Task 2) controlled by `createOpen`/`editSection` state
- Delete section dialog: `Dialog` with title `Delete "{section.title}"?`, body `This will permanently delete the section and all {n} pages inside it. This cannot be undone.`, buttons "Keep Section" (ghost) and "Delete Section" (destructive with Loader2 during delete). Calls `apiClient.delete('/guide/sections/${id}')` then invalidates query.
- Delete page dialog: similar pattern with page title. Calls `apiClient.delete('/guide/pages/${id}')`.
- Page create handler: `onCreatePage(sectionId)` calls `apiClient.post('/guide/pages', { section_id: sectionId, title: 'Untitled page', content: '' })` then navigates to `/admin/guide/pages/${newPage.id}` using `router.push()`.
- Page edit handler: `onEditPage(pageId)` calls `router.push('/admin/guide/pages/${pageId}')`.
- Use `useRouter` from `next/navigation` and `useQueryClient` from `@tanstack/react-query`.
- Use `toast` from `sonner` for success/error notifications.
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "Guide Management" frontend/components/ops/Sidebar.tsx
    - grep -q "MANAGE_GUIDE" frontend/components/ops/Sidebar.tsx
    - grep -q "admin/guide" frontend/components/ops/Sidebar.tsx
    - test -f frontend/app/(ops)/admin/guide/page.tsx
    - grep -q "guide-sections-admin" frontend/app/(ops)/admin/guide/page.tsx
    - grep -q "GuideSectionList" frontend/app/(ops)/admin/guide/page.tsx
    - test -f frontend/components/ops/guide/admin/GuideSectionList.tsx
    - test -f frontend/components/ops/guide/admin/GuideSectionCard.tsx
    - grep -q "DynamicIcon" frontend/components/ops/guide/admin/GuideSectionCard.tsx
    - grep -q "BorderBeam" frontend/components/ops/guide/admin/GuideSectionCard.tsx
    - grep -q "ChevronUp" frontend/components/ops/guide/admin/GuideSectionCard.tsx
    - grep -q "ChevronDown" frontend/components/ops/guide/admin/GuideSectionCard.tsx
    - test -f frontend/components/ops/guide/admin/GuideAdminSkeleton.tsx
  </acceptance_criteria>
  <done>
    Admin guide management page renders at /admin/guide with section list, expand/collapse for pages, reorder arrows, and sidebar nav entry gated by MANAGE_GUIDE permission. Section and page CRUD operations call the correct backend endpoints.
  </done>
</task>

<task type="auto">
  <name>Task 2: Section form Sheet with icon picker, color picker, and role checkboxes</name>
  <files>
    frontend/components/ops/guide/admin/GuideSectionForm.tsx,
    frontend/components/ops/guide/admin/GuideIconPicker.tsx,
    frontend/components/ops/guide/admin/GuideColorPicker.tsx
  </files>
  <read_first>
    frontend/components/ops/guide/admin/GuideSectionList.tsx,
    frontend/app/(ops)/admin/guide/page.tsx,
    frontend/components/ops/guide/DynamicIcon.tsx,
    frontend/components/ops/operations/vendors/VendorForm.tsx,
    frontend/components/ops/operations/zones/ZoneForm.tsx,
    frontend/lib/types/roles.ts,
    .planning/phases/16-admin-cms/16-UI-SPEC.md
  </read_first>
  <action>
**1. Create `GuideIconPicker.tsx` (per D-11, UI-SPEC Icon Picker section).**

File: `frontend/components/ops/guide/admin/GuideIconPicker.tsx`

Props: `{ value: string | null, onChange: (icon: string) => void }`

Predefined list of 20 Lucide icon names (from UI-SPEC):
```typescript
const GUIDE_ICONS = [
  'BookOpen', 'ChefHat', 'ShoppingCart', 'PackageSearch', 'Salad',
  'Truck', 'LayoutDashboard', 'ClipboardList', 'Users', 'Shield',
  'Settings', 'BarChart3', 'Rocket', 'CheckCircle', 'Trophy',
  'Gauge', 'Monitor', 'CalendarDays', 'MessageSquare', 'FolderOpen',
] as const;
```

Render as `grid grid-cols-5 gap-2`. Each icon cell: `size-9 rounded-lg border flex items-center justify-center cursor-pointer hover:bg-muted`. Selected state: `bg-primary/10 border-primary`. Use `DynamicIcon` from `@/components/ops/guide/DynamicIcon` with `className="size-5"` to render each icon. On click, call `onChange(iconName)`.

Label above the grid: `<label className="text-[14px] font-medium">Section Icon</label>`

**2. Create `GuideColorPicker.tsx` (per D-11, UI-SPEC Color Picker section).**

File: `frontend/components/ops/guide/admin/GuideColorPicker.tsx`

Props: `{ value: string | null, onChange: (color: string) => void }`

12 predefined hex swatches (from UI-SPEC):
```typescript
const GUIDE_COLORS = [
  '#9E7AFF', '#FE8BBB', '#f97316', '#eab308', '#22c55e', '#06b6d4',
  '#3b82f6', '#8b5cf6', '#ec4899', '#14b8a6', '#f43f5e', '#64748b',
];
```

Render as `flex flex-wrap gap-2`. Each swatch: `<button>` with `size-7 rounded-full cursor-pointer border-2 border-transparent hover:scale-110 transition-transform` and `style={{ backgroundColor: color }}`. Selected state: `border-2 border-ring ring-2 ring-ring ring-offset-2`. On click, call `onChange(color)`.

Label: `<label className="text-[14px] font-medium">Accent Color</label>`

**3. Create `GuideSectionForm.tsx` (per D-08, D-11, UI-SPEC Section Form).**

File: `frontend/components/ops/guide/admin/GuideSectionForm.tsx`

Props: `{ open: boolean, onOpenChange: (open: boolean) => void, section?: GuideSection | null }`

If `section` is provided, it is an edit form pre-filled with the section's values. Otherwise, it is a create form.

Uses shadcn `Sheet` (side="right", className="max-w-[500px] w-full") — same pattern as VendorForm and ZoneForm.

Form fields (inside `SheetContent`):
- `SheetHeader` with `SheetTitle`: "Add Section" or "Edit Section" depending on mode
- Title: `Input` with `label="Section title"`, required, `value={title}` state
- Description: `Textarea` with `label="Description"`, optional, `rows={3}`, `value={description}` state
- Icon picker: render `GuideIconPicker` with `value={icon}` and `onChange={setIcon}`
- Color picker: render `GuideColorPicker` with `value={accentColor}` and `onChange={setAccentColor}`
- Role multi-select: label "Visible to roles". Map all `Object.entries(ROLE_DISPLAY_NAMES)` to checkboxes. Each: `<div className="flex items-center gap-2"><Checkbox checked={selectedRoles.includes(roleCode)} onCheckedChange={(checked) => { ... }} id={roleCode} /><label htmlFor={roleCode} className="text-[14px]">{displayName}</label></div>`. Use `selectedRoles: string[]` state.
- `SheetFooter`: "Cancel" button (`variant="ghost"`, calls `onOpenChange(false)`) + "Save Section" button (`variant="default"`, loading state with `Loader2`)

On submit:
- Create mode: `apiClient.post('/guide/sections', { title, description: description || undefined, icon: icon || undefined, accent_color: accentColor || undefined, role_codes: selectedRoles.length > 0 ? selectedRoles : undefined })`
- Edit mode: `apiClient.patch('/guide/sections/${section.id}', { ...same fields })`
- On success: `toast.success('Section created')` or `toast.success('Section updated')`, invalidate `['guide-sections-admin']` query, call `onOpenChange(false)`
- On error: `toast.error('Failed to save section')`

Reset form state when `open` changes or `section` changes. Use `useEffect` to populate form when editing.

Import `useQueryClient` from `@tanstack/react-query`, `toast` from `sonner`.
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - test -f frontend/components/ops/guide/admin/GuideIconPicker.tsx
    - grep -q "GUIDE_ICONS" frontend/components/ops/guide/admin/GuideIconPicker.tsx
    - grep -q "DynamicIcon" frontend/components/ops/guide/admin/GuideIconPicker.tsx
    - grep -q "grid grid-cols-5" frontend/components/ops/guide/admin/GuideIconPicker.tsx
    - test -f frontend/components/ops/guide/admin/GuideColorPicker.tsx
    - grep -q "GUIDE_COLORS" frontend/components/ops/guide/admin/GuideColorPicker.tsx
    - grep -q "rounded-full" frontend/components/ops/guide/admin/GuideColorPicker.tsx
    - test -f frontend/components/ops/guide/admin/GuideSectionForm.tsx
    - grep -q "Sheet" frontend/components/ops/guide/admin/GuideSectionForm.tsx
    - grep -q "GuideIconPicker" frontend/components/ops/guide/admin/GuideSectionForm.tsx
    - grep -q "GuideColorPicker" frontend/components/ops/guide/admin/GuideSectionForm.tsx
    - grep -q "ROLE_DISPLAY_NAMES" frontend/components/ops/guide/admin/GuideSectionForm.tsx
    - grep -q "Checkbox" frontend/components/ops/guide/admin/GuideSectionForm.tsx
    - grep -q "guide/sections" frontend/components/ops/guide/admin/GuideSectionForm.tsx
  </acceptance_criteria>
  <done>
    Section form Sheet opens for create/edit with all fields (title, description, icon picker with 20 Lucide icons in 5x4 grid, color picker with 12 swatches, role multi-select checkboxes). Form submits to backend API and invalidates React Query cache. Icon picker and color picker show selected state visually.
  </done>
</task>

</tasks>

<verification>
1. `cd frontend && npx tsc --noEmit` passes with zero errors
2. `cd frontend && npx next build` succeeds
3. All 8 new/modified files exist and contain the expected patterns per acceptance criteria
4. Sidebar shows "Guide Management" nav item for MANAGE_GUIDE permission holders
5. `/admin/guide` page renders section list with expand/collapse, CRUD operations, and reorder arrows
</verification>

<success_criteria>
- Admin guide management page renders at /admin/guide with header, empty state, and section list
- Sections show with icon, title, role badges, status badge, page count, and action buttons
- Section form Sheet creates and edits sections with all 5 fields
- Delete dialogs show cascade warnings with page counts
- Reorder (up/down arrows) swaps sort_order values via PATCH API calls
- Pages list within expanded sections with create, edit (navigates to /admin/guide/pages/[id]), and delete
- TypeScript compiles with zero errors
</success_criteria>

<output>
After completion, create `.planning/phases/16-admin-cms/16-01-SUMMARY.md`
</output>
