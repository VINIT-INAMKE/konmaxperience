---
phase: 13-customer-experience
plan: 02
type: execute
wave: 2
depends_on: ["13-01"]
files_modified:
  - frontend/app/(public)/layout.tsx
  - frontend/app/(public)/feedback/[orderId]/page.tsx
  - frontend/app/(public)/events/page.tsx
  - frontend/app/(public)/events/[id]/page.tsx
  - frontend/app/(public)/menu/page.tsx
  - frontend/components/public/StarRatingInput.tsx
  - frontend/components/public/FeedbackThankYou.tsx
  - frontend/components/public/EventCard.tsx
  - frontend/components/public/EventBookingForm.tsx
  - frontend/components/public/CapacityBadge.tsx
  - frontend/components/public/MenuBrandTabs.tsx
  - frontend/components/public/MenuItemPublicCard.tsx
  - frontend/components/public/AvailabilityBadge.tsx
autonomous: true
requirements:
  - CUST-01
  - CUST-02
  - CUST-03
must_haves:
  truths:
    - "Customer can scan QR code, land on /feedback/[orderId], rate their meal 1-5 stars, and submit without logging in"
    - "After feedback submission, customer sees animated thank-you screen with confetti"
    - "Customer can browse upcoming events at /events, see capacity status, and book with name+phone+guests"
    - "Booking form rejects when event is sold out or guests exceed remaining spots"
    - "Customer can view digital menu at /menu with brand tabs, item names, prices, and Available/Sold Out badges"
    - "All public pages render in light theme with no sidebar and no auth redirect"
  artifacts:
    - path: "frontend/app/(public)/layout.tsx"
      provides: "Light-theme public layout with header and footer"
      contains: "Konma Xperience"
    - path: "frontend/app/(public)/feedback/[orderId]/page.tsx"
      provides: "Feedback form page with star rating"
      contains: "StarRatingInput"
    - path: "frontend/app/(public)/events/page.tsx"
      provides: "Public event listing grid"
      contains: "EventCard"
    - path: "frontend/app/(public)/events/[id]/page.tsx"
      provides: "Event detail + booking form"
      contains: "EventBookingForm"
    - path: "frontend/app/(public)/menu/page.tsx"
      provides: "Digital menu display"
      contains: "MenuBrandTabs"
  key_links:
    - from: "frontend/app/(public)/feedback/[orderId]/page.tsx"
      to: "/feedback"
      via: "apiClient.post"
      pattern: "apiClient\\.post.*feedback"
    - from: "frontend/app/(public)/events/[id]/page.tsx"
      to: "/events/:id/bookings"
      via: "apiClient.post"
      pattern: "apiClient\\.post.*bookings"
    - from: "frontend/app/(public)/menu/page.tsx"
      to: "/menu/availability"
      via: "useQuery with refetchInterval 60000"
      pattern: "refetchInterval.*60"
---

<objective>
Build all customer-facing public pages: feedback form (QR-accessed), event listing with booking, and digital menu display. Creates the `(public)` route group with its own light-theme layout, 8 new components, and 5 pages.

Purpose: These are the first public-facing surfaces of Konma Xperience OS — what customers see after dining, when browsing events, or viewing the menu via QR code or display screen.
Output: 5 Next.js pages under `(public)` route group, 8 shared components in `components/public/`, fully responsive mobile-first design.
</objective>

<execution_context>
@D:/aditee/konmaxperience/.claude/get-shit-done/workflows/execute-plan.md
@D:/aditee/konmaxperience/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-customer-experience/13-CONTEXT.md
@.planning/phases/13-customer-experience/13-RESEARCH.md
@.planning/phases/13-customer-experience/13-UI-SPEC.md
@.planning/phases/13-customer-experience/13-01-SUMMARY.md

<interfaces>
<!-- Types created in Plan 01 that this plan consumes -->

From frontend/lib/types/feedback.ts (created in Plan 01):
```typescript
export interface Feedback {
  id: string; order_id: string | null; rating: number;
  comment: string | null; customer_name: string | null;
  customer_phone: string | null; created_at: string;
  order?: { id: string } | null;
}
export interface CreateFeedbackPayload {
  order_id?: string; rating: number; comment?: string;
  customer_name?: string; customer_phone?: string;
}
```

From frontend/lib/types/events.ts (created in Plan 01):
```typescript
export type EventType = 'dining' | 'workshop' | 'pop_up' | 'tasting' | 'other';
export type EventStatus = 'upcoming' | 'past' | 'cancelled';
export interface Event {
  id: string; title: string; event_type: EventType; date: string;
  capacity: number; price: number; description: string | null;
  image_url: string | null; status: EventStatus;
  zone?: { id: string; name: string } | null;
  brand?: { id: string; name: string } | null;
  booked_guests?: number; spots_remaining?: number;
}
export interface CreateBookingPayload {
  customer_name: string; customer_phone: string; guests: number;
}
export const EVENT_TYPE_LABELS: Record<EventType, string>;
```

From frontend/lib/types/menu.ts (existing):
```typescript
export interface MenuItem {
  id: string; name: string; base_price: number;
  image_url: string | null; available: boolean;
  status: MenuItemStatus;
  category?: { id: string; name: string; brand_id: string } | null;
}
export interface MenuCategory {
  id: string; name: string; brand_id: string;
  sort_order: number; status: MenuCategoryStatus;
  MenuItems?: MenuItem[];
}
```

From frontend/lib/api-client.ts (existing):
```typescript
export const apiClient = {
  get: <T>(path: string) => request<T>('GET', path),
  post: <T>(path: string, body?: unknown) => request<T>('POST', path, body),
};
```

From frontend/components/ui/confetti.tsx (existing):
```typescript
type Api = { fire: (options?: ConfettiOptions) => void }
// Usage: <Confetti ref={confettiRef} manualstart ... />
// Fire: confettiRef.current?.fire()
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Public layout + Feedback page with star rating and thank-you confetti</name>
  <files>
    frontend/app/(public)/layout.tsx,
    frontend/app/(public)/feedback/[orderId]/page.tsx,
    frontend/components/public/StarRatingInput.tsx,
    frontend/components/public/FeedbackThankYou.tsx
  </files>
  <read_first>
    frontend/app/(ops)/layout.tsx,
    frontend/components/ui/confetti.tsx,
    frontend/components/ui/blur-fade.tsx,
    frontend/lib/api-client.ts,
    frontend/lib/types/feedback.ts,
    frontend/app/layout.tsx
  </read_first>
  <action>
**IMPORTANT:** Before writing any Next.js code, read `node_modules/next/dist/docs/` guides relevant to layouts and route groups per `frontend/AGENTS.md`.

**1. Public layout — `frontend/app/(public)/layout.tsx`:**

Create new route group directory and layout. This is a `'use client'` layout (no auth, no server-side logic).

- NO import of `useAuthStore` or any auth module.
- NO sidebar.
- Light theme wrapper: wrap children in a `<div className="light min-h-screen bg-white text-foreground flex flex-col">`. The `light` class overrides Tailwind's dark mode. If that doesn't work (check if Tailwind v4 uses class-based dark mode by reading the app's tailwind config), use `data-theme="light"`.
- Header: `<header className="h-14 border-b bg-white/95 backdrop-blur sticky top-0 z-10 flex items-center px-4">` containing a `<span className="text-sm font-semibold tracking-tight text-gray-900">Konma Xperience</span>`.
- Main: `<main className="flex-1">{children}</main>`.
- Footer: `<footer className="h-10 flex items-center justify-center"><span className="text-xs text-gray-500">Powered by Konma Xperience</span></footer>`.

**2. StarRatingInput component — `frontend/components/public/StarRatingInput.tsx`:**

`'use client'` component. Props: `{ value: number; onChange: (v: number) => void }`.

- Render 5 star buttons in a flex row with `gap-1`.
- Each button: `<button type="button" className="min-h-[44px] min-w-[44px] flex items-center justify-center rounded-md hover:bg-gray-100 transition-colors">`.
- Use `Star` icon from `lucide-react`. Size: `size-8`.
- Hover state: `useState<number>(0)` for hovered star. On hover, fill all stars up to hovered. On mouse leave, revert to `value`.
- Active state: fill all stars up to `value` with `fill-primary text-primary` (amber-500 for light theme: use `className="fill-amber-500 text-amber-500"` for filled, `"fill-none text-gray-300"` for unfilled).
- Touch-friendly: 44px minimum targets.

**3. FeedbackThankYou component — `frontend/components/public/FeedbackThankYou.tsx`:**

`'use client'` component. No props.

- Render centered content: `<div className="flex flex-col items-center justify-center py-16 space-y-4">`.
- Heading: `<h2 className="text-3xl font-semibold">Thank you!</h2>` per UI-SPEC copywriting.
- Body: `<p className="text-base text-gray-500">Your feedback helps us improve every meal.</p>`.
- Import `Confetti` from `@/components/ui/confetti`. Use `ref` with `manualstart` prop. Fire confetti on mount via `useEffect(() => { confettiRef.current?.fire(); }, [])`.
- Wrap in `BlurFade` with `direction="up"` for entrance animation.

**4. Feedback page — `frontend/app/(public)/feedback/[orderId]/page.tsx`:**

`'use client'` page. Extract `orderId` from `useParams()`.

Layout: centered `max-w-md mx-auto px-4 py-8`.

State:
- `submitted: boolean` — toggles form vs thank-you.
- `rating: number` — 0 initially (no selection).
- `comment: string`.
- `customer_name: string`.
- `customer_phone: string`.
- `submitting: boolean` — loading state.
- `error: string | null`.

Load: No data fetch needed on mount. The `orderId` param is just passed to the POST body. Optionally query `GET /orders/${orderId}` to verify order exists — but per UI-SPEC interaction contract: "If order not found, show inline info message: 'We couldn't find your order, but you can still leave feedback.' Form renders regardless." So wrap in try/catch, show info message if 404, but still render form.

Form fields (per D-02):
1. `<StarRatingInput value={rating} onChange={setRating} />` with label "Rate your meal" above it.
2. `<Textarea placeholder="Tell us what you loved or what we can do better..." />` for comment.
3. `<Input placeholder="Your name (optional)" maxLength={100} />` for customer_name.
4. `<Input placeholder="Your number (optional)" maxLength={20} />` for customer_phone.

Validation (client-side per UI-SPEC):
- Rating required (1-5). Submit button disabled until rating > 0.
- Comment optional.
- Name optional (max 100 chars).
- Phone optional (max 20 chars).

Submission:
- Button text: "Submit Feedback" with `Loader2` spinner when submitting.
- POST to `/feedback` with body: `{ order_id: orderId, rating, comment: comment || undefined, customer_name: customer_name || undefined, customer_phone: customer_phone || undefined }`.
- On success: set `submitted = true`. Form fades out, FeedbackThankYou fades in.
- On error: show inline error below button: "Couldn't submit your feedback -- please check your connection and try again."
- Per D-03: No redirect after submission. Page is the end.

Sub-heading under page title: `<p className="text-sm text-gray-500">Takes 30 seconds. Helps us improve.</p>`.

Wrap page in `BlurFade`.

  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - File frontend/app/(public)/layout.tsx exists and contains "Konma Xperience" and "Powered by Konma Xperience"
    - grep "light" frontend/app/(public)/layout.tsx returns a match (light theme class)
    - grep "useAuthStore" frontend/app/(public)/layout.tsx returns NO match (no auth in public layout)
    - File frontend/components/public/StarRatingInput.tsx exists and contains "min-h-\[44px\]" (touch targets)
    - File frontend/components/public/FeedbackThankYou.tsx exists and contains "Thank you!" and "confetti"
    - File frontend/app/(public)/feedback/[orderId]/page.tsx exists and contains "apiClient.post" and "StarRatingInput" and "Submit Feedback"
    - grep "Sidebar" frontend/app/(public)/layout.tsx returns NO match
    - npx tsc --noEmit succeeds
  </acceptance_criteria>
  <done>
    Public layout renders light-theme header + footer with no sidebar or auth. Feedback page at /feedback/[orderId] shows star rating (44px touch targets), comment, optional name/phone. On submit, POST to /feedback, then shows confetti thank-you. All pages mobile-first and responsive.
  </done>
</task>

<task type="auto">
  <name>Task 2: Events listing + detail + booking, Digital menu display</name>
  <files>
    frontend/app/(public)/events/page.tsx,
    frontend/app/(public)/events/[id]/page.tsx,
    frontend/app/(public)/menu/page.tsx,
    frontend/components/public/EventCard.tsx,
    frontend/components/public/EventBookingForm.tsx,
    frontend/components/public/CapacityBadge.tsx,
    frontend/components/public/MenuBrandTabs.tsx,
    frontend/components/public/MenuItemPublicCard.tsx,
    frontend/components/public/AvailabilityBadge.tsx
  </files>
  <read_first>
    frontend/app/(public)/layout.tsx,
    frontend/lib/types/events.ts,
    frontend/lib/types/menu.ts,
    frontend/lib/api-client.ts,
    frontend/components/ui/magic-card.tsx,
    frontend/components/ui/badge.tsx,
    frontend/components/ui/blur-fade.tsx,
    frontend/app/(ops)/operations/menu/page.tsx
  </read_first>
  <action>
**IMPORTANT:** Before writing any Next.js code, read `node_modules/next/dist/docs/` guides relevant to dynamic routes per `frontend/AGENTS.md`.

**1. CapacityBadge — `frontend/components/public/CapacityBadge.tsx`:**

Props: `{ spotsRemaining: number }`.
- If `spotsRemaining > 0`: render `<Badge variant="secondary">{spotsRemaining} spots left</Badge>`.
- If `spotsRemaining <= 0`: render `<Badge variant="outline" className="text-gray-400">Sold Out</Badge>`.

**2. AvailabilityBadge — `frontend/components/public/AvailabilityBadge.tsx`:**

Props: `{ available: boolean }`.
- If available: `<Badge variant="default">Available</Badge>`.
- If not: `<Badge variant="secondary" className="text-gray-400">Sold Out</Badge>`.

**3. EventCard — `frontend/components/public/EventCard.tsx`:**

Props: `{ event: Event }`. Uses `MagicCard` wrapper from `@/components/ui/magic-card`.

Layout inside card (per D-05):
- Title: `<h3 className="text-xl font-semibold">{event.title}</h3>`.
- Date: formatted with `new Date(event.date).toLocaleDateString('en-IN', { weekday: 'short', month: 'short', day: 'numeric', hour: '2-digit', minute: '2-digit' })`.
- Event type badge: `<Badge variant="secondary">{EVENT_TYPE_LABELS[event.event_type]}</Badge>`.
- Price: `<span className="text-sm">Rs. {event.price}</span>` (Decimal comes as number from API).
- `<CapacityBadge spotsRemaining={event.spots_remaining ?? 0} />`.
- Zone and brand names if present.
- Wrap in `<Link href={"/events/" + event.id}>` so clicking opens detail.

MagicCard: Use default `gradientColor` for light theme (no `#1a1a2e` override since public is light).

**4. EventBookingForm — `frontend/components/public/EventBookingForm.tsx`:**

Props: `{ eventId: string; eventDate: string; spotsRemaining: number; onBooked: () => void }`.

`'use client'` component. State: `customer_name`, `customer_phone`, `guests` (default 1), `submitting`, `error`, `booked` (boolean).

- If `spotsRemaining <= 0` or `booked === false` and spots are 0: show disabled state with text "This event is full" per UI-SPEC.
- Form fields (per D-06):
  - Name: `<Input placeholder="Your name" required />`.
  - Phone: `<Input placeholder="Your phone number" required />`.
  - Guests: number input with min=1, max=spotsRemaining. Use `<Input type="number" min={1} max={spotsRemaining} />` with +/- buttons around it for stepper per UI-SPEC.
- Validation: name required (min 1 char), phone required (min 5 chars), guests >= 1 and <= spotsRemaining.
- Submit: POST to `/events/${eventId}/bookings` with `{ customer_name, customer_phone, guests }`.
- On success: set `booked = true`. Show inline confirmation: "You're booked! We'll see you on [formatted date]." per UI-SPEC. Call `onBooked()` to refresh parent data.
- On error: show inline error below button. If capacity error from server: "Sorry, this event just filled up. No spots remain for [N] guests." per UI-SPEC.
- CTA text: "Confirm Booking" per UI-SPEC.
- Never use toast on public pages (per UI-SPEC interaction contract).

**5. Events listing page — `frontend/app/(public)/events/page.tsx`:**

`'use client'` page. Title: "Upcoming Experiences" per UI-SPEC.

- Fetch: `useQuery({ queryKey: ['public-events'], queryFn: () => apiClient.get<Event[]>('/events') })`.
- Grid: `<div className="grid gap-6 md:grid-cols-2">` per mobile responsiveness contract.
- Map events to `<EventCard>` wrapped in `<BlurFade delay={index * 0.05} direction="up">` for staggered entrance.
- Empty state: heading "No upcoming events", body "Check back soon -- we're always planning something new." per UI-SPEC.
- Loading: 4 `<Skeleton>` cards in grid.
- Max width: `max-w-4xl mx-auto px-4 py-8`.

**6. Event detail page — `frontend/app/(public)/events/[id]/page.tsx`:**

`'use client'` page. Extract `id` from `useParams()`.

- Fetch: `useQuery({ queryKey: ['public-event', id], queryFn: () => apiClient.get<Event>('/events/' + id) })`.
- Desktop (md+): 2-column grid. Left: image (or placeholder with `event_type` icon). Right: title, date, type badge, price, description, zone/brand, capacity badge, booking form.
- Mobile: single column, image above details.
- Image: if `image_url`, use `next/image` with `object-cover` in aspect-ratio container. If null, show placeholder with event type label centered, muted background.
- Show `<CapacityBadge spotsRemaining={event.spots_remaining ?? 0} />`.
- Show `<EventBookingForm>` below details.
- Section title above form: "Book Your Spot" per UI-SPEC.
- Wrap in `BlurFade`.

**7. MenuBrandTabs — `frontend/components/public/MenuBrandTabs.tsx`:**

Props: `{ brands: { id: string; name: string }[]; activeBrandId: string; onBrandChange: (id: string) => void }`.

- Use shadcn `Tabs` / `TabsList` / `TabsTrigger` with brand names.
- First brand selected by default (per D-09).
- Horizontal scroll on mobile if many brands: `<TabsList className="overflow-x-auto flex-nowrap w-full justify-start">`.

**8. MenuItemPublicCard — `frontend/components/public/MenuItemPublicCard.tsx`:**

Props: `{ item: MenuItem; available: boolean }`.

- Card layout: name, price (base_price only per D-11), image (if `image_url`, use `next/image`; if null, placeholder with first letter centered), availability badge.
- Not clickable (per UI-SPEC — display only, no interactive elements).
- Image: 1:1 aspect ratio container with `object-cover`.
- Price: `Rs. {item.base_price}`.
- `<AvailabilityBadge available={available} />`.

**9. Digital menu page — `frontend/app/(public)/menu/page.tsx`:**

`'use client'` page. Title: "Our Menu" per UI-SPEC.

- Fetch brands: query categories grouped by brand. Use `useQuery({ queryKey: ['public-menu-categories'], queryFn: () => apiClient.get<MenuCategory[]>('/menu/categories') })`.
- Extract unique brands from categories: `Array.from(new Map(categories.map(c => [c.brand_id, { id: c.brand_id, name: '' }]))).map(...)`. NOTE: categories include brand_id but not brand name. Fetch brands separately OR use the brand name from the ops endpoint. For simplicity, fetch items with `brand_id` filter. The existing `GET /menu/items?brand_id=X` returns items. The brand names can be derived from the categories' data or fetch brands.

  Actually, the simplest approach: fetch all categories (they have brand_id), fetch all items, fetch brands list via `GET /brands` (this endpoint exists and can be made @Public() if needed, OR just hardcode brand lookup from category data).

  Better approach: Since `MenuCategory` does not include brand name, the public menu page should:
  1. Fetch all items: `GET /menu/items` (returns all active items with category relation including brand_id).
  2. Fetch all categories: `GET /menu/categories` (returns categories with brand_id).
  3. Group items by brand_id from their category.
  4. For brand names, the categories only have brand_id. Use a separate `GET /brands` call. This endpoint is not yet @Public(). To avoid modifying more backend code, use the brand name from the first item's category data OR add brand name to the category include in the menu service.

  For this plan: keep it simple. Fetch items + categories. The brand tabs show brand names. Since we need brand names and categories only have brand_id, the executor should check if `GET /menu/categories` includes the brand relation. If not, add a quick `GET /brands` call (the BrandsController already exists, just needs @Public() on the GET handler). If this is too complex, skip the brand name and use the brand_id as a fallback label. The executor should check the actual API response shape.

  PRACTICAL APPROACH: Use `GET /menu/categories` which returns categories. Group by brand_id. For brand names, fetch `GET /operations/brands` (existing BrandsController) — but this is auth-protected. Instead, add `@Public()` to the `GET` handler in `brands.controller.ts` (just one decorator addition), OR fetch categories and items together and derive brand info from item.category.brand if the include is present.

  FINAL DECISION: The executor should add `@Public()` to the `@Get()` handler in `backend/src/brands/brands.controller.ts` (the findAll method). This is a single-line change. Then the public menu page can fetch brands at `/brands`.

- Fetch availability: `useQuery({ queryKey: ['public-menu-availability'], queryFn: () => apiClient.get<Record<string, { available: boolean; servings_remaining: number }>>('/menu/availability'), refetchInterval: 60_000 })` per D-10 and Research Pattern 5.

- State: `activeBrandId` — first brand selected.
- On brand tab change: filter displayed categories and items client-side. Do NOT re-fetch availability (per Research Pitfall 5 — cache full map, filter client-side).
- Display: For active brand, show categories as sections. Under each category heading, show item cards in grid: `sm:grid-cols-2 lg:grid-cols-3` per mobile responsiveness contract.
- Item availability: look up `availability[item.id]?.available` — pass boolean to `<AvailabilityBadge>`. Show "Available" or "Sold Out" only (per D-10, no serving count).
- Max width: `max-w-4xl mx-auto px-4 py-8`.
- Empty state: "Menu is being updated. Check back shortly." per UI-SPEC.
- Loading: Skeletons for tabs + card grid.

NOTE: The executor must also add `@Public()` to `@Get()` in `backend/src/brands/brands.controller.ts`. Read that file first, add the import for `Public` and the decorator. This is a minimal change (2 lines).

  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - File frontend/app/(public)/events/page.tsx exists and contains "Upcoming Experiences" and "EventCard"
    - File frontend/app/(public)/events/[id]/page.tsx exists and contains "EventBookingForm" and "Book Your Spot"
    - File frontend/app/(public)/menu/page.tsx exists and contains "Our Menu" and "MenuBrandTabs" and "refetchInterval"
    - grep "60.000\|60000\|60_000" frontend/app/(public)/menu/page.tsx returns a match (60s availability refresh)
    - File frontend/components/public/EventCard.tsx exists and contains "MagicCard"
    - File frontend/components/public/EventBookingForm.tsx exists and contains "Confirm Booking" and does NOT contain "toast"
    - File frontend/components/public/CapacityBadge.tsx exists and contains "spots left" and "Sold Out"
    - File frontend/components/public/AvailabilityBadge.tsx exists and contains "Available" and "Sold Out"
    - File frontend/components/public/MenuItemPublicCard.tsx exists and does NOT contain "onClick" (display only per D-09)
    - npx tsc --noEmit succeeds
  </acceptance_criteria>
  <done>
    Public events listing at /events shows card grid with MagicCard spotlight, capacity badges, and staggered BlurFade entrance. Event detail at /events/[id] shows 2-col desktop / 1-col mobile layout with EventBookingForm enforcing capacity. Digital menu at /menu shows brand tabs, category sections, item cards with base_price and binary availability badges refreshing every 60 seconds. All pages use light theme, are mobile-first responsive, and never import auth.
  </done>
</task>

</tasks>

<verification>
1. `cd frontend && npx tsc --noEmit` succeeds
2. All 5 public pages exist under `frontend/app/(public)/`
3. All 8 public components exist under `frontend/components/public/`
4. No public page imports `useAuthStore` (grep confirms)
5. Light theme class applied in public layout
6. Menu availability uses 60-second refetchInterval
7. Event booking form shows inline errors (no toast)
</verification>

<success_criteria>
- /feedback/[orderId] page: star rating, comment, optional fields, confetti thank-you on submit
- /events page: card grid of upcoming events with capacity status
- /events/[id] page: event detail with inline booking form, capacity enforcement
- /menu page: brand tabs, category sections, item cards with Available/Sold Out, 60s availability refresh
- All public pages: light theme, no sidebar, no auth, mobile-first responsive
- Zero TypeScript errors
</success_criteria>

<output>
After completion, create `.planning/phases/13-customer-experience/13-02-SUMMARY.md`
</output>
