---
phase: 13-customer-experience
plan: 03
type: execute
wave: 2
depends_on: ["13-01"]
files_modified:
  - frontend/app/(ops)/operations/feedback/page.tsx
  - frontend/app/(ops)/operations/events/page.tsx
  - frontend/components/ops/operations/feedback/FeedbackRow.tsx
  - frontend/components/ops/operations/feedback/FeedbackStatsCard.tsx
  - frontend/components/ops/operations/feedback/RatingFilterTabs.tsx
  - frontend/components/ops/operations/events/EventForm.tsx
  - frontend/components/ops/operations/events/EventRow.tsx
  - frontend/components/ops/operations/events/BookingListSheet.tsx
  - frontend/components/ops/Sidebar.tsx
autonomous: true
requirements:
  - CUST-01
  - CUST-02
must_haves:
  truths:
    - "Ops staff can view all customer feedback at /operations/feedback with average rating stats and rating/date filters"
    - "Ops staff can create, edit, and delete experience events at /operations/events"
    - "Ops staff can view bookings for any event via a Sheet overlay"
    - "Sidebar shows Feedback and Events links under Operations section"
    - "Order detail page can generate QR code for feedback (via existing QR endpoint)"
  artifacts:
    - path: "frontend/app/(ops)/operations/feedback/page.tsx"
      provides: "Feedback ops page with stats card and filtered table"
      contains: "Customer Feedback"
    - path: "frontend/app/(ops)/operations/events/page.tsx"
      provides: "Events ops CRUD page"
      contains: "Experience Events"
    - path: "frontend/components/ops/Sidebar.tsx"
      provides: "Updated sidebar with Feedback + Events nav items"
      contains: "Feedback"
  key_links:
    - from: "frontend/app/(ops)/operations/feedback/page.tsx"
      to: "/feedback"
      via: "apiClient.get"
      pattern: "apiClient\\.get.*feedback"
    - from: "frontend/app/(ops)/operations/events/page.tsx"
      to: "/events/all"
      via: "apiClient.get"
      pattern: "apiClient\\.get.*events/all"
    - from: "frontend/components/ops/Sidebar.tsx"
      to: "operationsNav"
      via: "array entries"
      pattern: "Feedback.*Events"
---

<objective>
Build internal ops pages for managing customer feedback and experience events. Creates the feedback viewing page with stats and filters, the events CRUD page with booking list, and updates the sidebar navigation.

Purpose: Give ops staff visibility into customer feedback and the ability to manage experience events — the internal counterpart to the public-facing pages built in Plan 02.
Output: 2 ops pages, 6 new components, sidebar navigation update with Feedback and Events links.
</objective>

<execution_context>
@D:/aditee/konmaxperience/.claude/get-shit-done/workflows/execute-plan.md
@D:/aditee/konmaxperience/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-customer-experience/13-CONTEXT.md
@.planning/phases/13-customer-experience/13-RESEARCH.md
@.planning/phases/13-customer-experience/13-UI-SPEC.md
@.planning/phases/13-customer-experience/13-01-SUMMARY.md

<interfaces>
<!-- Types and APIs created in Plan 01 -->

From frontend/lib/types/feedback.ts (Plan 01):
```typescript
export interface Feedback {
  id: string; order_id: string | null; rating: number;
  comment: string | null; customer_name: string | null;
  customer_phone: string | null; created_at: string;
  order?: { id: string } | null;
}
export interface FeedbackStats {
  average_rating: number; total_count: number;
}
```

From frontend/lib/types/events.ts (Plan 01):
```typescript
export interface Event {
  id: string; title: string; event_type: EventType; date: string;
  capacity: number; price: number; description: string | null;
  image_url: string | null; status: EventStatus;
  zone?: { id: string; name: string } | null;
  brand?: { id: string; name: string } | null;
  bookings?: EventBooking[];
  booked_guests?: number; spots_remaining?: number;
}
export interface EventBooking {
  id: string; event_id: string; customer_name: string;
  customer_phone: string; guests: number; created_at: string;
}
export interface CreateEventPayload { ... }
export interface UpdateEventPayload extends Partial<CreateEventPayload> { status?: EventStatus; }
export const EVENT_TYPE_LABELS: Record<EventType, string>;
export const EVENT_STATUS_LABELS: Record<EventStatus, string>;
```

API endpoints (Plan 01):
- GET /feedback?rating=N&date_from=X&date_to=Y — auth required (MANAGE_POS)
- GET /feedback/stats — auth required (MANAGE_POS)
- GET /events/all — auth required (MANAGE_OPS) — all events for ops
- POST /events — auth required (MANAGE_OPS)
- PATCH /events/:id — auth required (MANAGE_OPS)
- DELETE /events/:id — auth required (MANAGE_OPS)
- GET /events/:id/bookings — auth required (MANAGE_OPS)
- GET /orders/:id/qr — auth required (MANAGE_POS)

From frontend/components/ops/Sidebar.tsx (lines 203-216):
```typescript
const operationsNav: NavItem[] = [
  { label: 'Zones', href: '/operations/zones', icon: <MapPin className="size-4" /> },
  // ... 11 more items ending with Procurement ...
];
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Ops feedback page with stats card, rating/date filters, and feedback table</name>
  <files>
    frontend/app/(ops)/operations/feedback/page.tsx,
    frontend/components/ops/operations/feedback/FeedbackRow.tsx,
    frontend/components/ops/operations/feedback/FeedbackStatsCard.tsx,
    frontend/components/ops/operations/feedback/RatingFilterTabs.tsx
  </files>
  <read_first>
    frontend/lib/types/feedback.ts,
    frontend/lib/api-client.ts,
    frontend/app/(ops)/operations/inventory/page.tsx,
    frontend/components/ui/tabs.tsx,
    frontend/components/ui/skeleton.tsx
  </read_first>
  <action>
**1. FeedbackStatsCard — `frontend/components/ops/operations/feedback/FeedbackStatsCard.tsx`:**

Props: `{ stats: FeedbackStats | undefined; isLoading: boolean }`.

- shadcn `Card` + `CardContent`.
- Display average rating as `text-2xl font-semibold` number (1 decimal place) per UI-SPEC visual focal point. Use `text-amber-500` for the rating number.
- Display total feedback count as secondary text.
- Show star icons (filled) next to average — render `Math.round(stats.average_rating)` filled stars.
- Loading: `<Skeleton className="h-8 w-16" />` for rating, `<Skeleton className="h-4 w-24" />` for count.
- Label: "Avg. Rating" per UI-SPEC copywriting.

**2. RatingFilterTabs — `frontend/components/ops/operations/feedback/RatingFilterTabs.tsx`:**

Props: `{ value: string; onChange: (v: string) => void }`.

- Use shadcn `Tabs` with values: "all", "5", "4", "3", "2", "1".
- Tab labels: "All", "5 star", "4 star", "3 star", "2 star", "1 star" (use star unicode or Star icon).

**3. FeedbackRow — `frontend/components/ops/operations/feedback/FeedbackRow.tsx`:**

Props: `{ feedback: Feedback }`.

- Table row with columns per UI-SPEC interaction contract:
  - Rating: render `feedback.rating` filled star icons (read-only display, small `size-3`).
  - Comment: truncate at 80 chars. If longer, show "..." with an expand toggle button that shows full text below.
  - Customer: `feedback.customer_name || 'Anonymous'`.
  - Order: if `feedback.order?.id`, render as `<Link href={"/pos/orders"} className="text-primary underline">` showing truncated order ID. If no order, show "—".
  - Date: formatted with `toLocaleDateString`.
- State: `expanded: boolean` for comment toggle.

**4. Feedback ops page — `frontend/app/(ops)/operations/feedback/page.tsx`:**

`'use client'` page. Title: "Customer Feedback" per UI-SPEC.

State:
- `ratingFilter: string` — "all" or "1"-"5".
- `dateFilter: string` — "all", "today", "week", "month".

Fetch stats: `useQuery({ queryKey: ['feedback-stats'], queryFn: () => apiClient.get<FeedbackStats>('/feedback/stats') })`.

Fetch feedback: `useQuery({ queryKey: ['feedback-list', ratingFilter, dateFilter], queryFn: () => { ... } })`.
- Build query params: `?rating=${ratingFilter !== 'all' ? ratingFilter : ''}` + date params based on dateFilter.
- For "today": `date_from` = today ISO start. For "week": `date_from` = 7 days ago. For "month": `date_from` = 30 days ago.

Layout (matches existing ops page patterns):
- `<BlurFade>` wrapper.
- Header: `<h1 className="text-xl font-semibold">Customer Feedback</h1>`.
- `<FeedbackStatsCard>` at top.
- Filter row: `<RatingFilterTabs>` + date Select (Today / This Week / This Month / All Time) using shadcn `Select`.
- Table: `<table>` with columns: Rating, Comment, Customer, Order, Date. Map feedback to `<FeedbackRow>`.
- Empty state: heading "No feedback yet", body "Feedback submitted via QR codes and links will appear here." per UI-SPEC.
- Error state: "Couldn't load feedback. Refresh the page to try again." per UI-SPEC.
- Loading: table skeleton rows.
- Page width: standard ops `max-w-[1200px]` from layout.

  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - File frontend/app/(ops)/operations/feedback/page.tsx exists and contains "Customer Feedback"
    - grep "FeedbackStatsCard" frontend/app/(ops)/operations/feedback/page.tsx returns a match
    - grep "RatingFilterTabs" frontend/app/(ops)/operations/feedback/page.tsx returns a match
    - File frontend/components/ops/operations/feedback/FeedbackStatsCard.tsx exists and contains "Avg. Rating"
    - File frontend/components/ops/operations/feedback/FeedbackRow.tsx exists and contains "Anonymous"
    - grep "No feedback yet" frontend/app/(ops)/operations/feedback/page.tsx returns a match
    - npx tsc --noEmit succeeds
  </acceptance_criteria>
  <done>
    Ops feedback page at /operations/feedback shows FeedbackStatsCard with average rating (amber, 2xl) and total count. Rating filter tabs (All/5/4/3/2/1) and date filter Select. Table displays feedback rows with star display, truncated comment with expand, customer name, order link, and date. Empty and error states match UI-SPEC copywriting.
  </done>
</task>

<task type="auto">
  <name>Task 2: Ops events CRUD page with booking list + Sidebar update</name>
  <files>
    frontend/app/(ops)/operations/events/page.tsx,
    frontend/components/ops/operations/events/EventForm.tsx,
    frontend/components/ops/operations/events/EventRow.tsx,
    frontend/components/ops/operations/events/BookingListSheet.tsx,
    frontend/components/ops/Sidebar.tsx
  </files>
  <read_first>
    frontend/lib/types/events.ts,
    frontend/lib/api-client.ts,
    frontend/app/(ops)/operations/zones/page.tsx,
    frontend/components/ops/Sidebar.tsx,
    frontend/components/ui/sheet.tsx,
    frontend/components/ui/dialog.tsx,
    frontend/lib/types/zone.ts,
    frontend/lib/types/brand.ts
  </read_first>
  <action>
**1. EventForm — `frontend/components/ops/operations/events/EventForm.tsx`:**

Props: `{ event?: Event; open: boolean; onOpenChange: (open: boolean) => void; onSaved: () => void }`.

- shadcn `Sheet` + `SheetContent` (right side) per UI-SPEC interaction contract and Phase 6 create/edit pattern.
- If `event` prop provided: edit mode (PATCH). If not: create mode (POST).
- Form fields (per D-08): title (Input, required), event_type (Select with options: dining, workshop, pop_up, tasting, other), date (Input type="datetime-local", required), capacity (Input type="number", min 1, required), price (Input type="number", min 0, step 0.01, required), zone_id (Select fetched from `/zones`), brand_id (Select fetched from `/brands`), description (Textarea, optional), image_url (Input, optional).
- Use `react-hook-form` + `zod` for validation. Schema:
  - title: z.string().min(3).max(200)
  - event_type: z.string()
  - date: z.string().min(1, { message: "Date is required" })
  - capacity: z.coerce.number().min(1)
  - price: z.coerce.number().min(0)
  - zone_id: z.string().optional()
  - brand_id: z.string().optional()
  - description: z.string().max(2000).optional()
  - image_url: z.string().optional()
- On submit: POST `/events` (create) or PATCH `/events/${event.id}` (edit). On success, call `onSaved()` to refetch parent, close sheet.
- In edit mode: populate form with event data via `useEffect` when event prop changes (same pattern as ZoneForm/BrandForm per Phase 6 decision).

**2. EventRow — `frontend/components/ops/operations/events/EventRow.tsx`:**

Props: `{ event: Event; onEdit: () => void; onViewBookings: () => void; onDelete: () => void }`.

- Table row with columns: Title, Date (formatted), Type (badge), Capacity fill (e.g., "12 / 40" with thin progress bar `h-1` below per UI-SPEC), Bookings count (button), Actions (edit, delete).
- Capacity fill: `<div className="text-sm">{event.booked_guests ?? 0} / {event.capacity}</div>` + `<div className="h-1 bg-muted rounded-full overflow-hidden"><div className="h-full bg-primary rounded-full" style={{ width: \`${Math.min(100, ((event.booked_guests ?? 0) / event.capacity) * 100)}%\` }} /></div>`.
- "View Bookings" button: calls `onViewBookings()`.
- Edit button: calls `onEdit()`.
- Delete button: calls `onDelete()`.

**3. BookingListSheet — `frontend/components/ops/operations/events/BookingListSheet.tsx`:**

Props: `{ eventId: string | null; eventName: string; open: boolean; onOpenChange: (open: boolean) => void }`.

- shadcn `Sheet` + `SheetContent`.
- Title: "Bookings for [Event Name]" per UI-SPEC.
- Fetch: `useQuery({ queryKey: ['event-bookings', eventId], queryFn: () => apiClient.get<EventBooking[]>('/events/' + eventId + '/bookings'), enabled: !!eventId && open })`.
- Display: list of bookings with customer_name, customer_phone, guests count, booked date.
- Empty state: "No bookings yet for this event." per UI-SPEC.
- Read-only — no CRUD on bookings.

**4. Events ops page — `frontend/app/(ops)/operations/events/page.tsx`:**

`'use client'` page. Title: "Experience Events" per UI-SPEC.

State:
- `formOpen: boolean` — sheet open state.
- `editingEvent: Event | undefined` — event being edited.
- `bookingsEventId: string | null` + `bookingsEventName: string` — booking sheet.
- `deletingEvent: Event | null` — delete confirmation.

Fetch: `useQuery({ queryKey: ['ops-events'], queryFn: () => apiClient.get<Event[]>('/events/all') })`.

Layout:
- Header: title + "Create Event" `<Button>` (right-aligned, primary variant) per UI-SPEC visual focal point.
- Table with EventRow per event.
- Delete confirmation: shadcn `Dialog` with copy: "Delete event: This will permanently remove the event and all its bookings. This cannot be undone." per UI-SPEC. Two buttons: "Keep Event" (secondary) + "Delete Event" (destructive variant). On confirm: DELETE `/events/${event.id}`, refetch, close dialog.
- `<EventForm open={formOpen} event={editingEvent} onOpenChange={setFormOpen} onSaved={refetch} />`
- `<BookingListSheet eventId={bookingsEventId} eventName={bookingsEventName} open={!!bookingsEventId} onOpenChange={(o) => !o && setBookingsEventId(null)} />`
- Empty state: heading "No events created", body "Create an event to start collecting bookings." per UI-SPEC.
- Error state: "Couldn't load events. Refresh the page to try again." per UI-SPEC.
- Wrap in `<BlurFade>`.

**5. Sidebar update — `frontend/components/ops/Sidebar.tsx`:**

Add two entries to the END of the `operationsNav` array (after the Procurement entry at line ~216):

```typescript
{ label: 'Feedback', href: '/operations/feedback', icon: <MessageSquare className="size-4" /> },
{ label: 'Events', href: '/operations/events', icon: <CalendarDays className="size-4" /> },
```

Add imports at the top: `MessageSquare, CalendarDays` from `lucide-react` (add to the existing import block).

  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - File frontend/app/(ops)/operations/events/page.tsx exists and contains "Experience Events" and "Create Event"
    - grep "EventForm" frontend/app/(ops)/operations/events/page.tsx returns a match
    - grep "BookingListSheet" frontend/app/(ops)/operations/events/page.tsx returns a match
    - grep "Delete event" frontend/app/(ops)/operations/events/page.tsx returns a match (delete confirmation dialog)
    - File frontend/components/ops/operations/events/EventForm.tsx exists and contains "Sheet"
    - File frontend/components/ops/operations/events/BookingListSheet.tsx exists and contains "Bookings for"
    - File frontend/components/ops/operations/events/EventRow.tsx exists and contains "h-1" (capacity progress bar)
    - grep "Feedback" frontend/components/ops/Sidebar.tsx returns a match in operationsNav
    - grep "Events" frontend/components/ops/Sidebar.tsx returns a match in operationsNav
    - grep "MessageSquare" frontend/components/ops/Sidebar.tsx returns a match
    - grep "CalendarDays" frontend/components/ops/Sidebar.tsx returns a match
    - npx tsc --noEmit succeeds
  </acceptance_criteria>
  <done>
    Ops events page at /operations/events shows table with capacity progress bars, create/edit via Sheet form (matching Phase 6 pattern), delete with confirmation Dialog, and booking list Sheet. Ops feedback page wired in previous task. Sidebar has Feedback and Events links under Operations. All TypeScript compiles.
  </done>
</task>

</tasks>

<verification>
1. `cd frontend && npx tsc --noEmit` succeeds
2. Both ops pages exist under `frontend/app/(ops)/operations/`
3. All 6 ops components exist under `frontend/components/ops/operations/`
4. Sidebar contains Feedback and Events entries in operationsNav
5. Events page has delete confirmation dialog
6. Booking list Sheet fetches from `/events/:id/bookings`
</verification>

<success_criteria>
- /operations/feedback page: stats card (amber avg rating), rating tabs filter, date filter, feedback table with expand toggle
- /operations/events page: table with capacity bars, create/edit Sheet, delete Dialog, booking list Sheet
- Sidebar: Feedback and Events links under Operations section
- Matches existing ops page patterns (BlurFade wrapper, dark theme, standard layout)
- Zero TypeScript errors
</success_criteria>

<output>
After completion, create `.planning/phases/13-customer-experience/13-03-SUMMARY.md`
</output>
