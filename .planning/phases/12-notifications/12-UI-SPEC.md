---
phase: 12
slug: notifications
status: draft
shadcn_initialized: true
preset: base-nova / neutral
created: 2026-03-22
---

# Phase 12 — UI Design Contract

> Visual and interaction contract for frontend phases. Generated by gsd-ui-researcher, verified by gsd-ui-checker.

---

## Design System

| Property | Value |
|----------|-------|
| Tool | shadcn (base-nova style) |
| Preset | style: base-nova, baseColor: neutral, cssVariables: true |
| Component library | base-ui (via shadcn base-nova) |
| Icon library | lucide-react (size-4 = 16px standard, size-5 = 20px for bell) |
| Font | Geist Sans (--font-geist-sans via next/font) |

Source: `frontend/components.json` + `frontend/app/globals.css`

---

## Spacing Scale

Declared values (must be multiples of 4):

| Token | Value | Usage |
|-------|-------|-------|
| xs | 4px | Icon gaps, inline badge padding |
| sm | 8px | Notification item internal gap, compact element spacing |
| md | 16px | Default element spacing, card padding |
| lg | 24px | Section padding, panel header padding |
| xl | 32px | Layout gaps |
| 2xl | 48px | Major section breaks |
| 3xl | 64px | Page-level spacing |

Exceptions:
- Bell icon touch target: 44px minimum (accessibility). Use `size-11` or `min-w-[44px] min-h-[44px]` wrapper with `size-5` icon inside.
- Notification unread badge: 20px height (`h-5`) matching existing Badge component default.
- Popover panel width: 320px (`w-80`) — wider than default `w-72` to accommodate notification body text.

---

## Typography

Established pattern across all ops pages (source: 12 existing page components):

| Role | Size | Weight | Line Height | Usage |
|------|------|--------|-------------|-------|
| Body | 14px (text-sm) | 400 (font-normal) | 1.5 | Notification body text, timestamps, empty state copy |
| Label | 13px (text-[13px]) | 500 (font-medium) | 1.4 | Notification type label, section nav labels |
| Heading | 20px (text-[20px]) | 700 (font-bold) | 1.2 | Page heading: "Notifications" |
| Display | 28px (text-[28px]) | 600 (font-semibold) | 1.2 | Not used on this page — reserved for other ops pages |

Note: Display size (28px font-semibold) is the dominant page-heading pattern in ops pages (decisions, brands, zones, etc.). The Notifications page uses the board-page pattern (20px font-bold) consistent with the `/boards/*` pages which also show feeds/lists.

Specific sizes for notification components:
- Notification title in panel card: `text-sm font-medium` (14px/500)
- Notification body/subtitle: `text-sm text-muted-foreground` (14px/400)
- Timestamp: `text-xs text-muted-foreground` (12px/400)
- Unread count badge: `text-xs font-medium` (12px/500)
- Panel "Mark all as read" action: `text-sm font-medium` (14px/500)
- Notification type pill: `text-[11px] font-medium uppercase tracking-wider` matching section label pattern from Sidebar

---

## Color

| Role | Value | Usage |
|------|-------|-------|
| Dominant (60%) | `--background` oklch(1 0 0) light / oklch(0.145 0 0) dark | Page background, notification panel background |
| Secondary (30%) | `--card` oklch(1 0 0) light / oklch(0.205 0 0) dark | Notification item card surfaces, popover bg |
| Accent (10%) | `--primary` oklch(0.205 0 0) light / oklch(0.922 0 0) dark | Bell badge unread dot, active/read CTA button fill |
| Destructive | `--destructive` oklch(0.577 0.245 27.325) light / oklch(0.704 0.191 22.216) dark | Not used in this phase — no destructive actions |

Accent reserved for:
1. Unread count badge on bell icon (red dot or primary-colored count bubble)
2. "Mark all as read" button (primary fill on hover only — ghost/outline at rest)
3. Active notification highlight ring on hover

Semantic notification type colors (inline only — not full-surface accent):
- Task deadline/blocker: `text-amber-400 bg-amber-950 border-amber-500/20` — matches existing Approvals badge pattern in Sidebar
- Low stock alert: `text-orange-400 bg-orange-950 border-orange-500/20`
- Order/kitchen/delivery: `text-blue-400 bg-blue-950 border-blue-500/20`
- Approval pending: `text-amber-400 bg-amber-950 border-amber-500/20`

Unread notification item: `bg-primary/5` background tint on the card row. Read items: `bg-transparent`.

Source: `frontend/app/globals.css` CSS variables + existing badge pattern in `frontend/components/ops/Sidebar.tsx` (line 153)

---

## Component Inventory

All components are already installed. No new shadcn additions required.

| Component | Source | Usage in Phase 12 |
|-----------|--------|-------------------|
| `Badge` | `@/components/ui/badge` | Unread count badge on bell, notification type pill |
| `Popover` / `PopoverContent` | `@/components/ui/popover` | Notification dropdown panel (w-80, side=bottom, align=end) |
| `Button` | `@/components/ui/button` | Bell icon button trigger, "Mark all as read", "View all" |
| `Tabs` / `TabsList` / `TabsTrigger` | `@/components/ui/tabs` | Full notifications page filters (All / Unread / by type) |
| `BlurFade` | `@/components/ui/blur-fade` | Page entry animation on /notifications page |
| `Skeleton` | `@/components/ui/skeleton` | Loading state in notification panel (3 skeleton rows) |
| `ScrollArea` | `@/components/ui/scroll-area` | Scrollable panel list (max-h-[480px]) |
| `Separator` | `@/components/ui/separator` | Divider between panel header and notification list |
| `AnimatedList` | `@/components/ui/animated-list` | Notification list on /notifications page |
| Lucide `Bell` | lucide-react | Bell trigger icon in Sidebar header (size-5) |
| Lucide `BellOff` | lucide-react | Empty state icon in panel when 0 notifications |
| Lucide `Check` | lucide-react | Mark as read action per item |

No new component installations required. No third-party registry blocks needed.

---

## Layout Specifications

### 1. Bell Icon in Sidebar Header

The sidebar header currently renders:
```
<div className="px-4 py-4 border-b">
  <span className="text-sm font-semibold tracking-tight">Konma Xperience</span>
</div>
```

Modify to flex row:
```
<div className="px-4 py-3 border-b flex items-center justify-between">
  <span className="text-sm font-semibold tracking-tight">Konma Xperience</span>
  <NotificationBell />  {/* new component */}
</div>
```

`NotificationBell` renders:
- `PopoverTrigger` wrapping a ghost `Button` with `size-icon` variant, `size-8` dimensions
- `Bell` icon at `size-4` inside
- Unread count badge: absolute-positioned, top-0 right-0, `h-4 w-4 rounded-full bg-destructive text-destructive-foreground text-[10px] font-medium` for red dot style. Hidden when count = 0.
- Max displayed count: 9+

### 2. Notification Popover Panel

- `PopoverContent` with `side="bottom"` `align="end"` `sideOffset={8}` `className="w-80 p-0"`
- Panel header: `flex items-center justify-between px-4 py-3 border-b`
  - Left: "Notifications" label at `text-sm font-semibold`
  - Right: "Mark all as read" ghost button at `text-xs font-medium text-muted-foreground hover:text-foreground` — disabled when unread count = 0
- Notification list: `ScrollArea` with `max-h-[480px]`
  - Each item: `NotificationItem` component
- Panel footer: `border-t px-4 py-2`
  - "View all notifications" link → `/notifications` at `text-sm font-medium`

### 3. NotificationItem Component

```
<div className={cn(
  "flex items-start gap-3 px-4 py-3 cursor-pointer hover:bg-muted transition-colors",
  !item.is_read && "bg-primary/5"
)}>
  <div className="mt-0.5 shrink-0">
    <TypeIcon />  {/* lucide icon, size-4, colored by type */}
  </div>
  <div className="flex-1 min-w-0">
    <p className="text-sm font-medium truncate">{item.title}</p>
    <p className="text-sm text-muted-foreground line-clamp-2">{item.body}</p>
    <p className="text-xs text-muted-foreground mt-1">{relativeTime}</p>
  </div>
  {!item.is_read && (
    <div className="mt-1 size-2 rounded-full bg-primary shrink-0" />
  )}
</div>
```

Type icons (lucide, all size-4):
- Task due (NOTF-01): `Clock` — `text-amber-400`
- Task blocked (NOTF-02): `ShieldAlert` — `text-amber-400`
- Approval pending (NOTF-03): `ClipboardCheck` — `text-amber-400`
- Low stock (NOTF-04): `PackageX` — `text-orange-400`
- New order / kitchen (NOTF-05): `ChefHat` — `text-blue-400`
- Order ready / POS (NOTF-06): `Bell` — `text-green-400`
- Delivery update (NOTF-07): `Truck` — `text-blue-400`

### 4. Full /notifications Page

URL: `/notifications`
Layout: Standard ops page layout with sidebar (existing layout.tsx).

Structure:
```
<main className="flex-1 overflow-auto p-6">
  <BlurFade>
    <div className="max-w-2xl mx-auto">
      <div className="flex items-center justify-between mb-6">
        <h1 className="text-[20px] font-bold">Notifications</h1>
        <Button variant="outline" size="sm">Mark all as read</Button>
      </div>
      <Tabs defaultValue="all">
        <TabsList> {/* All | Unread | Tasks | Approvals | Ops */} </TabsList>
        <TabsContent>
          {/* NotificationItem list, full width, no truncation */}
        </TabsContent>
      </Tabs>
    </div>
  </BlurFade>
</main>
```

Pagination: "Load more" button at bottom (not infinite scroll) — 20 items per page.

---

## Interaction Contracts

### Bell Polling
- `GET /notifications/unread-count` every 30 seconds via `refetchInterval: 30_000`
- React Query key: `['notifications', 'unread-count']`
- Panel list: `GET /notifications?limit=20` fetched `onOpenChange` when panel opens
- React Query key: `['notifications', 'recent']`

### Mark as Read
- Clicking any notification item: fires `PATCH /notifications/:id/read` optimistically (mark is_read=true in cache immediately, rollback on error)
- "Mark all as read": fires `POST /notifications/read-all`, invalidates `['notifications']` and `['notifications', 'unread-count']`
- Navigation: after marking read, navigate to `item.link_url` using Next.js `router.push()`

### Full Page Filters
- Tabs: All / Unread / Tasks / Approvals / Operations
- Filter maps to `type` query param: tasks = `task_due,task_blocked`, approvals = `approval_pending`, ops = `low_stock,new_order,order_ready,delivery_update`
- Date range filter: two date inputs in a row, `text-sm`, 32px height inputs
- No destructive actions on this page

### Sonner Toast (supplementary)
- On new notification arriving during polling (unread count increases): do NOT show a Sonner toast — bell badge update is sufficient.
- Exception: Order ready notification (NOTF-06) shows a Sonner toast with `sonner.success("Order #${id} is ready")` because POS staff may be looking at a different page. Duration: 5000ms.

---

## Copywriting Contract

| Element | Copy |
|---------|------|
| Primary CTA (panel) | "Mark all as read" |
| Primary CTA (page) | "Mark all as read" |
| Panel header title | "Notifications" |
| Empty state heading (panel) | "You're all caught up" |
| Empty state body (panel) | "No new notifications. Check back when something needs your attention." |
| Empty state heading (page, all tab) | "No notifications yet" |
| Empty state body (page, all tab) | "Alerts for tasks, approvals, stock levels, and orders will appear here." |
| Empty state heading (page, unread tab) | "All caught up" |
| Empty state body (page, unread tab) | "You have no unread notifications." |
| Notification titles by type (7 types) | See table below |
| Error state | "Could not load notifications. Refresh the page to try again." |
| Load more button | "Load more" |
| View all link | "View all notifications" |

### Notification Title Templates (stored in DB, generated by worker)

| Type | Title Template | Body Template |
|------|---------------|---------------|
| NOTF-01 task_due | "Task due in {{hours}}h" | "{{taskName}} in {{questName}} is due soon." |
| NOTF-02 task_blocked | "Task blocked" | "{{taskName}} is blocked: {{blockerReason}}" |
| NOTF-03 approval_pending | "Approval waiting {{hours}}h" | "{{taskName}} evidence has been pending approval for over 24 hours." |
| NOTF-04 low_stock | "Low stock: {{ingredientName}}" | "{{ingredientName}} is at {{currentQty}} {{unit}}, below minimum level of {{minQty}} {{unit}}." |
| NOTF-05 new_order | "New order #{{orderId}}" | "{{channel}} order placed. {{itemCount}} item(s) — {{totalAmount}}." |
| NOTF-06 order_ready | "Order #{{orderId}} ready" | "{{channel}} order is ready for {{action}}." (action: "serving" / "pickup" / "dispatch") |
| NOTF-07 delivery_update | "Delivery {{status}}" | "Order #{{orderId}} to {{deliveryAddress}}: {{status}}." |

No destructive actions exist in this phase. No confirmation dialogs required.

---

## States Inventory

Every UI surface must handle these states:

| Surface | Loading | Empty | Error | Populated |
|---------|---------|-------|-------|-----------|
| Bell unread count | No spinner (silent polling) | Badge hidden | Badge hidden | Badge visible with count |
| Notification panel | 3 Skeleton rows | BellOff icon + "You're all caught up" | "Could not load" inline text | NotificationItem list |
| /notifications page | Skeleton list (5 rows) | Empty state with BellOff icon | Alert component with retry | NotificationItem list + pagination |
| Mark all read button | Disabled + loading spinner | Disabled (grayed) | Toast error (Sonner) | Normal |

---

## Accessibility

- Bell button: `aria-label="Notifications"`, `aria-haspopup="true"`, `aria-expanded={open}`
- Unread badge: `aria-label={`${count} unread notifications`}` on the badge span
- Notification panel: `role="region"` `aria-label="Recent notifications"`
- Notification items: keyboard navigable, `Enter`/`Space` triggers navigation + mark-read
- Empty state BellOff icon: `aria-hidden="true"` (decorative)
- All type icons in NotificationItem: `aria-hidden="true"` (decorative — title text is sufficient)

---

## Registry Safety

| Registry | Blocks Used | Safety Gate |
|----------|-------------|-------------|
| shadcn official | badge, popover, button, tabs, scroll-area, separator, skeleton | not required |
| @magicui | animated-list, blur-fade (already installed) | view passed — already in codebase |
| None (third-party) | No new third-party blocks | not applicable |

No new registry additions. All required components are already installed in `frontend/components/ui/`.

---

## Checker Sign-Off

- [ ] Dimension 1 Copywriting: PASS
- [ ] Dimension 2 Visuals: PASS
- [ ] Dimension 3 Color: PASS
- [ ] Dimension 4 Typography: PASS
- [ ] Dimension 5 Spacing: PASS
- [ ] Dimension 6 Registry Safety: PASS

**Approval:** pending
