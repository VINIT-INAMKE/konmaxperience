---
phase: 09-kitchen-prep
plan: 05
type: execute
wave: 3
depends_on: [09-02, 09-03]
files_modified:
  - frontend/app/(ops)/operations/kitchen/kds/page.tsx
  - frontend/components/ops/kitchen/kds/KdsBoard.tsx
  - frontend/components/ops/kitchen/kds/KdsZoneColumn.tsx
  - frontend/components/ops/kitchen/kds/KdsOrderCard.tsx
  - frontend/components/ops/kitchen/kds/KdsOrderItem.tsx
  - frontend/components/ops/kitchen/kds/KdsElapsedTimer.tsx
  - frontend/components/ops/kitchen/kds/KdsItemStatusBadge.tsx
  - frontend/components/ops/kitchen/kds/KdsExitButton.tsx
  - frontend/components/ops/kitchen/kds/KdsMetricsBar.tsx
  - frontend/app/(ops)/operations/kitchen/waste/page.tsx
  - frontend/components/ops/kitchen/waste/WasteLogForm.tsx
  - frontend/components/ops/kitchen/waste/WasteLogRow.tsx
  - frontend/components/ops/kitchen/waste/WasteReasonBadge.tsx
autonomous: true
requirements: [KITCHEN-02, KITCHEN-04, KITCHEN-05]

must_haves:
  truths:
    - "KDS displays a full-screen view with orders grouped by zone columns, polled every 5 seconds"
    - "Cook can tap order items to advance status (pending to preparing to ready) with Sonner toast"
    - "Elapsed timer updates every second with green/amber/red color thresholds"
    - "New order cards flash with BorderBeam for 3 seconds, completed orders fade out after 30 seconds"
    - "Waste Log page shows history table and form to log new waste entries"
    - "KDS metrics bar shows orders in queue, items completed today, and waste percentage (per D-15 / KITCHEN-05)"
  artifacts:
    - path: "frontend/app/(ops)/operations/kitchen/kds/page.tsx"
      provides: "Full-screen KDS page with CSS fixed overlay"
      contains: "fixed inset-0 z-50"
    - path: "frontend/components/ops/kitchen/kds/KdsBoard.tsx"
      provides: "Zone columns grid with 5s polling"
      contains: "refetchInterval"
    - path: "frontend/components/ops/kitchen/kds/KdsElapsedTimer.tsx"
      provides: "Per-second timer with green/amber/red colors"
      contains: "setInterval"
    - path: "frontend/components/ops/kitchen/kds/KdsMetricsBar.tsx"
      provides: "Metrics bar showing orders, completed items, and waste percentage"
      contains: "waste_percentage"
    - path: "frontend/app/(ops)/operations/kitchen/waste/page.tsx"
      provides: "Waste Log page with history and form"
      contains: "WasteLogForm"
  key_links:
    - from: "KdsBoard.tsx"
      to: "/kitchen/kds"
      via: "useQuery with refetchInterval: 5000"
      pattern: "refetchInterval.*5000"
    - from: "KdsOrderItem.tsx"
      to: "/kitchen/kds/items/:id/status"
      via: "PATCH on tap to advance status"
      pattern: "kds/items"
    - from: "KdsMetricsBar.tsx"
      to: "/kitchen/metrics"
      via: "useQuery fetching metrics including waste_percentage"
      pattern: "kitchen/metrics"
    - from: "WasteLogForm.tsx"
      to: "/kitchen/waste"
      via: "POST to create waste log entry"
      pattern: "kitchen/waste"
---

<objective>
Build the KDS full-screen display and Waste Log page — the two remaining Phase 9 frontend pages.

Purpose: KDS (D-04 through D-10) is the kitchen's primary operational interface. The full-screen layout with zone columns, elapsed timers, and status tapping enables efficient kitchen flow. Waste Log (D-12) tracks cost impact of spoilage and errors.
Output: Full-screen KDS at /operations/kitchen/kds with 5s polling and touch-optimized interface. Waste Log at /operations/kitchen/waste with form and history table.
</objective>

<execution_context>
@D:/aditee/konmaxperience/.claude/get-shit-done/workflows/execute-plan.md
@D:/aditee/konmaxperience/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-kitchen-prep/09-CONTEXT.md
@.planning/phases/09-kitchen-prep/09-UI-SPEC.md

@.planning/phases/09-kitchen-prep/09-01-SUMMARY.md
@.planning/phases/09-kitchen-prep/09-02-SUMMARY.md
@.planning/phases/09-kitchen-prep/09-03-SUMMARY.md

<interfaces>
<!-- Frontend types from Plan 01 -->
From frontend/lib/types/kds.ts:
```typescript
export interface KdsOrderItem { id: string; status: OrderItemStatus; menu_item_id: string; menu_item_name: string; quantity: number; item_notes: string | null; }
export interface KdsOrder { id: string; customer_name: string | null; created_at: string; status: OrderStatus; items: KdsOrderItem[]; zone_id: string; }
export interface KdsZoneData { zone_id: string; zone_name: string; orders: KdsOrder[]; }
export interface KitchenMetrics { orders_in_queue: number; items_completed_today: number; active_prep_batches: number; waste_today_cost: number; waste_percentage: number; average_prep_time_minutes: number | null; }
export type OrderItemStatus = 'pending' | 'preparing' | 'ready';
```

From frontend/lib/types/kitchen.ts:
```typescript
export interface WasteLog { id: string; waste_type: WasteType; ingredient_id: string | null; prep_batch_id: string | null; quantity: number; unit: string; reason: WasteReason; reason_notes: string | null; cost_impact: number; logged_by: string | null; zone_id: string; created_at: string; ingredient?: ...; prep_batch?: ...; zone?: ...; creator?: ... | null; }
export type WasteReason = 'spoilage' | 'over_prep' | 'cooking_error' | 'expired' | 'other';
export const WASTE_REASONS: WasteReason[];
export const WASTE_REASON_LABELS: Record<WasteReason, string>;
export const WASTE_REASON_BADGE_CLASSES: Record<WasteReason, string>;
export const WASTE_TYPE_LABELS: Record<WasteType, string>;
```

From frontend/lib/api-client.ts:
```typescript
export const apiClient = { get, post, patch, delete };
```

KDS UI-SPEC Layout:
```
Full viewport (100vw x 100vh), dark bg oklch(0.10 0 0)
  Fixed top bar: "Kitchen Display" + metrics + Exit button
  Scrollable zone columns grid (CSS grid, auto-fit minmax 280px)
    KdsZoneColumn -> KdsOrderCard -> KdsOrderItem
  No sidebar (CSS fixed overlay z-50)
```

KDS Timer Colors (from UI-SPEC):
- Green: oklch(0.627 0.194 142.495) — elapsed < 10 min
- Amber: oklch(0.769 0.188 70.08) — elapsed 10-20 min
- Red: var(--destructive) — elapsed > 20 min

Waste Log Layout (from UI-SPEC):
```
BlurFade wrapper
  lg:grid-cols-3 gap-6
    Left (col-span-2): waste history Table + AnimatedList
    Right (col-span-1): WasteLogForm Card
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: KDS full-screen page with zone columns, polling, timers, and status tapping</name>
  <files>frontend/app/(ops)/operations/kitchen/kds/page.tsx, frontend/components/ops/kitchen/kds/KdsBoard.tsx, frontend/components/ops/kitchen/kds/KdsZoneColumn.tsx, frontend/components/ops/kitchen/kds/KdsOrderCard.tsx, frontend/components/ops/kitchen/kds/KdsOrderItem.tsx, frontend/components/ops/kitchen/kds/KdsElapsedTimer.tsx, frontend/components/ops/kitchen/kds/KdsItemStatusBadge.tsx, frontend/components/ops/kitchen/kds/KdsExitButton.tsx, frontend/components/ops/kitchen/kds/KdsMetricsBar.tsx</files>
  <read_first>
    - frontend/app/(ops)/layout.tsx
    - frontend/lib/api-client.ts
    - frontend/lib/types/kds.ts
    - frontend/components/ui/border-beam.tsx
    - frontend/components/ui/number-ticker.tsx
    - .planning/phases/09-kitchen-prep/09-UI-SPEC.md
  </read_first>
  <action>
**1. Create KdsElapsedTimer** (`frontend/components/ops/kitchen/kds/KdsElapsedTimer.tsx`):
- Props: `createdAt: string`
- useState for elapsed seconds, useEffect with setInterval(tick, 1000) per Research Pattern 6
- Calculate minutes from elapsed seconds: `Math.floor(elapsed / 60)`
- Display format: "Xm Ys" for < 1 hour, "Xh Ym" for >= 1 hour
- Color classes per UI-SPEC KDS Timer Colors:
  - \< 10 min: `text-[oklch(0.627_0.194_142.495)]` (green)
  - 10-20 min: `text-[oklch(0.769_0.188_70.08)]` (amber)
  - \> 20 min: `text-destructive` (red)
- Font per KDS typography: 24px bold, line-height 1.0 -> `text-2xl font-bold leading-none`
- Cleanup: return () => clearInterval(id) in useEffect

**2. Create KdsItemStatusBadge** (`frontend/components/ops/kitchen/kds/KdsItemStatusBadge.tsx`):
- Props: `status: OrderItemStatus`
- Color map: pending = muted, preparing = amber, ready = green
- Small Badge component

**3. Create KdsOrderItem** (`frontend/components/ops/kitchen/kds/KdsOrderItem.tsx`):
- Props: `item: KdsOrderItem`, `onStatusAdvance: (itemId: string, newStatus: OrderItemStatus) => void`
- Per UI-SPEC: min 48px height for touch targets
- Display: item name (18px regular), quantity, status badge, item_notes (if any)
- Tap handler: onClick calls onStatusAdvance with next status:
  - pending -> preparing
  - preparing -> ready
  - ready -> no action (already final)
- Visual feedback: cursor-pointer for non-ready items, opacity-50 + cursor-default for ready items

**4. Create KdsOrderCard** (`frontend/components/ops/kitchen/kds/KdsOrderCard.tsx`):
- Props: `order: KdsOrder`, `isNew: boolean`, `isComplete: boolean`, `onStatusAdvance: fn`
- Per KDS typography: Order # in 28px bold, customer label 14px regular
- KdsElapsedTimer for elapsed time
- List of KdsOrderItem components (min 48px each)
- NEW order animation (D-08): When `isNew` is true, render BorderBeam overlay for 3 seconds. Use useState + useEffect with setTimeout to remove after 3s.
- COMPLETE fade-out (D-10): When `isComplete` is true (all items ready), start 30s countdown. After 30s, apply `opacity-0 transition-opacity duration-1000`. Use useState + setTimeout.
- Card styling per UI-SPEC: bg-[oklch(0.205_0_0)] (var(--card) dark), rounded-lg, p-4

**5. Create KdsZoneColumn** (`frontend/components/ops/kitchen/kds/KdsZoneColumn.tsx`):
- Props: `zone: KdsZoneData`, `newOrderIds: Set<string>`, `onStatusAdvance: fn`
- Zone name header: 18px bold per KDS typography, sticky top
- Scrollable order card list (overflow-y-auto, max-h remaining viewport)
- Minimum column width 280px per UI-SPEC

**6. Create KdsExitButton** (`frontend/components/ops/kitchen/kds/KdsExitButton.tsx`):
- Fixed top-4 right-4 position
- Uses next/navigation router.back() or router.push('/operations/kitchen/prep-batches')
- X icon (Lucide) or "Exit" text button
- z-index above KDS content

**7. Create KdsMetricsBar** (`frontend/components/ops/kitchen/kds/KdsMetricsBar.tsx`):
- Props: none (fetches own data)
- Fetches GET /kitchen/metrics
- Displays per D-15 / KITCHEN-05:
  - "Orders: {n}" using NumberTicker
  - "Completed Today: {n}" using NumberTicker
  - "Waste: {waste_percentage}%" — show the waste_percentage from KitchenMetrics. Format to 1 decimal place. Color: green if < 5%, amber if 5-10%, red if > 10%.
- Compact horizontal bar layout for top of KDS

**8. Create KdsBoard** (`frontend/components/ops/kitchen/kds/KdsBoard.tsx`):
- useQuery to fetch GET /kitchen/kds with `refetchInterval: 5000` and `refetchIntervalInBackground: true` per D-09
- NO loading spinner on background polls — only show initial loading state on first load
- New order detection (per Research Pitfall 4): maintain `seenOrderIds` in useRef(Set). On each poll result, compute newIds = current order IDs minus seen IDs. Pass newOrderIds to zone columns. After 3s, update seenOrderIds to include all current IDs.
- Error state: show fixed banner "Connection issue — retrying..." per copywriting contract when isError is true
- Empty state per copywriting: "No orders in queue" heading + "Orders placed on POS will appear here automatically." body
- CSS grid for zone columns: `grid grid-cols-[repeat(auto-fit,minmax(280px,1fr))]` or similar auto-fit pattern
- Handle onStatusAdvance: mutation to PATCH /kitchen/kds/items/:id/status, on success Sonner toast per D-06: "{item_name} — {new status}" (e.g. "Chicken Tikka — preparing"), invalidate kds-orders query

**9. Create KDS page** (`frontend/app/(ops)/operations/kitchen/kds/page.tsx`):
- Full-screen overlay per Research Pattern 5 and UI-SPEC:
```jsx
<div className="fixed inset-0 z-50 bg-[oklch(0.10_0_0)] overflow-hidden flex flex-col">
  {/* Top bar: title + metrics + exit */}
  <div className="flex items-center justify-between px-6 py-4 border-b border-white/10">
    <h1 className="text-xl font-bold text-white">Kitchen Display</h1>
    <KdsMetricsBar />
    <KdsExitButton />
  </div>
  {/* Zone columns */}
  <div className="flex-1 overflow-y-auto p-4">
    <KdsBoard />
  </div>
</div>
```
- No sidebar visible (fixed overlay covers everything)
- All text white/light on dark background for high contrast

**10. Run `cd frontend && npx tsc --noEmit`.**
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "fixed inset-0 z-50" frontend/app/(ops)/operations/kitchen/kds/page.tsx
    - grep -q "refetchInterval" frontend/components/ops/kitchen/kds/KdsBoard.tsx
    - grep -q "5000" frontend/components/ops/kitchen/kds/KdsBoard.tsx
    - grep -q "setInterval" frontend/components/ops/kitchen/kds/KdsElapsedTimer.tsx
    - grep -q "oklch(0.627" frontend/components/ops/kitchen/kds/KdsElapsedTimer.tsx
    - grep -q "BorderBeam" frontend/components/ops/kitchen/kds/KdsOrderCard.tsx
    - grep -q "opacity-0" frontend/components/ops/kitchen/kds/KdsOrderCard.tsx
    - grep -q "onStatusAdvance" frontend/components/ops/kitchen/kds/KdsOrderItem.tsx
    - grep -q "seenOrderIds\|seenIds\|seen" frontend/components/ops/kitchen/kds/KdsBoard.tsx
    - grep -q "NumberTicker" frontend/components/ops/kitchen/kds/KdsMetricsBar.tsx
    - grep -q "waste_percentage" frontend/components/ops/kitchen/kds/KdsMetricsBar.tsx
    - grep -q "Kitchen Display" frontend/app/(ops)/operations/kitchen/kds/page.tsx
    - grep -q "KdsExitButton" frontend/components/ops/kitchen/kds/KdsExitButton.tsx
  </acceptance_criteria>
  <done>Full-screen KDS page with fixed overlay (z-50, dark background), zone columns grid with 5s polling, elapsed timer with green/amber/red thresholds, tap-to-advance item status with Sonner toast, BorderBeam on new orders (3s), fade-out on complete (30s), metrics bar with NumberTicker showing orders + completed + waste percentage (per D-15), exit button. Empty state matches copywriting. tsc passes.</done>
</task>

<task type="auto">
  <name>Task 2: Waste Log page with history table and logging form</name>
  <files>frontend/app/(ops)/operations/kitchen/waste/page.tsx, frontend/components/ops/kitchen/waste/WasteLogForm.tsx, frontend/components/ops/kitchen/waste/WasteLogRow.tsx, frontend/components/ops/kitchen/waste/WasteReasonBadge.tsx</files>
  <read_first>
    - frontend/lib/types/kitchen.ts
    - frontend/lib/api-client.ts
    - frontend/app/(ops)/operations/procurement/page.tsx
    - frontend/components/ops/operations/inventory/StockAdjustmentSheet.tsx
    - .planning/phases/09-kitchen-prep/09-UI-SPEC.md
  </read_first>
  <action>
**1. Create WasteReasonBadge** (`frontend/components/ops/kitchen/waste/WasteReasonBadge.tsx`):
- Props: `reason: WasteReason`
- Render Badge with WASTE_REASON_BADGE_CLASSES[reason] and WASTE_REASON_LABELS[reason]

**2. Create WasteLogRow** (`frontend/components/ops/kitchen/waste/WasteLogRow.tsx`):
- Props: `entry: WasteLog`
- Table row columns: Date (formatted created_at), Type (WASTE_TYPE_LABELS[waste_type]), Item (ingredient name or prep_batch recipe name), Quantity + Unit, Reason (WasteReasonBadge), Cost Impact (formatted currency with 2 decimal places)
- Logged by: show creator name or "System" when logged_by is null (system-generated expiry entries)

**3. Create WasteLogForm** (`frontend/components/ops/kitchen/waste/WasteLogForm.tsx`):
- Props: `onSuccess: () => void`
- Per D-12 and UI-SPEC layout: Card wrapper
- Fields:
  - waste_type: Select with options ingredient / prep_batch (using WASTE_TYPE_LABELS)
  - Item: dynamic Select based on waste_type
    - If ingredient: fetch GET /ingredients, show name list
    - If prep_batch: fetch GET /kitchen/prep-batches?status=active, show recipe name + qty remaining
  - quantity: Input type number
  - unit: Input or Select (show the item's base_unit or batch unit as hint/default)
  - reason: Select with WASTE_REASONS options using WASTE_REASON_LABELS
  - reason_notes: Textarea, optional
  - zone_id: Select with zones from GET /zones
- Submit: POST /kitchen/waste with form data. On success: Sonner toast success, call onSuccess (invalidates query), reset form. On error: Sonner toast per copywriting "Failed to log waste. Try again."
- "Log Waste" submit button (primary CTA per copywriting)
- After successful submission, show the auto-calculated cost_impact from the response (read-only display below form per UI-SPEC)

**4. Create Waste Log page** (`frontend/app/(ops)/operations/kitchen/waste/page.tsx`):
- BlurFade wrapper
- h1: "Waste Log" (text-xl font-semibold)
- Two-column grid per UI-SPEC: `lg:grid-cols-3 gap-6`
  - Left (lg:col-span-2): waste history table
    - useQuery to fetch GET /kitchen/waste
    - Table with columns: Date, Type, Item, Qty, Reason, Cost Impact, Logged By
    - Map entries to WasteLogRow components
    - AnimatedList with delay=150ms per established Phase 8 pattern
    - Empty state per copywriting: "No waste logged today" / "Record waste from spoilage, over-prep, or cooking errors to track cost impact."
  - Right (lg:col-span-1): WasteLogForm in Card
    - Pass onSuccess that invalidates 'waste-log' query key

**5. Run `cd frontend && npx tsc --noEmit`.**
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "WasteLogForm" frontend/app/(ops)/operations/kitchen/waste/page.tsx
    - grep -q "WasteLogRow" frontend/app/(ops)/operations/kitchen/waste/page.tsx
    - grep -q "kitchen/waste" frontend/components/ops/kitchen/waste/WasteLogForm.tsx
    - grep -q "WasteReasonBadge" frontend/components/ops/kitchen/waste/WasteLogRow.tsx
    - grep -q "WASTE_REASON_BADGE_CLASSES" frontend/components/ops/kitchen/waste/WasteReasonBadge.tsx
    - grep -q "Log Waste" frontend/components/ops/kitchen/waste/WasteLogForm.tsx
    - grep -q "System" frontend/components/ops/kitchen/waste/WasteLogRow.tsx
    - grep -q "grid-cols-3" frontend/app/(ops)/operations/kitchen/waste/page.tsx
    - grep -q "AnimatedList" frontend/app/(ops)/operations/kitchen/waste/page.tsx
    - grep -q "BlurFade" frontend/app/(ops)/operations/kitchen/waste/page.tsx
  </acceptance_criteria>
  <done>Waste Log page with two-column layout: history table (AnimatedList, WasteLogRow with WasteReasonBadge, system/user attribution) and WasteLogForm (type-dependent item select, auto cost_impact display, Sonner toasts). Empty state matches copywriting. tsc passes.</done>
</task>

</tasks>

<verification>
- `cd frontend && npx tsc --noEmit` passes
- KDS page at /operations/kitchen/kds renders full-screen overlay with zone columns
- KDS polls every 5 seconds with React Query refetchInterval
- Elapsed timer shows green/amber/red color transitions
- Item tap advances status with Sonner toast
- New orders show BorderBeam for 3 seconds
- Completed orders fade out after 30 seconds
- KDS metrics bar shows orders in queue, items completed today, and waste percentage
- Waste Log page at /operations/kitchen/waste shows history + form in grid layout
</verification>

<success_criteria>
- KDS full-screen renders over sidebar with oklch(0.10 0 0) dark background
- Zone columns use CSS grid with min 280px per column
- Elapsed timer updates every second with correct color thresholds (10min green->amber, 20min amber->red)
- Order item status advances on tap: pending->preparing->ready with Sonner toast
- New order detection tracks seen IDs and only flashes new orders with BorderBeam
- Complete orders fade out with opacity transition after 30 seconds
- KDS metrics bar shows orders in queue, items completed today, and waste percentage (per D-15 / KITCHEN-05)
- Waste Log form dynamically switches item options based on waste type
- "System" shown for auto-generated expiry waste entries (null logged_by)
- All copy matches the UI-SPEC copywriting contract
</success_criteria>

<output>
After completion, create `.planning/phases/09-kitchen-prep/09-05-SUMMARY.md`
</output>
