---
phase: 07-recipe-ingredient-management
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/prisma/schema.prisma
  - backend/prisma/seed.ts
  - backend/prisma/migrations/20260321_phase_7/migration.sql
  - frontend/lib/types/recipe.ts
  - frontend/lib/types/ingredient.ts
  - frontend/lib/types/vendor.ts
  - frontend/lib/types/menu.ts
  - frontend/lib/types/index.ts
autonomous: true
requirements:
  - RECIPE-01
  - RECIPE-02
  - RECIPE-03
  - RECIPE-04
  - RECIPE-05
  - RECIPE-07

must_haves:
  truths:
    - "9 new Prisma models exist and migration runs cleanly"
    - "UnitConversion table seeded with 6 conversion pairs"
    - "Frontend types match Prisma schema exactly"
  artifacts:
    - path: "backend/prisma/schema.prisma"
      provides: "Recipe, RecipeLine, Ingredient, UnitConversion, Vendor, VendorPrice, MenuCategory, MenuItem, ChannelModifier models"
      contains: "model Recipe"
    - path: "backend/prisma/seed.ts"
      provides: "UnitConversion seed data"
      contains: "unitConversion"
    - path: "frontend/lib/types/recipe.ts"
      provides: "Recipe, RecipeLine, BomLineInput types"
      exports: ["Recipe", "RecipeLine"]
    - path: "frontend/lib/types/ingredient.ts"
      provides: "Ingredient, IngredientCategory types"
      exports: ["Ingredient", "IngredientCategory"]
    - path: "frontend/lib/types/vendor.ts"
      provides: "Vendor, VendorPrice types"
      exports: ["Vendor", "VendorPrice"]
    - path: "frontend/lib/types/menu.ts"
      provides: "MenuCategory, MenuItem, ChannelModifier, FoodCostThreshold types"
      exports: ["MenuItem", "MenuCategory", "ChannelModifier"]
  key_links:
    - from: "frontend/lib/types/recipe.ts"
      to: "backend/prisma/schema.prisma"
      via: "field names match Prisma model"
      pattern: "recipe_id.*string"
---

<objective>
Prisma schema migration for all 9 Phase 7 models, UnitConversion seed data, Asset.linked_recipe_id column, and shared frontend TypeScript types.

Purpose: Every subsequent plan depends on the schema and types. This is the single-source-of-truth layer.
Output: Migration SQL, updated seed, 4 frontend type files.
</objective>

<execution_context>
@D:/aditee/konmaxperience/.claude/get-shit-done/workflows/execute-plan.md
@D:/aditee/konmaxperience/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@docs/superpowers/specs/2026-03-20-food-production-pipeline-design.md
@.planning/phases/07-recipe-ingredient-management/07-CONTEXT.md
@.planning/phases/07-recipe-ingredient-management/07-RESEARCH.md

<interfaces>
<!-- Existing Prisma models that Phase 7 entities reference -->

From backend/prisma/schema.prisma:
```prisma
model Brand {
  id            String  @id @default(uuid())
  name          String
  brand_type    String
  status        String  @default("idea")
  owner_user_id String?
  owner         User?   @relation(fields: [owner_user_id], references: [id])
  notes         String?
  assets        Asset[]
}

model Zone {
  id            String  @id @default(uuid())
  name          String
  zone_type     String
  owner_user_id String?
  owner         User?   @relation(fields: [owner_user_id], references: [id])
  status        String  @default("planned")
  notes         String?
}

model Asset {
  id              String   @id @default(uuid())
  name            String
  asset_type      String
  linked_task_id  String?
  linked_task     Task?    @relation(fields: [linked_task_id], references: [id])
  linked_brand_id String?
  linked_brand    Brand?   @relation(fields: [linked_brand_id], references: [id])
  url             String
  status          String   @default("draft")
  created_by      String
  creator         User     @relation(fields: [created_by], references: [id])
  created_at      DateTime @default(now())
}
```

From frontend/lib/types/brand.ts:
```typescript
export interface Brand {
  id: string;
  name: string;
  brand_type: BrandType;
  status: BrandStatus;
  owner_user_id: string | null;
  owner?: { id: string; name: string } | null;
  notes: string | null;
}
```

From frontend/lib/types/index.ts:
```typescript
export * from './auth';
export * from './roles';
// ... existing re-exports
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Prisma schema migration — 9 new models + Asset.linked_recipe_id</name>
  <files>backend/prisma/schema.prisma, backend/prisma/seed.ts</files>
  <read_first>
    - backend/prisma/schema.prisma (full file — understand existing models, relations, naming conventions)
    - backend/prisma/seed.ts (full file — understand seed patterns for adding UnitConversion)
    - docs/superpowers/specs/2026-03-20-food-production-pipeline-design.md (sections 3.1-3.4 — exact field specs)
  </read_first>
  <action>
**Add 9 new models to schema.prisma** following exact field names from pipeline spec sections 3.1-3.4. Per D-14, D-15, D-16, D-17, D-18:

1. **Recipe** model (per D-14, spec 3.1):
   - Fields: id (uuid), name (String), description (String), prep_steps (String), cooking_method (String), yield_qty (Decimal), yield_unit (String), portion_size (String), shelf_life_hours (Int?), brand_id (String?), zone_id (String?), image_url (String?), computed_cost (Decimal?), status (String @default("draft")), created_by (String), created_at (DateTime @default(now())), updated_at (DateTime @updatedAt)
   - Relations: brand (Brand?), zone (Zone?), creator (User), RecipeLines (RecipeLine[] @relation("RecipeLines")), SourceLines (RecipeLine[] @relation("SourceRecipeLines")), MenuItems (MenuItem[])
   - Add `recipes Recipe[]` to User model (relation name "RecipeCreator" or use created_by field)
   - Add `recipes Recipe[]` to Brand model
   - Add `recipes Recipe[]` to Zone model

2. **RecipeLine** model (per D-14, spec 3.1):
   - Fields: id (uuid), recipe_id (String), input_type (String — "ingredient" | "recipe"), ingredient_id (String?), source_recipe_id (String?), quantity (Decimal), unit (String), prep_notes (String?), sort_order (Int @default(0))
   - Relations: recipe (Recipe @relation("RecipeLines")), ingredient (Ingredient?), source_recipe (Recipe? @relation("SourceRecipeLines"))

3. **Ingredient** model (per D-15, spec 3.2):
   - Fields: id (uuid), name (String), category (String), base_unit (String), min_stock_level (Decimal), created_at (DateTime), updated_at (DateTime @updatedAt)
   - Relations: RecipeLines (RecipeLine[]), VendorPrices (VendorPrice[])

4. **UnitConversion** model (per D-16, spec 3.2):
   - Fields: id (uuid), from_unit (String), to_unit (String), factor (Decimal)
   - Constraint: @@unique([from_unit, to_unit])

5. **Vendor** model (per D-17, spec 3.3):
   - Fields: id (uuid), name (String), phone (String?), email (String?), address (String?), payment_terms (String?), status (String @default("active")), created_at (DateTime @default(now()))
   - Relations: VendorPrices (VendorPrice[])

6. **VendorPrice** model (per D-17, spec 3.3):
   - Fields: id (uuid), vendor_id (String), ingredient_id (String), price (Decimal), unit (String), effective_date (DateTime), created_at (DateTime @default(now()))
   - Relations: vendor (Vendor), ingredient (Ingredient)

7. **MenuCategory** model (per D-18, spec 3.4):
   - Fields: id (uuid), name (String), brand_id (String), sort_order (Int @default(0)), status (String @default("active"))
   - Relations: brand (Brand), MenuItems (MenuItem[])
   - Add `menu_categories MenuCategory[]` to Brand model

8. **MenuItem** model (per D-18, spec 3.4):
   - Fields: id (uuid), recipe_id (String), category_id (String), name (String), base_price (Decimal), image_url (String?), available (Boolean @default(true)), status (String @default("active")), created_at (DateTime), updated_at (DateTime @updatedAt)
   - Relations: recipe (Recipe), category (MenuCategory)

9. **ChannelModifier** model (per D-18, spec 3.4):
   - Fields: id (uuid), channel_type (String), modifier_type (String — "fixed" | "percentage"), modifier_value (Decimal), status (String @default("active"))
   - Constraint: @@unique([channel_type])

10. **Asset model update** (per spec 3.11):
    - Add `linked_recipe_id String?` and `linked_recipe Recipe? @relation(fields: [linked_recipe_id], references: [id])` to Asset
    - Add `linked_assets Asset[]` to Recipe model

**Update seed.ts** — add UnitConversion seed after the channels seed block (per D-16):
```typescript
// Seed unit conversions
const UNIT_CONVERSIONS = [
  { from_unit: 'kg',     to_unit: 'g',      factor: 1000    },
  { from_unit: 'g',      to_unit: 'kg',     factor: 0.001   },
  { from_unit: 'L',      to_unit: 'ml',     factor: 1000    },
  { from_unit: 'ml',     to_unit: 'L',      factor: 0.001   },
  { from_unit: 'dozen',  to_unit: 'pieces', factor: 12      },
  { from_unit: 'pieces', to_unit: 'dozen',  factor: 0.08333 },
];

// Inside $transaction, after channels:
await tx.unitConversion.deleteMany({});
for (const uc of UNIT_CONVERSIONS) {
  await tx.unitConversion.create({ data: uc });
}
```
Add log line: `console.log('  - ${UNIT_CONVERSIONS.length} unit conversions');`

**Run migration:** `cd backend && npx prisma migrate dev --name phase_7_recipe`

IMPORTANT: Use `Decimal` Prisma type (not Float) for all monetary and quantity fields. Follow existing schema naming conventions: snake_case fields, uuid ids, @default(now()) for created_at.
  </action>
  <verify>
    <automated>cd backend && npx prisma validate</automated>
  </verify>
  <acceptance_criteria>
    - grep "model Recipe" backend/prisma/schema.prisma returns a match
    - grep "model RecipeLine" backend/prisma/schema.prisma returns a match
    - grep "model Ingredient" backend/prisma/schema.prisma returns a match
    - grep "model UnitConversion" backend/prisma/schema.prisma returns a match
    - grep "model Vendor" backend/prisma/schema.prisma returns a match
    - grep "model VendorPrice" backend/prisma/schema.prisma returns a match
    - grep "model MenuCategory" backend/prisma/schema.prisma returns a match
    - grep "model MenuItem" backend/prisma/schema.prisma returns a match
    - grep "model ChannelModifier" backend/prisma/schema.prisma returns a match
    - grep "linked_recipe_id" backend/prisma/schema.prisma returns a match (Asset update)
    - grep "unitConversion" backend/prisma/seed.ts returns a match
    - grep "UNIT_CONVERSIONS" backend/prisma/seed.ts returns a match
    - npx prisma validate exits 0
  </acceptance_criteria>
  <done>All 9 new Prisma models match pipeline spec exactly, Asset.linked_recipe_id added, UnitConversion seed data present, migration generated, prisma validate passes</done>
</task>

<task type="auto">
  <name>Task 2: Frontend shared types — recipe, ingredient, vendor, menu</name>
  <files>frontend/lib/types/recipe.ts, frontend/lib/types/ingredient.ts, frontend/lib/types/vendor.ts, frontend/lib/types/menu.ts, frontend/lib/types/index.ts</files>
  <read_first>
    - frontend/lib/types/brand.ts (pattern for type file structure, naming, export style)
    - frontend/lib/types/asset.ts (pattern for status types, labels)
    - frontend/lib/types/index.ts (barrel export pattern)
    - backend/prisma/schema.prisma (the models just created in Task 1 — field names must match)
  </read_first>
  <action>
Create 4 frontend type files following the established pattern from brand.ts (snake_case fields matching Prisma, type unions for enums, label records, exported arrays):

**frontend/lib/types/recipe.ts:**
```typescript
export type RecipeStatus = 'draft' | 'approved' | 'archived';

export interface RecipeLine {
  id: string;
  recipe_id: string;
  input_type: 'ingredient' | 'recipe';
  ingredient_id: string | null;
  source_recipe_id: string | null;
  ingredient?: { id: string; name: string; base_unit: string; category: string } | null;
  source_recipe?: Recipe | null;
  quantity: number;
  unit: string;
  prep_notes: string | null;
  sort_order: number;
}

export interface Recipe {
  id: string;
  name: string;
  description: string;
  prep_steps: string;
  cooking_method: string;
  yield_qty: number;
  yield_unit: string;
  portion_size: string;
  shelf_life_hours: number | null;
  brand_id: string | null;
  zone_id: string | null;
  brand?: { id: string; name: string } | null;
  zone?: { id: string; name: string } | null;
  image_url: string | null;
  computed_cost: number | null;
  status: RecipeStatus;
  created_by: string;
  creator?: { id: string; name: string } | null;
  created_at: string;
  updated_at: string;
  RecipeLines?: RecipeLine[];
}

export interface BomLineInput {
  input_type: 'ingredient' | 'recipe';
  item_id: string;
  quantity: number;
  unit: string;
  prep_notes?: string;
}

export const RECIPE_STATUS_LABELS: Record<RecipeStatus, string> = {
  draft: 'Draft',
  approved: 'Approved',
  archived: 'Archived',
};

export const RECIPE_STATUSES: RecipeStatus[] = ['draft', 'approved', 'archived'];

export const YIELD_UNITS = ['g', 'ml', 'pieces', 'portions'] as const;
```

**frontend/lib/types/ingredient.ts:**
```typescript
export type IngredientCategory = 'dairy' | 'vegetable' | 'spice' | 'grain' | 'meat' | 'oil';

export interface Ingredient {
  id: string;
  name: string;
  category: IngredientCategory;
  base_unit: string;
  min_stock_level: number;
  created_at: string;
  updated_at: string;
}

export const INGREDIENT_CATEGORIES: IngredientCategory[] = ['dairy', 'vegetable', 'spice', 'grain', 'meat', 'oil'];

export const INGREDIENT_CATEGORY_LABELS: Record<IngredientCategory, string> = {
  dairy: 'Dairy',
  vegetable: 'Vegetable',
  spice: 'Spice',
  grain: 'Grain',
  meat: 'Meat',
  oil: 'Oil',
};

export const BASE_UNITS = ['g', 'ml', 'pieces', 'kg', 'L'] as const;
```

**frontend/lib/types/vendor.ts:**
```typescript
export type VendorStatus = 'active' | 'inactive';

export interface VendorPrice {
  id: string;
  vendor_id: string;
  ingredient_id: string;
  ingredient?: { id: string; name: string; base_unit: string } | null;
  price: number;
  unit: string;
  effective_date: string;
  created_at: string;
}

export interface Vendor {
  id: string;
  name: string;
  phone: string | null;
  email: string | null;
  address: string | null;
  payment_terms: string | null;
  status: VendorStatus;
  created_at: string;
  VendorPrices?: VendorPrice[];
}

export const VENDOR_STATUS_LABELS: Record<VendorStatus, string> = {
  active: 'Active',
  inactive: 'Inactive',
};

export const PAYMENT_TERMS_OPTIONS = ['COD', 'Net 7', 'Net 30'] as const;
```

**frontend/lib/types/menu.ts:**
```typescript
export type MenuCategoryStatus = 'active' | 'inactive';
export type MenuItemStatus = 'active' | 'inactive';
export type ModifierType = 'fixed' | 'percentage';

export interface MenuCategory {
  id: string;
  name: string;
  brand_id: string;
  sort_order: number;
  status: MenuCategoryStatus;
  MenuItems?: MenuItem[];
}

export interface MenuItem {
  id: string;
  recipe_id: string;
  category_id: string;
  name: string;
  base_price: number;
  image_url: string | null;
  available: boolean;
  status: MenuItemStatus;
  created_at: string;
  updated_at: string;
  recipe?: { id: string; name: string; computed_cost: number | null; yield_qty: number } | null;
  category?: { id: string; name: string; brand_id: string } | null;
}

export interface ChannelModifier {
  id: string;
  channel_type: string;
  modifier_type: ModifierType;
  modifier_value: number;
  status: string;
}

export function calcFoodCostPercent(computedCost: number | null, basePrice: number): number | null {
  if (!computedCost || !basePrice) return null;
  return (computedCost / basePrice) * 100;
}
```

**Update frontend/lib/types/index.ts** — add 4 new re-exports at end:
```typescript
export * from './recipe';
export * from './ingredient';
export * from './vendor';
export * from './menu';
```
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit --pretty 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - grep "export interface Recipe" frontend/lib/types/recipe.ts returns a match
    - grep "export interface RecipeLine" frontend/lib/types/recipe.ts returns a match
    - grep "export interface BomLineInput" frontend/lib/types/recipe.ts returns a match
    - grep "export interface Ingredient" frontend/lib/types/ingredient.ts returns a match
    - grep "export type IngredientCategory" frontend/lib/types/ingredient.ts returns a match
    - grep "export interface Vendor" frontend/lib/types/vendor.ts returns a match
    - grep "export interface VendorPrice" frontend/lib/types/vendor.ts returns a match
    - grep "export interface MenuItem" frontend/lib/types/menu.ts returns a match
    - grep "export interface MenuCategory" frontend/lib/types/menu.ts returns a match
    - grep "export interface ChannelModifier" frontend/lib/types/menu.ts returns a match
    - grep "export function calcFoodCostPercent" frontend/lib/types/menu.ts returns a match
    - grep "recipe" frontend/lib/types/index.ts returns a match
    - grep "ingredient" frontend/lib/types/index.ts returns a match
    - grep "vendor" frontend/lib/types/index.ts returns a match
    - grep "menu" frontend/lib/types/index.ts returns a match
    - tsc --noEmit exits cleanly (no type errors)
  </acceptance_criteria>
  <done>4 frontend type files exist with all types matching Prisma schema, barrel export updated, TypeScript compiles without errors</done>
</task>

</tasks>

<verification>
- `cd backend && npx prisma validate` passes
- `cd frontend && npx tsc --noEmit` passes
- All 9 model names greppable in schema.prisma
- UnitConversion seed data present in seed.ts
- All type files export expected interfaces
</verification>

<success_criteria>
- 9 new Prisma models match pipeline spec 3.1-3.4 exactly
- Asset model has linked_recipe_id column
- UnitConversion seed with 6 conversion pairs
- 4 frontend type files with complete interfaces
- Both prisma validate and tsc --noEmit pass
</success_criteria>

<output>
After completion, create `.planning/phases/07-recipe-ingredient-management/07-01-SUMMARY.md`
</output>
