---
phase: 04-gamification-readiness-intelligence
plan: 03
type: execute
wave: 2
depends_on: [04-01, 04-02]
files_modified:
  - frontend/lib/types/readiness.ts
  - frontend/lib/types/leaderboard.ts
  - frontend/components/ops/readiness/ReadinessMeterRing.tsx
  - frontend/components/ops/readiness/ReadinessGrid.tsx
  - frontend/components/ops/readiness/MeterDetailPanel.tsx
  - frontend/components/ops/leaderboard/LeaderboardPodium.tsx
  - frontend/components/ops/leaderboard/LeaderboardTable.tsx
  - frontend/app/(ops)/readiness/page.tsx
  - frontend/app/(ops)/leaderboard/page.tsx
autonomous: true
requirements: [INTL-01, INTL-02, INTL-04]

must_haves:
  truths:
    - "/readiness page displays all 10 readiness meters as animated color-coded rings in a responsive grid"
    - "Clicking a readiness meter ring expands a detail panel showing valid tasks feeding into that meter"
    - "Readiness meter rings are green at 70%+, amber at 30-69%, red at below 30%"
    - "/leaderboard page shows top 3 users as elevated podium with remaining in a ranked table"
    - "FOUNDER_ADMIN is excluded from the leaderboard rankings"
    - "When leaderboard kill switch is off, /leaderboard shows 'Leaderboard Paused' state"
    - "Current user's leaderboard row is highlighted with ShineBorder"
  artifacts:
    - path: "frontend/app/(ops)/readiness/page.tsx"
      provides: "Readiness Intelligence page"
      min_lines: 30
    - path: "frontend/app/(ops)/leaderboard/page.tsx"
      provides: "Team Leaderboard page"
      min_lines: 30
    - path: "frontend/components/ops/readiness/ReadinessMeterRing.tsx"
      provides: "Single readiness meter ring component"
      exports: ["ReadinessMeterRing"]
    - path: "frontend/components/ops/readiness/ReadinessGrid.tsx"
      provides: "Responsive grid of all 10 meters"
      exports: ["ReadinessGrid"]
    - path: "frontend/components/ops/readiness/MeterDetailPanel.tsx"
      provides: "Expandable panel showing tasks for a meter"
      exports: ["MeterDetailPanel"]
    - path: "frontend/components/ops/leaderboard/LeaderboardPodium.tsx"
      provides: "Top 3 podium visual"
      exports: ["LeaderboardPodium"]
    - path: "frontend/components/ops/leaderboard/LeaderboardTable.tsx"
      provides: "Ranked table of remaining users"
      exports: ["LeaderboardTable"]
  key_links:
    - from: "frontend/app/(ops)/readiness/page.tsx"
      to: "/readiness-meters"
      via: "React Query useQuery"
      pattern: "queryKey.*readiness-meters"
    - from: "frontend/components/ops/readiness/MeterDetailPanel.tsx"
      to: "/readiness-meters/:id/tasks"
      via: "React Query useQuery"
      pattern: "readiness-meters.*tasks"
    - from: "frontend/app/(ops)/leaderboard/page.tsx"
      to: "/leaderboard"
      via: "React Query useQuery"
      pattern: "queryKey.*leaderboard"
---

<objective>
Build the /readiness and /leaderboard frontend pages with all supporting components. The readiness page shows all 10 operational readiness meters as animated color-coded rings in a responsive grid with expandable task detail panels. The leaderboard page shows a top-3 podium visual with ranked table below, current user highlighting, and kill switch paused state.

Purpose: Make operational readiness and team rankings visible — the gamification payoff where validated work translates into visual progress and competitive rankings.
Output: Two fully functional pages with all states (loading, empty, error, populated), accessibility attributes, and MagicUI animations.
</objective>

<execution_context>
@D:/aditee/konmaxperience/.claude/get-shit-done/workflows/execute-plan.md
@D:/aditee/konmaxperience/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-gamification-readiness-intelligence/04-CONTEXT.md
@.planning/phases/04-gamification-readiness-intelligence/04-RESEARCH.md
@.planning/phases/04-gamification-readiness-intelligence/04-UI-SPEC.md

<interfaces>
<!-- Backend API contracts (from Plan 01) -->

GET /readiness-meters response:
```typescript
interface ReadinessMeter {
  id: string;
  code: string;
  name: string;
  description: string;
  current_value: number; // 0-100 float
  target_value: number;
  weight: number;
}
// Returns: ReadinessMeter[] ordered by code ASC
```

GET /readiness-meters/:id/tasks response:
```typescript
interface MeterTaskEvent {
  id: string;
  task_id: string;
  value: number;
  created_at: string;
  task: {
    id: string;
    title: string;
    valid_xp: number;
    owner: { id: string; name: string };
  };
}
// Returns: MeterTaskEvent[] ordered by created_at DESC
```

GET /leaderboard response:
```typescript
interface LeaderboardResponse {
  enabled: boolean;
  users: Array<{
    id: string;
    name: string;
    xp_total: number;
    level: number;
    function: string;
  }>;
}
```

<!-- Shared types (from Plan 02) -->

From frontend/lib/types/gamification.ts:
```typescript
export function getMeterColors(value: number): { primary: string; secondary: string; textClass: string };
export const LEVEL_COLORS: Record<number, { text: string; bg: string }>;
export function getXpForNextLevel(currentXp: number): { current: number; target: number; percent: number };
```

From frontend/components/ops/gamification/LevelBadge.tsx:
```typescript
export function LevelBadge(props: { level: number; showGlow?: boolean; className?: string }): JSX.Element;
```

<!-- MagicUI Components (pre-installed) -->

animated-circular-progress-bar: value, gaugePrimaryColor, gaugeSecondaryColor, className
number-ticker: value, startValue, delay, className
blur-fade: delay, inView, className, children
animated-list: delay, children
interactive-hover-button: text, className, children
hyper-text: text, className, animateOnLoad
avatar-circles: avatarUrls, numPeople, className
shine-border: borderWidth, color, className, children
magic-card: gradientColor, className, children
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Readiness types, components (ReadinessMeterRing, ReadinessGrid, MeterDetailPanel), and /readiness page</name>
  <files>
    frontend/lib/types/readiness.ts,
    frontend/components/ops/readiness/ReadinessMeterRing.tsx,
    frontend/components/ops/readiness/ReadinessGrid.tsx,
    frontend/components/ops/readiness/MeterDetailPanel.tsx,
    frontend/app/(ops)/readiness/page.tsx
  </files>
  <read_first>
    frontend/lib/types/gamification.ts,
    frontend/components/ui/animated-circular-progress-bar.tsx,
    frontend/components/ui/number-ticker.tsx,
    frontend/components/ui/blur-fade.tsx,
    frontend/components/ui/animated-list.tsx,
    frontend/components/ui/interactive-hover-button.tsx,
    frontend/lib/api-client.ts,
    frontend/app/(ops)/missions/page.tsx,
    .planning/phases/04-gamification-readiness-intelligence/04-UI-SPEC.md
  </read_first>
  <action>
    **1. Readiness types** — Create `frontend/lib/types/readiness.ts`:
    ```typescript
    export interface ReadinessMeter {
      id: string;
      code: string;
      name: string;
      description: string;
      current_value: number;
      target_value: number;
      weight: number;
    }

    export interface MeterTaskEvent {
      id: string;
      task_id: string;
      value: number;
      created_at: string;
      task: {
        id: string;
        title: string;
        valid_xp: number;
        owner: { id: string; name: string };
      };
    }
    ```

    **2. ReadinessMeterRing** — `frontend/components/ops/readiness/ReadinessMeterRing.tsx`:
    - Props: `{ meter: ReadinessMeter; mini?: boolean; selected?: boolean; onClick?: () => void }`
    - Uses `getMeterColors(meter.current_value)` for color coding.
    - Renders `AnimatedCircularProgressBar`:
      - `value={meter.current_value}`
      - `gaugePrimaryColor={colors.primary}`
      - `gaugeSecondaryColor={colors.secondary}`
      - `className={mini ? "size-16 text-sm font-medium" : "size-40 text-2xl font-semibold"}`
    - Inside the ring center (using absolute positioning), render `NumberTicker value={meter.current_value} className={cn("font-semibold", colors.textClass)}` with `%` suffix.
    - Below the ring: meter name label (14px, muted).
    - On hover (non-mini): show `InteractiveHoverButton text="View Tasks"` overlaid on ring center. Use `hover:scale-105 transition-transform duration-200` on the outer wrapper.
    - Ring wrapper has `onClick` handler and `cursor-pointer` class.
    - `aria-label="{meter.name}: {meter.current_value}% ready"` on the interactive element.
    - Selected state: subtle ring-2 ring-primary outline.

    **3. ReadinessGrid** — `frontend/components/ops/readiness/ReadinessGrid.tsx`:
    - Props: `{ meters: ReadinessMeter[]; selectedMeterId: string | null; onSelectMeter: (id: string | null) => void }`
    - Renders a CSS grid: `grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 xl:grid-cols-4 gap-6`.
    - Each meter wrapped in `BlurFade` with `delay={index * 0.05}`.
    - Clicking a ring calls `onSelectMeter(meter.id)` (toggles off if same meter clicked).
    - After the grid row containing the selected meter, render `MeterDetailPanel` for the selected meter.

    **4. MeterDetailPanel** — `frontend/components/ops/readiness/MeterDetailPanel.tsx`:
    - Props: `{ meterId: string; meterName: string; currentValue: number; onClose: () => void }`
    - Fetches `GET /readiness-meters/{meterId}/tasks` via React Query with queryKey `['readiness-meters', meterId, 'tasks']`.
    - Renders inside a `BlurFade` wrapper for entrance animation.
    - Title: `"{meterName} — {currentValue}% Ready"` (per copywriting contract).
    - Close button (X icon) in top-right.
    - Task list using `AnimatedList` with 50ms delay. Each item shows: task title, "+{valid_xp} XP", owner name.
    - Loading state: skeleton list items (3 shimmer rows).
    - Empty state: "No validated tasks contributing to this meter yet." (per copywriting).
    - Error state: inline error text with retry button.

    **5. /readiness page** — `frontend/app/(ops)/readiness/page.tsx`:
    - Page header: "Readiness Intelligence" h1 + subtitle "All values derived from validated task completions" (14px muted).
    - Fetches `GET /readiness-meters` via React Query with queryKey `['readiness-meters']`.
    - Renders `ReadinessGrid` with selected meter state managed via `useState<string | null>(null)`.
    - Loading state: skeleton rings grid (10 placeholder rings with opacity-30 pulse animation).
    - Empty state: "No validated tasks yet. Readiness meters update when tasks are validated with approved evidence."
    - Error state: Alert component with "Couldn't load readiness data. Refresh the page or try again in a moment." and retry button.
    - Wrap entire content in `BlurFade` for page entrance.
  </action>
  <verify>
    <automated>cd D:/aditee/konmaxperience/frontend && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - frontend/lib/types/readiness.ts contains "interface ReadinessMeter"
    - frontend/lib/types/readiness.ts contains "interface MeterTaskEvent"
    - frontend/components/ops/readiness/ReadinessMeterRing.tsx contains "AnimatedCircularProgressBar"
    - frontend/components/ops/readiness/ReadinessMeterRing.tsx contains "getMeterColors"
    - frontend/components/ops/readiness/ReadinessMeterRing.tsx contains "aria-label"
    - frontend/components/ops/readiness/ReadinessMeterRing.tsx contains "size-16" (mini variant)
    - frontend/components/ops/readiness/ReadinessGrid.tsx contains "grid-cols-4"
    - frontend/components/ops/readiness/ReadinessGrid.tsx contains "BlurFade"
    - frontend/components/ops/readiness/MeterDetailPanel.tsx contains "AnimatedList"
    - frontend/components/ops/readiness/MeterDetailPanel.tsx contains "readiness-meters"
    - frontend/components/ops/readiness/MeterDetailPanel.tsx contains "No validated tasks contributing"
    - frontend/app/(ops)/readiness/page.tsx contains "Readiness Intelligence"
    - frontend/app/(ops)/readiness/page.tsx contains "queryKey" with readiness-meters
    - TypeScript compiles without errors
  </acceptance_criteria>
  <done>/readiness page renders all 10 meters in responsive grid with color-coded rings, click-to-expand task detail panels, loading/empty/error states, and accessibility labels.</done>
</task>

<task type="auto">
  <name>Task 2: Leaderboard types, components (LeaderboardPodium, LeaderboardTable), and /leaderboard page</name>
  <files>
    frontend/lib/types/leaderboard.ts,
    frontend/components/ops/leaderboard/LeaderboardPodium.tsx,
    frontend/components/ops/leaderboard/LeaderboardTable.tsx,
    frontend/app/(ops)/leaderboard/page.tsx
  </files>
  <read_first>
    frontend/lib/types/gamification.ts,
    frontend/components/ops/gamification/LevelBadge.tsx,
    frontend/components/ui/hyper-text.tsx,
    frontend/components/ui/avatar-circles.tsx,
    frontend/components/ui/shine-border.tsx,
    frontend/components/ui/magic-card.tsx,
    frontend/components/ui/number-ticker.tsx,
    frontend/components/ui/blur-fade.tsx,
    frontend/components/ui/text-animate.tsx,
    frontend/lib/stores/auth-store.ts,
    frontend/lib/api-client.ts,
    .planning/phases/04-gamification-readiness-intelligence/04-UI-SPEC.md
  </read_first>
  <action>
    **1. Leaderboard types** — Create `frontend/lib/types/leaderboard.ts`:
    ```typescript
    export interface LeaderboardUser {
      id: string;
      name: string;
      xp_total: number;
      level: number;
      function: string;
    }

    export interface LeaderboardResponse {
      enabled: boolean;
      users: LeaderboardUser[];
    }
    ```

    **2. LeaderboardPodium** — `frontend/components/ops/leaderboard/LeaderboardPodium.tsx`:
    - Props: `{ users: LeaderboardUser[]; currentUserId: string }`
    - Takes top 3 users from the array. If fewer than 3, only show available.
    - Layout: 3 columns using flex. Center column (rank 1) is elevated with `style={{ transform: 'translateY(-20px)' }}`. Left = rank 2, right = rank 3.
    - Each column (centered content):
      - Avatar: 48px circle with DiceBear initials URL `https://api.dicebear.com/7.x/initials/svg?seed={name}`.
      - Name: `HyperText text={name} animateOnLoad className="text-base font-semibold"`.
      - `LevelBadge level={user.level}`.
      - XP: `NumberTicker value={user.xp_total} className="text-lg font-bold tabular-nums"` + "XP" label.
      - Rank number: 28px semibold, using `--primary` color.
    - If the current user is in the top 3, wrap their column with `ShineBorder borderWidth={2} color={['#a78bfa', '#22c55e']}`.
    - Wrap entire podium in `BlurFade`.

    **3. LeaderboardTable** — `frontend/components/ops/leaderboard/LeaderboardTable.tsx`:
    - Props: `{ users: LeaderboardUser[]; currentUserId: string; startRank: number }` (startRank is typically 4, after podium).
    - Renders a `MagicCard gradientColor="#1a1a2e"` wrapping a semantic HTML `<table>` with proper `<th scope="col">` headers: Rank, User, Level, XP, Tasks Validated (placeholder count for now, just show "-").
    - Each row:
      - Rank: `#{startRank + index}` in muted text.
      - User: Avatar (32px, DiceBear) + name.
      - Level: `LevelBadge level={user.level}`.
      - XP: `NumberTicker value={user.xp_total} className="text-sm tabular-nums"`.
    - Current user's row gets `bg-primary/5` background and is wrapped in `ShineBorder`.
    - Use `ScrollArea` if more than 10 rows.

    **4. /leaderboard page** — `frontend/app/(ops)/leaderboard/page.tsx`:
    - Fetches `GET /leaderboard` via React Query with queryKey `['leaderboard']`.
    - **Kill switch off state**: If `data.enabled === false`, render a Card with centered text: heading "Leaderboard Paused" and body "Rankings are currently hidden. Keep completing tasks to earn XP — your progress is being tracked." (per copywriting contract). No podium or table shown.
    - **Populated state**: Render `LeaderboardPodium` with first 3 users + `LeaderboardTable` with users from index 3 onward (startRank=4).
    - **Recent level-ups strip** at bottom: Use `AvatarCircles` for recent level-up user avatars + `TextAnimate` for "{Name} just reached Level {N}!" text. For v1, derive from the leaderboard data by checking if any user has level > 1 (simplified — full event sourcing is v2). Show last 3 users by level descending.
    - Page header: "Team Leaderboard" h1.
    - Loading state: skeleton avatar columns (3 podium placeholders) + skeleton table rows.
    - Empty state: "No rankings yet. Complete and validate tasks to earn XP and appear on the leaderboard."
    - Error state: Alert with "Couldn't load leaderboard. Refresh the page or try again in a moment." and retry button.
    - Wrap in `BlurFade` for page entrance.
    - Get `currentUserId` from `useAuthStore`.
  </action>
  <verify>
    <automated>cd D:/aditee/konmaxperience/frontend && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - frontend/lib/types/leaderboard.ts contains "interface LeaderboardUser"
    - frontend/lib/types/leaderboard.ts contains "interface LeaderboardResponse"
    - frontend/components/ops/leaderboard/LeaderboardPodium.tsx contains "HyperText"
    - frontend/components/ops/leaderboard/LeaderboardPodium.tsx contains "translateY(-20px)"
    - frontend/components/ops/leaderboard/LeaderboardPodium.tsx contains "ShineBorder"
    - frontend/components/ops/leaderboard/LeaderboardPodium.tsx contains "dicebear"
    - frontend/components/ops/leaderboard/LeaderboardTable.tsx contains "MagicCard"
    - frontend/components/ops/leaderboard/LeaderboardTable.tsx contains "<th scope"
    - frontend/components/ops/leaderboard/LeaderboardTable.tsx contains "bg-primary/5"
    - frontend/app/(ops)/leaderboard/page.tsx contains "Leaderboard Paused"
    - frontend/app/(ops)/leaderboard/page.tsx contains "data.enabled === false" or "!data?.enabled"
    - frontend/app/(ops)/leaderboard/page.tsx contains "queryKey" with leaderboard
    - frontend/app/(ops)/leaderboard/page.tsx contains "AvatarCircles"
    - TypeScript compiles without errors
  </acceptance_criteria>
  <done>/leaderboard page shows podium for top 3 with HyperText names, ranked table for remaining users with MagicCard wrapper, current user highlighting with ShineBorder, kill switch paused state, recent level-ups strip, and all 4 states handled.</done>
</task>

</tasks>

<verification>
```bash
cd D:/aditee/konmaxperience/frontend && npx tsc --noEmit 2>&1 | head -30
```
TypeScript compiles without errors. All readiness and leaderboard components render correctly.
</verification>

<success_criteria>
- /readiness page shows 10 animated rings with green/amber/red color coding
- Clicking a ring expands MeterDetailPanel showing valid tasks with AnimatedList
- /leaderboard page shows podium (top 3) with elevated center + ranked table
- FOUNDER_ADMIN excluded from leaderboard rankings
- Kill switch off shows "Leaderboard Paused" state
- Current user highlighted with ShineBorder
- Loading, empty, and error states handled for both pages
- Accessibility: aria-labels on rings, semantic table headers on leaderboard
</success_criteria>

<output>
After completion, create `.planning/phases/04-gamification-readiness-intelligence/04-03-SUMMARY.md`
</output>
