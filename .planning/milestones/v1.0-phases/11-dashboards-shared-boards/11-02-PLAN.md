---
phase: 11-dashboards-shared-boards
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/app/(ops)/dashboard/page.tsx
  - frontend/components/ops/dashboard/AdminPendingApprovalsWidget.tsx
  - frontend/components/ops/dashboard/AdminBlockersWidget.tsx
  - frontend/components/ops/dashboard/AdminAdHocInjectorWidget.tsx
  - frontend/components/ops/dashboard/AdminRecentDecisionsWidget.tsx
  - frontend/components/ops/dashboard/RoleDashboardSections.tsx
  - frontend/components/ops/Sidebar.tsx
autonomous: true
requirements:
  - DASH-01
  - DASH-02

must_haves:
  truths:
    - "Admin sees Mission Control with 8 widget sections in correct order per D-04: Approvals, Blockers, Ad-hoc injector, Readiness strip, KPI alerts, Decisions, Leaderboard, Low stock"
    - "Admin sees pending approvals count and list of up to 5 oldest pending"
    - "Admin sees active blockers count and list of up to 5 blocked tasks"
    - "Admin sees ad-hoc task injector as inline form card that creates tasks"
    - "Admin sees recent decisions feed with last 5 decisions after KPI alerts section"
    - "Role user sees My Dashboard with active tasks, quest progress, XP card, contribution meters, evidence count, and team alerts"
    - "Sidebar has new Boards section, Analytics link under Intelligence, Kitchen Dashboard link, Inventory Overview link"
  artifacts:
    - path: "frontend/app/(ops)/dashboard/page.tsx"
      provides: "Role-conditional dashboard page"
      contains: "isAdmin"
    - path: "frontend/components/ops/dashboard/AdminPendingApprovalsWidget.tsx"
      provides: "Pending approvals widget"
      contains: "Pending Approvals"
    - path: "frontend/components/ops/dashboard/AdminBlockersWidget.tsx"
      provides: "Active blockers widget"
      contains: "Active Blockers"
    - path: "frontend/components/ops/dashboard/AdminAdHocInjectorWidget.tsx"
      provides: "Ad-hoc task injector"
      contains: "Quick Task"
    - path: "frontend/components/ops/dashboard/AdminRecentDecisionsWidget.tsx"
      provides: "Recent decisions feed"
      contains: "Recent Decisions"
    - path: "frontend/components/ops/dashboard/RoleDashboardSections.tsx"
      provides: "Role user personal dashboard with tasks, quests, XP, contributions, and alerts"
      contains: "My Tasks"
    - path: "frontend/components/ops/Sidebar.tsx"
      provides: "Updated sidebar with Boards, Analytics, Kitchen Dashboard, Inventory Overview"
      contains: "boardsNav"
  key_links:
    - from: "frontend/app/(ops)/dashboard/page.tsx"
      to: "AdminPendingApprovalsWidget"
      via: "import and render in admin branch"
      pattern: "AdminPendingApprovalsWidget"
    - from: "frontend/components/ops/dashboard/AdminAdHocInjectorWidget.tsx"
      to: "/tasks"
      via: "apiClient.post('/tasks')"
      pattern: "apiClient.post"
    - from: "frontend/components/ops/dashboard/RoleDashboardSections.tsx"
      to: "/tasks"
      via: "apiClient.get('/tasks') with user scope"
      pattern: "apiClient.get.*tasks"
    - from: "frontend/components/ops/dashboard/RoleDashboardSections.tsx"
      to: "/readiness-meters"
      via: "apiClient.get('/readiness-meters') for contribution meters"
      pattern: "readiness-meters"
---

<objective>
Refactor the `/dashboard` page into role-conditional rendering: admin sees Mission Control with 8 widget sections in D-04 order (Approvals, Blockers, Ad-hoc injector, Readiness strip, KPI alerts, Decisions, Leaderboard, Low stock), role users see a personal dashboard with tasks, quests, XP, contribution meters, evidence count, and alerts. Update sidebar with all Phase 11 navigation additions.

Purpose: DASH-01 requires admin mission control with actionable widgets; DASH-02 requires a personal dashboard for role users including contribution meters and evidence. The sidebar changes support all 6 dashboard requirements by providing navigation to new pages.

Output: Refactored dashboard page, 4 new admin widgets, 1 role dashboard component, updated sidebar.
</objective>

<execution_context>
@D:/aditee/konmaxperience/.claude/get-shit-done/workflows/execute-plan.md
@D:/aditee/konmaxperience/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-dashboards-shared-boards/11-CONTEXT.md
@.planning/phases/11-dashboards-shared-boards/11-RESEARCH.md
@.planning/phases/11-dashboards-shared-boards/11-UI-SPEC.md

<interfaces>
<!-- Existing dashboard and sidebar patterns -->

From frontend/app/(ops)/dashboard/page.tsx:
```typescript
// Current page: single admin-only Mission Control with 4 widgets
// Refactor to: isAdmin ? <AdminDashboard /> : <RoleDashboard />
const user = useAuthStore((s) => s.user);
const isAdmin = user?.roleCode === RoleCode.FOUNDER_ADMIN;
```

From frontend/components/ops/Sidebar.tsx:
```typescript
interface NavItem {
  label: string;
  href: string;
  icon: React.ReactNode;
  disabled?: boolean;
  badge?: string;
  badgeClassName?: string;
}
// Sections: overviewNav, workNav, intelligenceNav, operationsNav, kitchenNav, posNav, adminNav
```

From frontend/lib/api-client.ts:
```typescript
export const apiClient = {
  get: <T>(path: string) => request<T>('GET', path),
  post: <T>(path: string, body?: unknown) => request<T>('POST', path, body),
};
```

From frontend/lib/types/evidence.ts:
```typescript
export interface Evidence {
  id: string; task_id: string; uploaded_by: string;
  uploader?: { id: string; name: string };
  type: EvidenceType; url: string; notes: string | null;
  approval_status: EvidenceApprovalStatus;
  created_at: string;
}
```

From frontend/lib/types/decisions.ts:
```typescript
export interface Decision {
  id: string; title: string; type: DecisionType; status: DecisionStatus;
  context: string; created_at: string;
  created_by_user?: { id: string; name: string };
}
```

From backend/src/readiness/readiness.controller.ts:
```typescript
// GET /readiness-meters -> ReadinessMeter[] (all 10 readiness meters with current_value)
// GET /readiness-meters/:id/tasks -> tasks contributing to a specific meter
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Admin dashboard widgets (4 new components)</name>
  <files>
    frontend/components/ops/dashboard/AdminPendingApprovalsWidget.tsx,
    frontend/components/ops/dashboard/AdminBlockersWidget.tsx,
    frontend/components/ops/dashboard/AdminAdHocInjectorWidget.tsx,
    frontend/components/ops/dashboard/AdminRecentDecisionsWidget.tsx
  </files>
  <read_first>
    frontend/components/ops/dashboard/DashboardReadinessStrip.tsx,
    frontend/components/ops/dashboard/DashboardKpiAlert.tsx,
    frontend/components/ops/dashboard/DashboardLowStockAlert.tsx,
    frontend/lib/api-client.ts,
    frontend/lib/types/evidence.ts,
    frontend/lib/types/decisions.ts,
    frontend/app/(ops)/dashboard/page.tsx,
    frontend/components/ops/tasks/AdHocTaskSheet.tsx
  </read_first>
  <action>
    **1. Create `AdminPendingApprovalsWidget.tsx`:**
    - `'use client'` component
    - Uses `useQuery({ queryKey: ['approvals', 'pending'], queryFn: () => apiClient.get<Evidence[]>('/evidence?status=pending'), refetchInterval: 60_000 })` (60-second polling per UI-SPEC)
    - Card with CardHeader containing heading "Pending Approvals" (text-sm font-bold) and count Badge
    - CardContent: list of up to 5 entries sorted by `created_at` ascending (oldest first — longest waiting)
    - Each row: flex row with evidence type label, task name (truncated), waiting duration using `formatDistanceToNow(new Date(evidence.created_at), { addSuffix: false })` from date-fns, and Link to `/approvals`
    - "View all" Link at bottom navigating to `/approvals`
    - Empty state: "All clear" subtitle "No approvals waiting." with CheckCircle icon from lucide in text-emerald-500
    - Loading: Skeleton with 3 rows

    **2. Create `AdminBlockersWidget.tsx`:**
    - `'use client'` component
    - Uses `useQuery({ queryKey: ['tasks', 'blocked'], queryFn: () => apiClient.get<any[]>('/tasks?blocked=true') })` — no polling (low-frequency per UI-SPEC)
    - Card with heading "Active Blockers" and count Badge
    - List of up to 5 blocked tasks: task name, quest name (if available), blocked reason truncated to 60 chars
    - Each row links to task detail page
    - "View all" Link to `/admin/blockers`
    - Empty state: "No blockers" subtitle "All tasks are unblocked." with CheckCircle icon in text-emerald-500

    **3. Create `AdminAdHocInjectorWidget.tsx`:**
    - `'use client'` component with form state
    - Card with heading "Quick Task" (per UI-SPEC copywriting contract)
    - Form fields: Title (Input, required), Assign to (Select with users fetched from `GET /users`), Quest (Select with quests fetched from `GET /quests`), Description (Textarea, optional)
    - "Add Task" Button (primary variant, disabled until Title filled)
    - On submit: `apiClient.post('/tasks', { title, description, quest_id, assigned_to, task_type: 'adhoc', status: 'todo' })`
    - On success: Sonner `toast.success('Task added')`, form resets, invalidate `['tasks']` query key via `useQueryClient().invalidateQueries({ queryKey: ['tasks'] })`
    - On error: `toast.error('Could not add task. Try again.')`
    - Always expanded (no toggle) per UI-SPEC interaction contract
    - Users Select: loads `apiClient.get<any[]>('/users')`, maps to `{value: id, label: name}`
    - Quests Select: loads `apiClient.get<any[]>('/quests')`, maps to `{value: id, label: title}`

    **4. Create `AdminRecentDecisionsWidget.tsx`:**
    - `'use client'` component
    - Uses `useQuery({ queryKey: ['decisions', 'recent'], queryFn: () => apiClient.get<Decision[]>('/decisions') })`
    - Card with heading "Recent Decisions"
    - List of last 5 decisions (slice first 5 from response, sorted by created_at desc)
    - Each row: decision title truncated to 40 chars, type Badge (individual/cross_function/strategic), status Badge with color (proposed=amber, approved=emerald, rejected=destructive), relative timestamp
    - Click navigates to `/decisions` page
    - No "View all" needed per UI-SPEC (sidebar link sufficient)
  </action>
  <verify>
    <automated>cd D:/aditee/konmaxperience/frontend && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - frontend/components/ops/dashboard/AdminPendingApprovalsWidget.tsx contains `'Pending Approvals'` and `refetchInterval: 60_000` and `formatDistanceToNow` and `'All clear'`
    - frontend/components/ops/dashboard/AdminBlockersWidget.tsx contains `'Active Blockers'` and `'/tasks?blocked=true'` and `'No blockers'`
    - frontend/components/ops/dashboard/AdminAdHocInjectorWidget.tsx contains `'Quick Task'` and `'Add Task'` and `apiClient.post` and `task_type: 'adhoc'` and `toast.success`
    - frontend/components/ops/dashboard/AdminRecentDecisionsWidget.tsx contains `'Recent Decisions'` and `'/decisions'`
    - `cd D:/aditee/konmaxperience/frontend && npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>4 admin dashboard widgets created: pending approvals (60s polling, oldest-first, view all link), active blockers (task list, view all link), ad-hoc task injector (inline form, creates adhoc tasks), recent decisions (last 5 with badges)</done>
</task>

<task type="auto">
  <name>Task 2: Role dashboard + page refactor + sidebar update</name>
  <files>
    frontend/components/ops/dashboard/RoleDashboardSections.tsx,
    frontend/app/(ops)/dashboard/page.tsx,
    frontend/components/ops/Sidebar.tsx
  </files>
  <read_first>
    frontend/app/(ops)/dashboard/page.tsx,
    frontend/components/ops/Sidebar.tsx,
    frontend/components/ops/dashboard/DashboardReadinessStrip.tsx,
    frontend/components/ops/dashboard/DashboardKpiAlert.tsx,
    frontend/components/ops/dashboard/DashboardLeaderboardPreview.tsx,
    frontend/components/ops/dashboard/DashboardLowStockAlert.tsx,
    frontend/components/ops/dashboard/AdminPendingApprovalsWidget.tsx,
    frontend/components/ops/dashboard/AdminBlockersWidget.tsx,
    frontend/components/ops/dashboard/AdminAdHocInjectorWidget.tsx,
    frontend/components/ops/dashboard/AdminRecentDecisionsWidget.tsx,
    frontend/lib/stores/auth-store.ts,
    frontend/lib/types/roles.ts,
    frontend/components/ops/gamification/XpProgressBar.tsx,
    frontend/components/ui/animated-circular-progress-bar.tsx,
    backend/src/readiness/readiness.controller.ts
  </read_first>
  <action>
    **1. Create `frontend/components/ops/dashboard/RoleDashboardSections.tsx`:**
    - `'use client'` component accepting no props
    - Gets `user` from `useAuthStore`

    - Section 1 "My Tasks": `useQuery({ queryKey: ['tasks', 'my'], queryFn: () => apiClient.get<any[]>('/tasks') })` — filters client-side for `assigned_to === user.id` and `status !== 'done'`, sorted by `due_date` ascending (due-soon first per D-02)
    - Renders as list of task rows in a Card: task title, quest name, due date (formatted), status Badge
    - Section heading: "My Tasks" (text-sm font-bold per UI-SPEC)

    - Section 2 "Active Quest": `useQuery({ queryKey: ['quests', 'my'], queryFn: () => apiClient.get<any[]>('/quests') })` — filters for quests where `owner_user_id === user.id` and `status === 'in_progress'`
    - Renders first active quest as Card: quest title (Heading/20px), Progress bar (shadcn Progress) showing `progress` value, task count text (e.g. "5/8 tasks")

    - Section 3 "My Progress": horizontal strip Card showing XP progress bar and level badge
    - Uses `user.xp_total`, `user.level` from auth store
    - XP bar: `XpProgressBar` component (already exists at `frontend/components/ops/gamification/XpProgressBar.tsx`)
    - Level: `LevelBadge` component (already exists)

    - Section 4 "My Contributions" (per DASH-02 requirement for "contribution meters" and "evidence"):
    - Query: `useQuery({ queryKey: ['readiness-meters'], queryFn: () => apiClient.get<any[]>('/readiness-meters') })`
    - Query: `useQuery({ queryKey: ['evidence', 'my'], queryFn: () => apiClient.get<any[]>('/evidence/feed') })` — filter client-side for `uploaded_by === user.id`
    - Sub-section a) "Readiness Contributions": Compact row showing the user's role-relevant readiness meters (filter meters by name matching user's role domain, e.g. "Frontend" meter for Frontend Lead, "Procurement" meter for Procurement Lead). For each relevant meter, show: meter name, AnimatedCircularProgressBar with `current_value`, and meter current value as percentage label. If user's role doesn't map to a specific meter, show the "Villa" meter (overall readiness). Display up to 3 meters max in a horizontal flex row.
    - Sub-section b) "Evidence Submitted": compact stat showing count of user's evidence submissions as `${myEvidenceCount} submissions` text with a small Badge. Link to `/boards/evidence` for full feed.
    - Layout: Card with CardContent, sections separated by Separator

    - Section 5 "Team Alerts": compact strip with muted background (no Card wrapper — inline list per UI-SPEC)
    - Reuses `DashboardLowStockAlert` and `DashboardKpiAlert` existing components for data
    - Fetch kpis and low-stock via same queries as admin dashboard
    - Layout: `grid grid-cols-1 lg:grid-cols-2 gap-6` for rows 1+2, then full-width for rows 3+4+5
    - Empty states: "No active tasks" / "No active quest" / "All systems operational"

    **2. Refactor `frontend/app/(ops)/dashboard/page.tsx`:**
    - Keep BlurFade wrapper
    - Admin branch: heading "Mission Control" + AdminUserFilter
      - **D-04 order (CRITICAL — must follow exactly):**
        1. Row 1: `grid grid-cols-1 md:grid-cols-2 xl:grid-cols-3 gap-6` with 3 widgets:
           - AdminPendingApprovalsWidget
           - AdminBlockersWidget
           - AdminAdHocInjectorWidget
        2. Row 2: DashboardReadinessStrip (existing, full-width)
        3. Row 3: DashboardKpiAlert (existing, full-width)
        4. Row 4: AdminRecentDecisionsWidget (full-width — placed AFTER KPI alerts per D-04 position 6)
        5. Row 5: `grid grid-cols-1 lg:grid-cols-5 gap-6` — DashboardLeaderboardPreview (col-span-3) + DashboardLowStockAlert (col-span-2)
    - Role user branch: heading "My Dashboard" (no AdminUserFilter)
      - Renders `<RoleDashboardSections />`
    - Remove all existing skeleton components from page file — move data fetching to widget components where relevant (admin widgets own their queries)
    - Admin dashboard sections also own their own queries (readiness, kpis, leaderboard, low-stock) — keep existing useQuery hooks for these 4 but only in admin branch

    **3. Update `frontend/components/ops/Sidebar.tsx`:**

    Add 4 new navigation entries:

    a) **Boards section** (NEW — between workNav and intelligenceNav):
    ```typescript
    import { LayoutGrid, Medal, Eye } from 'lucide-react';
    // Add after existing imports

    const boardsNav: NavItem[] = [
      { label: 'Missions', href: '/boards/missions', icon: <Rocket className="size-4" /> },
      { label: 'Quests', href: '/boards/quests', icon: <CheckCircle className="size-4" /> },
      { label: 'Wins', href: '/boards/wins', icon: <Medal className="size-4" /> },
      { label: 'Evidence Feed', href: '/boards/evidence', icon: <Eye className="size-4" /> },
    ];
    ```
    - Section header: "Boards" with section label styling matching existing sections
    - Visible to all authenticated users (per D-18)

    b) **Analytics link** under Intelligence section:
    - Add to `intelligenceNav` array: `{ label: 'Analytics', href: '/intelligence/analytics', icon: <TrendingUp className="size-4" /> }`
    - Only render when `permissions.includes('MANAGE_KPIS')` — use `useAuthStore((s) => s.permissions)` which already exists in the store
    - Conditionally add: `...(permissions.includes('MANAGE_KPIS') ? [{ label: 'Analytics', href: '/intelligence/analytics', icon: <TrendingUp className="size-4" /> }] : [])`

    c) **Kitchen Dashboard** link under Kitchen section:
    - Add to `kitchenNav` array: `{ label: 'Dashboard', href: '/operations/kitchen/dashboard', icon: <LayoutDashboard className="size-4" /> }`

    d) **Inventory Overview** link under Operations section:
    - Add to `operationsNav` array after Inventory entry: `{ label: 'Inventory Overview', href: '/operations/inventory/dashboard', icon: <BarChart3 className="size-4" /> }`

    Render boardsNav section in JSX between Work and Intelligence:
    ```tsx
    <div className="pt-3 pb-1 px-2">
      <span className="text-[11px] font-medium uppercase tracking-wider text-muted-foreground/60">
        Boards
      </span>
    </div>
    {boardsNav.map((item) => (
      <NavLink key={item.label} item={item} active={isActive(item.href)} />
    ))}
    ```

    Import new icons at top: add `LayoutGrid`, `Medal`, `Eye` to the lucide-react import.
  </action>
  <verify>
    <automated>cd D:/aditee/konmaxperience/frontend && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - frontend/app/(ops)/dashboard/page.tsx contains `isAdmin ? ` branching and `'Mission Control'` and `'My Dashboard'`
    - frontend/app/(ops)/dashboard/page.tsx contains imports for AdminPendingApprovalsWidget, AdminBlockersWidget, AdminAdHocInjectorWidget, AdminRecentDecisionsWidget
    - frontend/app/(ops)/dashboard/page.tsx contains import for RoleDashboardSections
    - frontend/app/(ops)/dashboard/page.tsx renders AdminRecentDecisionsWidget AFTER DashboardKpiAlert (D-04 compliance — Decisions at position 6)
    - frontend/app/(ops)/dashboard/page.tsx does NOT render AdminRecentDecisionsWidget in the same grid row as Approvals/Blockers/Ad-hoc
    - frontend/components/ops/dashboard/RoleDashboardSections.tsx contains `'My Tasks'` and `'Active Quest'` and `'My Progress'` and `XpProgressBar`
    - frontend/components/ops/dashboard/RoleDashboardSections.tsx contains `'My Contributions'` or `'Readiness Contributions'` and `readiness-meters` query
    - frontend/components/ops/dashboard/RoleDashboardSections.tsx contains `AnimatedCircularProgressBar` for readiness contribution display
    - frontend/components/ops/dashboard/RoleDashboardSections.tsx contains evidence count display with `'/evidence/feed'` query
    - frontend/components/ops/Sidebar.tsx contains `boardsNav` array with 4 entries (Missions, Quests, Wins, Evidence Feed)
    - frontend/components/ops/Sidebar.tsx contains `'Boards'` section label
    - frontend/components/ops/Sidebar.tsx contains `'/intelligence/analytics'` with `MANAGE_KPIS` permission check
    - frontend/components/ops/Sidebar.tsx contains `'/operations/kitchen/dashboard'`
    - frontend/components/ops/Sidebar.tsx contains `'/operations/inventory/dashboard'`
    - `cd D:/aditee/konmaxperience/frontend && npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>Dashboard page has admin/role branching; admin sees 8 widget sections in D-04 order (Approvals, Blockers, Ad-hoc, Readiness, KPIs, Decisions, Leaderboard, Low stock); role users see my tasks, quest progress, XP strip, contribution meters with readiness rings, evidence count, team alerts; sidebar has Boards section, Analytics link, Kitchen Dashboard link, Inventory Overview link</done>
</task>

</tasks>

<verification>
- `cd D:/aditee/konmaxperience/frontend && npx tsc --noEmit` exits 0
- Dashboard page renders different sections for admin vs role users
- Admin dashboard shows Approvals -> Blockers -> Ad-hoc Injector -> Readiness -> KPIs -> Decisions -> Leaderboard -> Low Stock (D-04 order)
- Role user dashboard shows My Tasks, Active Quest, My Progress, My Contributions (readiness meters + evidence count), Team Alerts
- Sidebar shows Boards section between Work and Intelligence
</verification>

<success_criteria>
1. Admin dashboard has 4 new widgets + 4 existing widgets in D-04 order: Approvals, Blockers, Ad-hoc (Row 1), Readiness (Row 2), KPIs (Row 3), Decisions (Row 4), Leaderboard + Low Stock (Row 5)
2. Role user dashboard shows my tasks (due-soon first), active quest with progress, XP/level card, contribution meters (readiness rings for role-relevant meters), evidence submission count, team alerts
3. Sidebar has Boards section (4 links), Analytics under Intelligence (MANAGE_KPIS gated), Kitchen Dashboard, Inventory Overview
4. All components compile without TypeScript errors
</success_criteria>

<output>
After completion, create `.planning/phases/11-dashboards-shared-boards/11-02-SUMMARY.md`
</output>
