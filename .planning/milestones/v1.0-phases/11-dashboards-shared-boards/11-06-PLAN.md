---
phase: 11-dashboards-shared-boards
plan: 06
type: execute
wave: 1
depends_on: []
gap_closure: true
files_modified:
  - backend/src/kitchen/metrics/kitchen-metrics.service.ts
  - frontend/components/ops/kitchen/KitchenMetricsCards.tsx
autonomous: true
requirements:
  - DASH-03

must_haves:
  truths:
    - "Kitchen metrics endpoint returns zone_utilization array with zone name and active order count"
    - "Kitchen dashboard shows a 5th stat card or zone utilization section"
  artifacts:
    - path: "backend/src/kitchen/metrics/kitchen-metrics.service.ts"
      provides: "zone_utilization field in KitchenMetrics"
      contains: "zone_utilization"
    - path: "frontend/components/ops/kitchen/KitchenMetricsCards.tsx"
      provides: "Zone utilization display"
      contains: "zone_utilization"
---

<objective>
Close DASH-03 station utilization gap. Orders have zone_id — derive station/zone utilization from active orders grouped by zone. No schema migration needed.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Add zone utilization to kitchen metrics and display</name>
  <files>
    backend/src/kitchen/metrics/kitchen-metrics.service.ts,
    frontend/lib/types/analytics.ts,
    frontend/components/ops/kitchen/KitchenMetricsCards.tsx,
    frontend/app/(ops)/operations/kitchen/dashboard/page.tsx
  </files>
  <read_first>
    backend/src/kitchen/metrics/kitchen-metrics.service.ts,
    frontend/lib/types/analytics.ts,
    frontend/components/ops/kitchen/KitchenMetricsCards.tsx,
    frontend/app/(ops)/operations/kitchen/dashboard/page.tsx
  </read_first>
  <action>
    1. In kitchen-metrics.service.ts, add zone_utilization query after existing metrics:
       - Group active orders (status IN ['placed','preparing']) by zone_id with count
       - Include zone name via relation
       - Return as `zone_utilization: Array<{ zone_name: string; active_orders: number }>`

    2. Update KitchenMetrics interface in both service and frontend/lib/types/analytics.ts:
       - Add `zone_utilization: Array<{ zone_name: string; active_orders: number }>`

    3. In KitchenMetricsCards.tsx or kitchen dashboard page, add a zone utilization section:
       - Display below the 4 stat cards as a compact list
       - Each zone: name + active order count as a horizontal bar or badge
       - Empty state: "No active orders by zone"
  </action>
  <verify>
    <automated>cd D:/aditee/konmaxperience/backend && npx tsc --noEmit 2>&1 | head -10 && cd ../frontend && npx tsc --noEmit 2>&1 | head -10</automated>
  </verify>
  <acceptance_criteria>
    - backend/src/kitchen/metrics/kitchen-metrics.service.ts contains `zone_utilization`
    - frontend/lib/types/analytics.ts contains `zone_utilization`
    - TypeScript compiles in both backend and frontend
  </acceptance_criteria>
  <done>Kitchen metrics now include zone utilization derived from active orders per zone</done>
</task>

</tasks>
