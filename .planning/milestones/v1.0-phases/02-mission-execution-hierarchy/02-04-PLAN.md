---
phase: 02-mission-execution-hierarchy
plan: 04
type: execute
wave: 3
depends_on: [02-02, 02-03]
files_modified:
  - frontend/app/(ops)/quests/[id]/page.tsx
  - frontend/app/(ops)/quests/[id]/tasks/new/page.tsx
  - frontend/app/(ops)/tasks/[id]/page.tsx
  - frontend/app/(ops)/admin/blockers/page.tsx
  - frontend/components/ops/tasks/TaskKanban.tsx
  - frontend/components/ops/tasks/TaskKanbanCard.tsx
  - frontend/components/ops/tasks/TaskListView.tsx
  - frontend/components/ops/tasks/TaskViewToggle.tsx
  - frontend/components/ops/tasks/TaskForm.tsx
  - frontend/components/ops/tasks/BlockerDialog.tsx
  - frontend/components/ops/tasks/AdHocTaskSheet.tsx
  - frontend/components/ops/quests/ConfirmActivateDialog.tsx
  - frontend/components/ops/Sidebar.tsx
autonomous: false
requirements: [EXEC-03, EXEC-04, EXEC-05, EXEC-06, EXEC-07, EXEC-08]

must_haves:
  truths:
    - "Quest detail page shows tasks in both kanban board and list view with a toggle"
    - "Kanban has 4 columns: To Do, Doing, Done, Blocked"
    - "Drag-and-drop on kanban updates task status via PATCH /tasks/:id"
    - "Non-admin users can only drag their own tasks"
    - "Task detail page shows all task information on a full dedicated page"
    - "Tasks show type badge (Core/Ad-hoc/Improvement) with visual differentiation"
    - "Ad-hoc tasks are visually distinct from core tasks (badge + amber accent)"
    - "Blocked tasks show red badge with blocking reason"
    - "Task owners can report a blocker via dialog"
    - "Admin can see all blocked tasks on /admin/blockers page"
    - "Admin can inject ad-hoc tasks via sidebar shortcut (Sheet panel)"
    - "Task form has dependency picker using @reui/p-combobox-3 to link to another task"
    - "Sidebar Missions link is enabled and navigates to /missions"
    - "Admin can activate a quest via 'Activate quest' button, which locks baseline_task_count"
  artifacts:
    - path: "frontend/app/(ops)/quests/[id]/page.tsx"
      provides: "Quest detail with task kanban/list toggle and activate quest button"
      min_lines: 50
    - path: "frontend/components/ops/tasks/TaskKanban.tsx"
      provides: "4-column kanban board using @spectrumui/kanbanboard"
      contains: "kanban"
    - path: "frontend/components/ops/tasks/TaskKanbanCard.tsx"
      provides: "Individual task card in kanban"
      contains: "task_type"
    - path: "frontend/components/ops/tasks/TaskListView.tsx"
      provides: "Table view with sort and filter using AnimatedList"
      contains: "Table"
    - path: "frontend/app/(ops)/tasks/[id]/page.tsx"
      provides: "Full task detail page"
      min_lines: 40
    - path: "frontend/app/(ops)/admin/blockers/page.tsx"
      provides: "Admin blocked tasks overview"
      contains: "blocked"
    - path: "frontend/components/ops/tasks/AdHocTaskSheet.tsx"
      provides: "Global ad-hoc task injection via Sheet"
      contains: "Sheet"
    - path: "frontend/components/ops/quests/ConfirmActivateDialog.tsx"
      provides: "Confirmation dialog for quest activation with baseline lock warning"
      contains: "Activate"
    - path: "frontend/components/ops/Sidebar.tsx"
      provides: "Updated sidebar with enabled Missions link + ad-hoc shortcut"
      contains: "href: '/missions'"
  key_links:
    - from: "frontend/components/ops/tasks/TaskKanban.tsx"
      to: "/tasks/:id"
      via: "PATCH on drag-end to update status"
      pattern: "apiClient\\.patch.*tasks/"
    - from: "frontend/components/ops/tasks/BlockerDialog.tsx"
      to: "/tasks/:id/block"
      via: "POST on submit"
      pattern: "apiClient\\.post.*block"
    - from: "frontend/components/ops/tasks/AdHocTaskSheet.tsx"
      to: "/tasks"
      via: "POST with task_type='adhoc'"
      pattern: "task_type.*adhoc"
    - from: "frontend/app/(ops)/quests/[id]/page.tsx"
      to: "/tasks?quest_id=X"
      via: "apiClient.get for quest tasks"
      pattern: "apiClient\\.get.*tasks.*quest_id"
    - from: "frontend/app/(ops)/quests/[id]/page.tsx"
      to: "/quests/:id"
      via: "apiClient.patch for quest activation"
      pattern: "apiClient\\.patch.*quests/"
    - from: "frontend/components/ops/Sidebar.tsx"
      to: "frontend/components/ops/tasks/AdHocTaskSheet.tsx"
      via: "AdHocTaskSheet rendered in sidebar"
      pattern: "AdHocTaskSheet"
---

<objective>
Build the complete task management UI: quest detail page with kanban/list toggle, task creation form with dependency picker, task detail page, blocker reporting dialog, admin blockers overview, ad-hoc task injection via sidebar shortcut, quest activation button with confirmation dialog, and enable the Missions sidebar link. Uses @spectrumui/kanbanboard for the kanban, @reui/p-combobox-3 for the dependency picker, and MagicUI components for animations.

Purpose: Delivers the task execution interface that users interact with daily. The kanban board (EXEC-03), ad-hoc visual differentiation (EXEC-04, EXEC-05), dependency display (EXEC-06), blocker reporting (EXEC-07), and progress visibility (EXEC-08) all come together in this plan. The quest activation button completes EXEC-05 by giving admins the ability to lock baseline_task_count. This completes Phase 2's user-facing functionality.

Output: 4 page routes, 8 components (including ConfirmActivateDialog), updated Sidebar.
</objective>

<execution_context>
@D:/aditee/konmaxperience/.claude/get-shit-done/workflows/execute-plan.md
@D:/aditee/konmaxperience/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-mission-execution-hierarchy/02-CONTEXT.md
@.planning/phases/02-mission-execution-hierarchy/02-RESEARCH.md
@.planning/phases/02-mission-execution-hierarchy/02-UI-SPEC.md
@.planning/phases/02-mission-execution-hierarchy/02-01-SUMMARY.md
@.planning/phases/02-mission-execution-hierarchy/02-02-SUMMARY.md
@.planning/phases/02-mission-execution-hierarchy/02-03-SUMMARY.md

<interfaces>
<!-- Key types and patterns from Plans 01-03 and existing codebase. -->

From frontend/lib/types/tasks.ts (Plan 01):
```typescript
export type TaskType = 'core' | 'adhoc' | 'improvement';
export type TaskStatus = 'todo' | 'doing' | 'done' | 'blocked' | 'cancelled';
export type TaskPriority = 'low' | 'medium' | 'high' | 'critical';
export type TaskDomain = 'food' | 'art' | 'lifestyle' | 'ops' | 'procurement' | 'bi' | 'talent' | 'tech' | 'design';
export const TASK_TYPE_LABELS: Record<TaskType, string>;
export const TASK_STATUS_LABELS: Record<TaskStatus, string>;
export const TASK_PRIORITY_LABELS: Record<TaskPriority, string>;
export const TASK_DOMAIN_LABELS: Record<TaskDomain, string>;
export const KANBAN_COLUMNS: TaskStatus[] = ['todo', 'doing', 'done', 'blocked'];
export interface Task { id, mission_id, quest_id, title, description, task_type, domain, owner_user_id, owner?, status, priority, xp, blocked, blocked_reason, depends_on_task_id, depends_on?, due_date, is_own? }
export interface CreateTaskDto { mission_id, quest_id?, title, description, task_type, domain, owner_user_id, priority, xp?, depends_on_task_id?, due_date? }
export interface BlockTaskDto { reason: string }
```

From frontend/lib/types/quests.ts (Plan 01):
```typescript
export interface Quest { id, mission_id, title, description, week_number, owner_user_id, owner?, status, baseline_task_count, core_progress_percent, adhoc_progress_percent, progress_percent, tasks?, mission? }
```

From frontend/lib/types/missions.ts (Plan 01):
```typescript
export interface Mission { id, title, ... }
```

From frontend/lib/api-client.ts:
```typescript
export const apiClient = { get, post, patch, delete };
```

From frontend/components/ops/Sidebar.tsx (current -- Missions disabled):
```typescript
const mainNav: NavItem[] = [
  { label: 'Dashboard', href: '/dashboard', icon: <LayoutDashboard /> },
  { label: 'Missions', href: '#', icon: <Rocket />, disabled: true, badge: 'Coming soon' },
];
```

From frontend/components/ops/quests/QuestProgress.tsx (Plan 03):
```typescript
export function QuestProgress({ coreProgress, adhocProgress, baselineTaskCount, totalAdhocTasks }: QuestProgressProps);
```

Backend API contracts (Plan 02):
- GET /tasks?quest_id=X -- returns Task[] with is_own boolean
- POST /tasks -- creates task (permission varies by task_type)
- PATCH /tasks/:id -- updates task (triggers progress recalc if status changes)
- POST /tasks/:id/block -- blocks task with reason
- POST /tasks/:id/unblock -- unblocks task
- GET /tasks/blocked -- admin-only, all blocked tasks
- PATCH /quests/:id -- updates quest (status='active' triggers baseline lock)

Premium components installed in Plan 03:
- @spectrumui/kanbanboard -- use directly for kanban board
- @reui/p-combobox-3 -- use directly for dependency picker
- @magicui/animated-list -- use for task list view animations
- @magicui/confetti -- use for quest completion celebration
- @magicui/avatar-circles -- use for owner display
- @magicui/blur-fade -- use for page transitions
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build quest detail page with task kanban/list toggle, activate quest button, and task components</name>
  <read_first>
    - frontend/app/(ops)/admin/users/page.tsx (page pattern: 'use client', useQuery, state management)
    - frontend/lib/types/tasks.ts (Task interface, KANBAN_COLUMNS, type labels)
    - frontend/lib/types/quests.ts (Quest interface)
    - frontend/lib/api-client.ts (apiClient usage)
    - frontend/lib/stores/auth-store.ts (user.id for is_own checks, user.roleCode for admin checks)
    - frontend/components/ui/progress.tsx (shadcn Progress component API)
    - frontend/components/ui/tabs.tsx (shadcn Tabs component API)
    - frontend/components/ui/scroll-area.tsx (shadcn ScrollArea API)
    - frontend/components/ops/quests/QuestProgress.tsx (dual-track progress from Plan 03)
    - .planning/phases/02-mission-execution-hierarchy/02-CONTEXT.md (locked decisions: kanban columns, list view specs)
    - .planning/phases/02-mission-execution-hierarchy/02-UI-SPEC.md (full design contract including premium component usage)
    - .planning/phases/02-mission-execution-hierarchy/02-RESEARCH.md (Pattern 7: @dnd-kit kanban, task status columns)
    - .planning/phases/01-foundation-authentication/01-UI-SPEC.md (design tokens: spacing, typography, colors)
    - frontend/components/ (check where @spectrumui/kanbanboard was installed by Plan 03 Task 1)
    - frontend/components/ (check where @magicui/ components were installed)
  </read_first>
  <files>
    frontend/app/(ops)/quests/[id]/page.tsx
    frontend/components/ops/tasks/TaskKanban.tsx
    frontend/components/ops/tasks/TaskKanbanCard.tsx
    frontend/components/ops/tasks/TaskListView.tsx
    frontend/components/ops/tasks/TaskViewToggle.tsx
    frontend/components/ops/quests/ConfirmActivateDialog.tsx
  </files>
  <action>
    **frontend/components/ops/tasks/TaskViewToggle.tsx:**
    - Props: view: 'kanban' | 'list', onViewChange: (view: 'kanban' | 'list') => void
    - Use shadcn Tabs component: <Tabs value={view} onValueChange={onViewChange}>
    - Two tabs: "Board" (with Kanban icon from lucide-react) and "List" (with List icon)
    - Compact design: inline at top-right of task section

    **frontend/components/ops/tasks/TaskKanbanCard.tsx:**
    - Props: task: Task, isDraggable: boolean
    - Card content: task title (truncated to 1 line), task type badge (Core=default, Ad-hoc=amber badge with "Ad-hoc" text, Improvement=blue badge), priority badge (critical=red, high=orange, medium=default, low=muted), owner name (small text), due date if present (formatted with date-fns, red if overdue)
    - Blocked indicator: if task.blocked, red border-l-4 on card + red "Blocked" badge
    - Dependency indicator: if task.depends_on, small link icon + "Depends on: {depends_on.title}" truncated text (text-xs, muted)
    - isDraggable controls whether the card gets drag handle styling. When false, card renders without drag handle (read-only tasks in quest context).
    - Click anywhere on card navigates to /tasks/{task.id}
    - Ad-hoc visual differentiation: amber-500 left border (border-l-2) + amber "Ad-hoc" badge -- makes ad-hoc tasks immediately recognizable per CONTEXT.md

    **frontend/components/ops/tasks/TaskKanban.tsx:**
    - Props: tasks: Task[], onStatusChange: (taskId: string, newStatus: TaskStatus) => void, currentUserId: string, isAdmin: boolean
    - **Use `@spectrumui/kanbanboard` as the kanban board component.** Read the installed component files to understand its API (columns, cards, drag-and-drop events). Adapt it to work with our Task type and KANBAN_COLUMNS.
    - If @spectrumui/kanbanboard provides its own DnD handling, use it directly. If it requires @dnd-kit integration, wire it up using DndContext, DragOverlay, PointerSensor, KeyboardSensor from @dnd-kit/core.
    - 4 columns from KANBAN_COLUMNS: 'todo', 'doing', 'done', 'blocked'
    - Column headers: TASK_STATUS_LABELS[status] + count badge
    - Each column uses ScrollArea for vertical scrolling.
    - Layout: grid grid-cols-4 gap-4 h-[calc(100vh-280px)]
    - onDragEnd: extract new column and call onStatusChange(taskId, newStatus)
    - Drag permission: only allow drag on tasks where task.is_own === true OR isAdmin. Non-draggable tasks render without drag handle.
    - CRITICAL: Do NOT allow dragging TO the 'blocked' column via drag-and-drop. Blocking requires a reason (use BlockerDialog instead). Only allow drag FROM blocked TO other columns (unblock).
    - Column styling: bg-muted/30 rounded-lg p-2, column header text-sm font-medium

    **frontend/components/ops/tasks/TaskListView.tsx:**
    - Props: tasks: Task[], onStatusChange: (taskId: string, newStatus: TaskStatus) => void, currentUserId: string, isAdmin: boolean
    - **Wrap the task list items in `AnimatedList` from MagicUI** for staggered item entrance animations.
    - Use shadcn Table component
    - Columns: Title (with type badge inline), Status (Select dropdown for status change on own tasks), Priority (badge), Owner (name), Due date (formatted, red if overdue), Blocked (red badge if blocked)
    - Status Select dropdown: only enabled on tasks where is_own=true or isAdmin. Changing status calls onStatusChange.
    - Remove 'blocked' from status Select options -- blocking requires the blocker dialog.
    - Sortable columns: click column header to sort by that field. Default sort: priority desc, created_at desc.
    - Filterable: simple text filter input above table for title search.
    - Row click navigates to /tasks/{task.id}
    - Row hover: bg-muted (subtle)
    - Ad-hoc tasks: show amber "Ad-hoc" badge next to title

    **frontend/components/ops/quests/ConfirmActivateDialog.tsx:**
    - Props: questId: string, questTitle: string, coreTaskCount: number, open: boolean, onOpenChange: (open: boolean) => void, onActivated: () => void
    - Dialog with title "Activate quest"
    - Body text: "This will lock the baseline task count at {coreTaskCount} core tasks. Core progress will be calculated from this number. You can still add ad-hoc tasks after activation." (per UI-SPEC copywriting contract)
    - Two buttons: "Keep as planned" (ghost) and "Activate" (primary/accent)
    - On confirm: apiClient.patch(`/quests/${questId}`, { status: 'active' }), then call onActivated() which invalidates quests query
    - Loading state on submit: "Activating..." with spinner
    - **Trigger `Confetti` from MagicUI on successful activation** -- quest activation is a significant milestone

    **frontend/app/(ops)/quests/[id]/page.tsx:**
    - 'use client' with `use(params)` for id extraction (Next.js 16 async params)
    - **Wrap page content in `BlurFade`** for page transition animation.
    - Fetch quest: useQuery({ queryKey: ['quests', id], queryFn: () => apiClient.get<Quest>(`/quests/${id}`) })
    - Fetch tasks: useQuery({ queryKey: ['tasks', { questId: id }], queryFn: () => apiClient.get<Task[]>(`/tasks?quest_id=${id}`) })
    - Page header: quest title (h1) + "Week {week_number}" badge + status badge + back link to /missions/{quest.mission_id}
    - **"Activate quest" button:** Visible when quest.status === 'planned' AND user is admin. Primary/accent button. On click, opens ConfirmActivateDialog with coreTaskCount = count of tasks where task_type === 'core' from the tasks query. After activation, invalidate ['quests', id] query.
    - Progress section: QuestProgress component with coreProgress, adhocProgress, baselineTaskCount, totalAdhocTasks (count adhoc tasks from task list)
    - **Trigger `Confetti` from MagicUI when quest status changes to 'completed'** (check quest.status on data load).
    - Below progress: TaskViewToggle + "Add task" Button (links to /quests/{id}/tasks/new, visible to users with CREATE_TASK permission) + "Add ad-hoc task" Button (amber variant, visible to users with CREATE_ADHOC_TASK permission, links to /quests/{id}/tasks/new?type=adhoc)
    - View state: useState for 'kanban' | 'list', default 'kanban'
    - Render TaskKanban or TaskListView based on view state
    - onStatusChange handler: apiClient.patch(`/tasks/${taskId}`, { status: newStatus }), then invalidate tasks query
    - Extract currentUserId from useAuthStore, isAdmin from roleCode check
    - Mutation for status change: useMutation with invalidation of ['tasks', { questId: id }] and ['quests', id]
  </action>
  <verify>
    <automated>cd D:/aditee/konmaxperience/frontend && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - File frontend/app/(ops)/quests/[id]/page.tsx contains `use(params)` and `TaskKanban` and `TaskListView` and `TaskViewToggle` and `QuestProgress` and `ConfirmActivateDialog` and `BlurFade`
    - File frontend/app/(ops)/quests/[id]/page.tsx contains `Activate quest` button logic with status === 'planned' check
    - File frontend/components/ops/tasks/TaskKanban.tsx contains `kanban` (from @spectrumui/kanbanboard usage) and `KANBAN_COLUMNS` and `onStatusChange` and `is_own`
    - File frontend/components/ops/tasks/TaskKanban.tsx does NOT contain `Shoogle` or `shoogle` or `mcp.shoogle.dev`
    - File frontend/components/ops/tasks/TaskKanbanCard.tsx contains `isDraggable` and `task_type` and `blocked`
    - File frontend/components/ops/tasks/TaskListView.tsx contains `Table` and `onStatusChange` and `is_own` and `AnimatedList`
    - File frontend/components/ops/tasks/TaskViewToggle.tsx contains `Tabs` and `kanban` and `list`
    - File frontend/components/ops/quests/ConfirmActivateDialog.tsx contains `apiClient.patch` and `quests/` and `status` and `active` and `baseline` and `Confetti`
    - No file accesses params synchronously (all [id] pages use `use(params)`)
    - `cd frontend && npx tsc --noEmit` exits with code 0
  </acceptance_criteria>
  <done>
    Quest detail page shows tasks in kanban (4 columns: To Do, Doing, Done, Blocked) using @spectrumui/kanbanboard, and in list view with AnimatedList animations, toggled by TaskViewToggle. Ad-hoc tasks are visually distinct (amber border + badge). Blocked tasks show red badge and reason. Only owned tasks are draggable. Progress shows dual-track core vs ad-hoc bars. "Activate quest" button is visible for planned quests (admin only), opens ConfirmActivateDialog which locks baseline_task_count via PATCH /quests/:id with status='active'. Confetti fires on quest activation and completion.
  </done>
</task>

<task type="auto">
  <name>Task 2: Build task detail page, task form with @reui/p-combobox-3, blocker dialog, ad-hoc sheet, and sidebar update</name>
  <read_first>
    - frontend/components/ops/Sidebar.tsx (current sidebar with disabled Missions link)
    - frontend/app/(ops)/admin/users/page.tsx (page pattern for admin pages)
    - frontend/lib/types/tasks.ts (Task, CreateTaskDto, BlockTaskDto, all label constants)
    - frontend/lib/types/missions.ts (Mission interface for ad-hoc sheet mission/quest picker)
    - frontend/lib/types/quests.ts (Quest interface for ad-hoc sheet)
    - frontend/lib/types/roles.ts (RoleCode for admin check)
    - frontend/lib/api-client.ts (apiClient)
    - frontend/lib/stores/auth-store.ts (user state)
    - frontend/components/ui/sheet.tsx (Sheet component API)
    - frontend/components/ui/textarea.tsx (Textarea API)
    - frontend/components/ui/dialog.tsx (Dialog for blocker)
    - frontend/components/ (check where @reui/p-combobox-3 was installed by Plan 03 Task 1 -- read the component file to understand its API)
    - frontend/components/ (check where @magicui/avatar-circles and @magicui/blur-fade were installed)
    - .planning/phases/02-mission-execution-hierarchy/02-CONTEXT.md (locked decisions: task detail is full page, blocker reporting flow, ad-hoc from both quest page and sidebar)
    - .planning/phases/02-mission-execution-hierarchy/02-UI-SPEC.md (full design contract)
    - .planning/phases/01-foundation-authentication/01-UI-SPEC.md (design tokens)
  </read_first>
  <files>
    frontend/app/(ops)/quests/[id]/tasks/new/page.tsx
    frontend/app/(ops)/tasks/[id]/page.tsx
    frontend/app/(ops)/admin/blockers/page.tsx
    frontend/components/ops/tasks/TaskForm.tsx
    frontend/components/ops/tasks/BlockerDialog.tsx
    frontend/components/ops/tasks/AdHocTaskSheet.tsx
    frontend/components/ops/Sidebar.tsx
  </files>
  <action>
    **frontend/components/ops/tasks/TaskForm.tsx:**
    - Props: questId: string, missionId: string, onSubmit: (data: CreateTaskDto) => Promise<void>, isSubmitting: boolean, defaultTaskType?: TaskType
    - Fetch users for owner assignment: useQuery for GET /users
    - Fetch tasks in same mission for dependency picker: useQuery for GET /tasks?mission_id={missionId}
    - react-hook-form + zod schema: title (min 3), description (min 1), task_type (enum, default from defaultTaskType prop or 'core'), domain (enum), owner_user_id (uuid), priority (enum, default 'medium'), xp (int, min 0, default 25), depends_on_task_id (optional uuid), due_date (optional)
    - Fields layout (vertical stack, gap-4):
      1. title (Input)
      2. description (Textarea)
      3. Two-column row: task_type (Select with TASK_TYPE_LABELS), domain (Select with TASK_DOMAIN_LABELS)
      4. Two-column row: owner_user_id (Select from users), priority (Select with TASK_PRIORITY_LABELS)
      5. Two-column row: xp (Input type="number"), due_date (Input type="date")
      6. depends_on_task_id: **Use `@reui/p-combobox-3` component** for the dependency picker. Read the installed component files to understand its API (likely accepts items, onSelect, placeholder, searchable props). Pass task titles from the same mission as items, with status badge display. Selecting a task sets the FK. Clear button to remove dependency.
    - Hidden fields: mission_id, quest_id (from props)
    - When task_type is 'adhoc', show info text: "Ad-hoc tasks receive 70% XP weight" (text-xs text-amber-500)
    - When task_type is 'improvement', show: "Improvement tasks receive 80% XP weight" (text-xs text-blue-500)
    - Submit button: "Create task" (or "Create ad-hoc task" if task_type is adhoc)

    **frontend/app/(ops)/quests/[id]/tasks/new/page.tsx:**
    - 'use client' with `use(params)` for id extraction
    - Also read searchParams for ?type=adhoc: `const searchParams = use(props.searchParams)` (Next.js 16: searchParams is also a Promise)
    - **Wrap in `BlurFade`** for page transition animation.
    - Fetch quest to get mission_id: useQuery for GET /quests/{id}
    - Page title: "New task" (or "New ad-hoc task" if type=adhoc) with back link to /quests/{id}
    - Render TaskForm with questId={id}, missionId={quest.mission_id}, defaultTaskType from searchParams
    - On submit: apiClient.post('/tasks', data), invalidate tasks query, redirect to /quests/{id}
    - Max width: max-w-2xl mx-auto

    **frontend/app/(ops)/tasks/[id]/page.tsx:**
    - 'use client' with `use(params)` for task id
    - **Wrap in `BlurFade`** for page transition animation.
    - Fetch task: useQuery for GET /tasks/{id}
    - Full dedicated page (locked decision -- not a modal or side panel)
    - Page header: task title (h1) + type badge + status badge + priority badge
    - Back link to /quests/{task.quest_id} (or /missions/{task.mission_id} if no quest)
    - Detail sections:
      1. **Overview card:** Description text, domain badge, XP value ("25 XP" + effective XP if adhoc: "17 XP effective at 70%"), due date, created_at formatted
      2. **People card:** Owner (name + `AvatarCircles` from MagicUI for avatar display), Creator (name)
      3. **Status card:** Current status with dropdown to change (only if is_own or admin), blocked status with reason if blocked, "Report blocker" button (opens BlockerDialog), "Resolve blocker" button if currently blocked (calls POST /tasks/:id/unblock)
      4. **Dependency card:** If depends_on exists, show linked task title + status badge + link to that task. If dependency task is not done, show warning: "This task is blocked by an incomplete dependency". Dependency picker to change/set dependency.
      5. **Evidence section (Phase 3 placeholder):** Card with "Evidence upload coming in Phase 3" text + Upload icon (muted/disabled state)
    - Status change: useMutation calling apiClient.patch, invalidate task and quest queries
    - Admin can see additional info: created_by name, full audit trail of dates

    **frontend/components/ops/tasks/BlockerDialog.tsx:**
    - Props: taskId: string, open: boolean, onOpenChange: (open: boolean) => void, onBlocked: () => void
    - Dialog with title "Report a blocker"
    - Textarea for blocker reason (min 3 chars)
    - Submit: apiClient.post(`/tasks/${taskId}/block`, { reason }), then call onBlocked() which invalidates queries
    - Cancel button + Submit button ("Report blocker" in red/destructive variant)
    - Loading state on submit

    **frontend/app/(ops)/admin/blockers/page.tsx:**
    - 'use client'
    - **Wrap in `BlurFade`** for page transition animation.
    - Admin-only page (check isAdmin, redirect or show access denied if not)
    - Fetch blocked tasks: useQuery for GET /tasks/blocked
    - Page title: "Blocked tasks" with count badge
    - Table with columns: Task title (link to /tasks/{id}), Quest (link to /quests/{quest.id}), Mission (link to /missions/{mission.id}), Owner name, Blocked reason, Blocked since (relative time with date-fns formatDistanceToNow)
    - Empty state: checkmark icon + "No blocked tasks" + "All tasks are currently unblocked."
    - Each row has "Resolve" button that calls POST /tasks/:id/unblock and invalidates query

    **frontend/components/ops/tasks/AdHocTaskSheet.tsx:**
    - Props: open: boolean, onOpenChange: (open: boolean) => void
    - Sheet (slide-in panel from right) with title "Inject ad-hoc task"
    - Step 1: Select mission from dropdown (fetch GET /missions)
    - Step 2: After mission selected, select quest from dropdown (fetch GET /quests?mission_id=X)
    - Step 3: After quest selected, show inline TaskForm with task_type pre-set to 'adhoc' and locked (not changeable)
    - On submit: create task via apiClient.post('/tasks', data), close sheet, invalidate relevant queries
    - Only visible to FOUNDER_ADMIN (has CREATE_ADHOC_TASK permission)

    **frontend/components/ops/Sidebar.tsx:**
    - Read the existing file completely before modifying
    - Change the Missions nav item from disabled to enabled:
      - Change `href: '#'` to `href: '/missions'`
      - Remove `disabled: true`
      - Remove `badge: 'Coming soon'`
    - Add ad-hoc task shortcut for admin only:
      - Import Plus icon from lucide-react
      - Import AdHocTaskSheet component
      - Add useState for adHocOpen boolean
      - In the admin nav section (after "Permissions" item), add a new admin item:
        ```
        { label: 'Blockers', href: '/admin/blockers', icon: <AlertTriangle className="size-4" /> }
        ```
      - Below the admin nav section (before the user section at bottom), add a floating action button visible only to FOUNDER_ADMIN:
        ```tsx
        {isAdmin && (
          <div className="px-2 pb-2">
            <button
              onClick={() => setAdHocOpen(true)}
              className="w-full flex items-center gap-2 rounded-md px-3 py-2 text-sm bg-amber-500/10 text-amber-500 hover:bg-amber-500/20 transition-colors"
            >
              <Plus className="size-4" />
              Ad-hoc task
            </button>
          </div>
        )}
        ```
      - Render AdHocTaskSheet at bottom of sidebar: `<AdHocTaskSheet open={adHocOpen} onOpenChange={setAdHocOpen} />`
    - Import AlertTriangle from lucide-react for blockers nav icon
  </action>
  <verify>
    <automated>cd D:/aditee/konmaxperience/frontend && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - File frontend/app/(ops)/tasks/[id]/page.tsx contains `use(params)` and `apiClient.get` and `/tasks/` and `BlockerDialog` and `Evidence` and `BlurFade` and `AvatarCircles`
    - File frontend/app/(ops)/quests/[id]/tasks/new/page.tsx contains `use(params)` and `TaskForm` and `apiClient.post.*tasks` and `BlurFade`
    - File frontend/app/(ops)/admin/blockers/page.tsx contains `apiClient.get.*tasks/blocked` and `Table` and `formatDistanceToNow` and `BlurFade`
    - File frontend/components/ops/tasks/TaskForm.tsx contains `useForm` and `zodResolver` and `TASK_TYPE_LABELS` and `TASK_DOMAIN_LABELS` and `depends_on_task_id`
    - File frontend/components/ops/tasks/TaskForm.tsx contains import or usage of @reui/p-combobox-3 component (NOT Popover+Command)
    - File frontend/components/ops/tasks/BlockerDialog.tsx contains `Dialog` and `apiClient.post` and `block` and `reason`
    - File frontend/components/ops/tasks/AdHocTaskSheet.tsx contains `Sheet` and `adhoc` and `apiClient.post.*tasks`
    - File frontend/components/ops/Sidebar.tsx contains `href: '/missions'` (not `href: '#'`) and `AdHocTaskSheet` and `Blockers` and `AlertTriangle`
    - File frontend/components/ops/Sidebar.tsx does NOT contain `disabled: true` on the Missions nav item
    - No file accesses params synchronously (all [id] pages use `use(params)`)
    - `cd frontend && npx tsc --noEmit` exits with code 0
  </acceptance_criteria>
  <done>
    Task creation form supports all fields including @reui/p-combobox-3 dependency picker and XP weight info. Task detail page shows full information with status change, blocker reporting, AvatarCircles for owner display, dependency display, and Phase 3 evidence placeholder. Admin blockers page lists all blocked tasks with resolve action. Ad-hoc task injection available via sidebar Sheet panel. Sidebar has enabled Missions link and Blockers admin link. All pages use BlurFade for transitions.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify mission execution hierarchy end-to-end</name>
  <what-built>
    Complete mission execution hierarchy with:
    - Backend: 3 NestJS modules (missions, quests, tasks) with full CRUD, RBAC, and atomic progress recalculation
    - Frontend: Mission list/detail/create pages with MagicCard and AnimatedCircularProgressBar
    - Quest detail with @spectrumui/kanbanboard kanban / AnimatedList list view toggle
    - Task detail/create pages with @reui/p-combobox-3 dependency picker
    - "Activate quest" button with ConfirmActivateDialog that locks baseline_task_count
    - Dual-track progress display (core vs ad-hoc) with NumberTicker
    - Blocker reporting and admin blockers overview
    - Ad-hoc task injection from sidebar shortcut
    - BlurFade page transitions, AvatarCircles for team display, Confetti on quest activation/completion
  </what-built>
  <how-to-verify>
    Prerequisites: backend running on port 4000, frontend on port 3000, database migrated and seeded.

    1. **Login as admin** at http://localhost:3000/login
    2. **Check sidebar:** "Missions" link should be enabled (not grayed out). Click it.
    3. **Mission list:** Should show empty state with "New mission" button. Verify BlurFade entrance animation. Click "New mission".
    4. **Create mission:** Fill form (title: "Food Launch Sprint", phase: Foundation, scope: Food, dates optional). Submit. Verify redirect to /missions with new mission card showing MagicCard spotlight hover effect and 0% AnimatedCircularProgressBar.
    5. **Mission detail:** Click the mission card. Verify mission details, AnimatedCircularProgressBar progress, and empty quest state. Click "Add quest".
    6. **Create quest:** Fill form (title: "Week 1 - Kitchen Setup", week number: 1, select an owner). Submit. Verify quest card appears with "Week 1" badge, AvatarCircles for owner, and 0% progress.
    7. **Quest detail:** Click the quest card. Verify kanban view using @spectrumui/kanbanboard with empty columns. Toggle to list view and back.
    8. **Create tasks:** Click "Add task". Fill form (title: "Set up production station", type: Core, domain: Food, owner, priority: High). Submit. Create 2-3 more core tasks.
    9. **Activate quest:** Verify "Activate quest" button is visible (quest is planned). Click it. Verify ConfirmActivateDialog shows baseline task count. Click "Activate". Verify Confetti animation fires and quest status changes to "Active".
    10. **Create ad-hoc task:** Click "Add ad-hoc task" button (amber). Verify task_type is pre-set to "adhoc". Fill and submit. Verify ad-hoc task has amber visual differentiation (border + badge).
    11. **Drag-and-drop:** Drag the core task from "To Do" to "Doing". Verify status updates via kanban board.
    12. **Task detail:** Click a task card. Verify full detail page with all information, AvatarCircles for owner, the @reui/p-combobox-3 dependency picker, and Phase 3 evidence placeholder.
    13. **Blocker:** On task detail, click "Report blocker". Enter reason. Submit. Verify task shows blocked status with red badge.
    14. **Admin blockers:** Click "Blockers" in sidebar admin section. Verify blocked task appears in the table. Click "Resolve" to unblock.
    15. **Ad-hoc sidebar shortcut:** Click the amber "Ad-hoc task" button in sidebar. Verify Sheet opens with mission/quest picker, then task form.
    16. **Progress check:** After creating and completing some tasks, verify quest progress bars update (core track separate from ad-hoc track) with NumberTicker animation.
  </how-to-verify>
  <resume-signal>Type "approved" if the execution hierarchy works correctly, or describe any issues found.</resume-signal>
</task>

</tasks>

<verification>
- `cd frontend && npx tsc --noEmit` passes
- `cd backend && npx tsc --noEmit` passes
- `cd backend && npx jest --passWithNoTests --forceExit` passes
- All 4 page routes render without errors
- Kanban uses @spectrumui/kanbanboard (NOT built from scratch, NOT Shoogle MCP)
- Dependency picker uses @reui/p-combobox-3 (NOT Popover+Command)
- TaskListView uses AnimatedList from MagicUI
- Confetti fires on quest activation
- "Activate quest" button visible for planned quests, opens ConfirmActivateDialog
- ConfirmActivateDialog sends PATCH /quests/:id with status='active'
- Kanban drag-and-drop updates task status
- Blocker dialog posts to /tasks/:id/block
- Ad-hoc Sheet creates tasks with task_type='adhoc'
- Sidebar Missions link navigates to /missions
- Task detail page shows AvatarCircles for owner and dependency info via @reui/p-combobox-3
- Admin blockers page shows all blocked tasks
- All pages use BlurFade for transitions
</verification>

<success_criteria>
- Quest detail page shows tasks in kanban board (via @spectrumui/kanbanboard) with 4 columns
- Toggle switches between kanban and AnimatedList list view
- Only owned tasks are draggable in kanban
- Ad-hoc tasks are visually distinct (amber border + badge)
- "Activate quest" button works for planned quests, locks baseline via ConfirmActivateDialog
- Confetti fires on quest activation
- Task detail is a full dedicated page with @reui/p-combobox-3 dependency picker
- Blocker reporting works via dialog with reason
- Admin can see all blockers on /admin/blockers
- Ad-hoc task injection works from sidebar shortcut
- Sidebar Missions link is enabled
- Human verification checkpoint passes
</success_criteria>

<output>
After completion, create `.planning/phases/02-mission-execution-hierarchy/02-04-SUMMARY.md`
</output>
