---
phase: 10-pos-orders
plan: 03
type: execute
wave: 2
depends_on: ["10-01"]
files_modified:
  - frontend/app/(ops)/pos/page.tsx
  - frontend/components/ops/pos/PosMenuGrid.tsx
  - frontend/components/ops/pos/PosMenuItemCard.tsx
  - frontend/components/ops/pos/PosCartSidebar.tsx
  - frontend/components/ops/pos/PosCartItemRow.tsx
  - frontend/components/ops/pos/PosChannelFields.tsx
  - frontend/components/ops/Sidebar.tsx
autonomous: true
requirements:
  - POS-01

must_haves:
  truths:
    - "Staff can see menu items organized by Brand tabs and Category rows"
    - "Staff can tap a menu item to add it to the cart"
    - "Staff can adjust item quantity with + / - buttons"
    - "Cart displays subtotal, channel modifier, and total"
    - "Staff can select channel (dine-in / takeaway / delivery) and fill channel-specific fields"
    - "Staff can place an order and see confirmation toast"
    - "Servings-remaining badge shows on each item, sold-out items are grayed out"
    - "Full-screen terminal mode toggle works"
  artifacts:
    - path: "frontend/app/(ops)/pos/page.tsx"
      provides: "POS page with split-screen layout"
      contains: "PosMenuGrid"
    - path: "frontend/components/ops/pos/PosMenuGrid.tsx"
      provides: "Brand tabs + category sections + item card grid"
      contains: "Tabs"
    - path: "frontend/components/ops/pos/PosMenuItemCard.tsx"
      provides: "Tap-to-add item card with servings badge"
      contains: "MagicCard"
    - path: "frontend/components/ops/pos/PosCartSidebar.tsx"
      provides: "Order summary sidebar with channel selector and Place Order CTA"
      contains: "PulsatingButton"
    - path: "frontend/components/ops/pos/PosCartItemRow.tsx"
      provides: "Cart item row with quantity adjustment"
      contains: "AnimatedListItem"
    - path: "frontend/components/ops/pos/PosChannelFields.tsx"
      provides: "Conditional channel-specific input fields"
      contains: "channel"
  key_links:
    - from: "frontend/app/(ops)/pos/page.tsx"
      to: "/menu/availability"
      via: "useQuery with refetchInterval: 10000"
      pattern: "refetchInterval.*10000"
    - from: "frontend/app/(ops)/pos/page.tsx"
      to: "POST /orders"
      via: "useMutation for order placement"
      pattern: "POST.*orders"
    - from: "frontend/components/ops/Sidebar.tsx"
      to: "/pos"
      via: "posNav array"
      pattern: "posNav"
---

<objective>
Build the POS interface page with split-screen layout (menu grid left, cart sidebar right), brand/category navigation, tap-to-add items, servings-remaining badges, quantity adjustment, channel selector with conditional fields, Place Order flow, and full-screen terminal mode toggle.

Purpose: Implements POS-01 (D-01 through D-06) — the primary staff-facing order creation interface. This is the most interaction-heavy page in the system.
Output: /pos page with 6 components, sidebar navigation update.
</objective>

<execution_context>
@D:/aditee/konmaxperience/.claude/get-shit-done/workflows/execute-plan.md
@D:/aditee/konmaxperience/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/10-pos-orders/10-CONTEXT.md
@.planning/phases/10-pos-orders/10-RESEARCH.md
@.planning/phases/10-pos-orders/10-UI-SPEC.md
@.planning/phases/10-pos-orders/10-01-SUMMARY.md

<interfaces>
<!-- Types from Plan 01 that this plan uses -->

From frontend/lib/types/orders.ts (created in Plan 01):
```typescript
export interface CreateOrderPayload {
  channel: OrderChannel;
  zone_id: string;
  items: CreateOrderItemPayload[];
  table_number?: string;
  customer_name?: string;
  customer_phone?: string;
  delivery_address?: string;
  delivery_assigned_to?: string;
  notes?: string;
}
export interface CreateOrderItemPayload {
  menu_item_id: string;
  quantity: number;
  unit_price: number;
  item_notes?: string;
}
export type AvailabilityMap = Record<string, MenuItemAvailability>;
export interface MenuItemAvailability {
  available: boolean;
  servings_remaining: number;
}
```

From frontend/lib/types/kds.ts:
```typescript
export type OrderChannel = 'dine_in' | 'takeaway' | 'delivery';
export const ORDER_CHANNEL_LABELS: Record<OrderChannel, string> = {
  dine_in: 'Dine-In', takeaway: 'Takeaway', delivery: 'Delivery',
};
```

From backend/src/menu/menu.controller.ts — existing endpoints:
```
GET /menu/items?brand_id=X       → MenuItem[] (Phase 7)
GET /menu/categories?brand_id=X  → MenuCategory[] (Phase 7)
GET /menu/availability            → Record<menuItemId, { available, servings_remaining }> (Plan 01)
POST /orders                     → Order (Plan 01)
```

From frontend/components/ops/Sidebar.tsx:
```typescript
// kitchenNav array at line 196 — posNav must be inserted AFTER kitchenNav, BEFORE adminNav
// Section rendering at line 290-297 — add posNav section between Kitchen and Admin
```

MagicUI components (all pre-installed at frontend/components/ui/):
```
magic-card.tsx — MagicCard with gradientColor="#1a1a2e"
animated-list.tsx — AnimatedListItem (NOT AnimatedList wrapper — Research Pitfall 4)
pulsating-button.tsx — PulsatingButton for Place Order CTA
shimmer-button.tsx — ShimmerButton for Terminal Mode toggle
border-beam.tsx — BorderBeam for order confirmation flash
number-ticker.tsx — NumberTicker for cart total
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: POS page layout, menu grid, and menu item card components</name>
  <files>
    frontend/app/(ops)/pos/page.tsx,
    frontend/components/ops/pos/PosMenuGrid.tsx,
    frontend/components/ops/pos/PosMenuItemCard.tsx
  </files>
  <read_first>
    frontend/lib/types/orders.ts,
    frontend/lib/types/kds.ts,
    frontend/components/ui/magic-card.tsx,
    frontend/components/ui/shimmer-button.tsx,
    frontend/components/ui/number-ticker.tsx,
    frontend/lib/api-client.ts,
    frontend/app/(ops)/pos/page.tsx (if exists),
    frontend/app/(ops)/operations/menu/page.tsx (for menu query pattern reference)
  </read_first>
  <action>
    1. **POS page** — `frontend/app/(ops)/pos/page.tsx`:
      - State: `cartItems: CartItem[]` (local useState), `channel: OrderChannel` (default 'dine_in'), `isFullScreen: boolean` (default false), `selectedBrandId: string | null`, channel-specific fields (table_number, customer_phone, delivery_address, delivery_assigned_to, notes)
      - CartItem interface: `{ menu_item_id: string; name: string; unit_price: number; quantity: number }`
      - Queries:
        a. `useQuery(['brands'], () => apiClient.get('/brands'))` to get brand tabs. Use `effectiveBrandId = selectedBrandId || brands[0]?.id` pattern (Phase 7-06 decision).
        b. `useQuery(['menu', 'categories', effectiveBrandId], ...)` to get categories for selected brand
        c. `useQuery(['menu', 'items', effectiveBrandId], () => apiClient.get('/menu/items?brand_id=' + effectiveBrandId))` to get items
        d. `useQuery(['menu', 'availability-batch'], () => apiClient.get('/menu/availability'), { refetchInterval: 10000, staleTime: 8000 })` for batch availability (10s polling per Phase 9 metrics bar pattern)
      - Mutation: `useMutation({ mutationFn: (payload: CreateOrderPayload) => apiClient.post('/orders', payload) })` with onSuccess: Sonner toast "Order #XXXX placed" (last 4 hex of id, uppercase), reset cart to empty, refetch availability. Show BorderBeam on cart sidebar for 3 seconds. On error: Sonner error toast per UI-SPEC copy.
      - Helper functions: `addItem(menuItem)` — adds or increments quantity per Research Pattern 4. `updateQuantity(menu_item_id, delta)` — increments/decrements, removes at 0. `removeItem(menu_item_id)`.
      - Subtotal = `cartItems.reduce((sum, i) => sum + i.unit_price * i.quantity, 0)`. Channel modifier estimation: fetch from a query or compute display-only (server is authoritative per research anti-pattern). Show "Channel modifier applied at checkout" text rather than guessing.
      - Layout: Full-screen mode uses `fixed inset-0 z-50 bg-background overflow-hidden` class when `isFullScreen=true`. Regular mode: standard page within (ops) layout. Exit button: absolute top-4 right-4. ShimmerButton in top-right of page header for toggle.
      - Split screen: `<div className="flex h-[calc(100vh-4rem)]">` (or full viewport in fullscreen). Left panel: `<div className="flex-1 overflow-y-auto p-4">` contains PosMenuGrid. Right panel: `<div className="w-80 min-w-[320px] border-l border-border flex flex-col">` contains PosCartSidebar.
      - Zone ID: Use the first zone from brands data or a default zone. Since menu items belong to brand > recipe > zone, use `items[0]?.recipe?.zone_id` or the zone of the selected brand. Simplest: query zones and use the first kitchen-type zone, or expose as a setting. For MVP, pass the zone_id from the first cart item's brand/recipe zone.
      - Permission: Page should be accessible to users with MANAGE_POS permission (or all authenticated staff for MVP — the backend enforces the permission).

    2. **PosMenuGrid** — `frontend/components/ops/pos/PosMenuGrid.tsx`:
      - Props: `brands`, `categories`, `items`, `selectedBrandId`, `onBrandChange`, `availability: AvailabilityMap`, `onAddItem: (item) => void`
      - Brand selector: shadcn `Tabs` component at top, sticky. Each tab = brand name. Per UI-SPEC: heading typography (20px/700).
      - Below tabs: categories rendered as sections. Each category has a header (heading typography) and a grid of item cards.
      - Grid: `grid grid-cols-3 gap-3` within each category section per UI-SPEC.
      - Group items by category_id. If no categories, render all items in one section.

    3. **PosMenuItemCard** — `frontend/components/ops/pos/PosMenuItemCard.tsx`:
      - Props: `menuItem: { id, name, base_price, image_url, recipe_id }`, `availability: MenuItemAvailability | undefined`, `onAdd: () => void`
      - Uses MagicCard with gradientColor="#1a1a2e"
      - Layout per UI-SPEC: item name (14px/400), price "INR {price}" (Geist Mono 14px/700), servings badge top-right, "+ Add" button full-width bottom
      - Servings badge color logic per UI-SPEC: 6+ emerald, 2-5 amber, 1 red, 0 sold out
      - Sold out state: `opacity-60 pointer-events-none`, "Sold Out" label (12px/700 muted-foreground) replaces badge, no Add button
      - Card minimum: 140px wide, 120px tall (touch target per UI-SPEC)
      - Tap "+ Add" calls onAdd (parent manages cart state)
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "PosMenuGrid" frontend/app/\(ops\)/pos/page.tsx
    - grep -q "PosCartSidebar" frontend/app/\(ops\)/pos/page.tsx
    - grep -q "refetchInterval.*10000" frontend/app/\(ops\)/pos/page.tsx
    - grep -q "availability-batch" frontend/app/\(ops\)/pos/page.tsx
    - grep -q "isFullScreen" frontend/app/\(ops\)/pos/page.tsx
    - grep -q "ShimmerButton" frontend/app/\(ops\)/pos/page.tsx
    - grep -q "MagicCard" frontend/components/ops/pos/PosMenuItemCard.tsx
    - grep -q "gradientColor" frontend/components/ops/pos/PosMenuItemCard.tsx
    - grep -q "Sold Out" frontend/components/ops/pos/PosMenuItemCard.tsx
    - grep -q "grid-cols-3" frontend/components/ops/pos/PosMenuGrid.tsx
    - grep -q "Tabs" frontend/components/ops/pos/PosMenuGrid.tsx
  </acceptance_criteria>
  <done>
    POS page renders split-screen layout with menu grid left (brand tabs, category sections, 3-col item card grid) and cart sidebar right. Items show servings-remaining badges with color coding and sold-out state. Full-screen toggle works via CSS fixed positioning. Batch availability polls every 10 seconds.
  </done>
</task>

<task type="auto">
  <name>Task 2: Cart sidebar, cart item row, channel fields, sidebar nav update</name>
  <files>
    frontend/components/ops/pos/PosCartSidebar.tsx,
    frontend/components/ops/pos/PosCartItemRow.tsx,
    frontend/components/ops/pos/PosChannelFields.tsx,
    frontend/components/ops/Sidebar.tsx
  </files>
  <read_first>
    frontend/app/(ops)/pos/page.tsx,
    frontend/components/ui/animated-list.tsx,
    frontend/components/ui/pulsating-button.tsx,
    frontend/components/ui/border-beam.tsx,
    frontend/components/ops/Sidebar.tsx,
    frontend/lib/types/orders.ts
  </read_first>
  <action>
    1. **PosCartSidebar** — `frontend/components/ops/pos/PosCartSidebar.tsx`:
      - Props: `cartItems`, `channel`, `onChannelChange`, `channelFields` (table_number, customer_phone, etc.), `onChannelFieldChange`, `notes`, `onNotesChange`, `subtotal`, `onUpdateQuantity`, `onPlaceOrder`, `isPlacing` (loading state), `showBorderBeam` (flashes on order success)
      - Layout: `flex flex-col h-full`
        a. Header: "Order Summary" (heading 20px/700)
        b. ScrollArea for cart item list (overflow when many items)
        c. Use `AnimatePresence` with `AnimatedListItem` for each cart item (per Research Pitfall 4 — NOT AnimatedList wrapper). Key by menu_item_id.
        d. Empty state: "No items yet" heading + "Tap any menu item to add it to the order." body per UI-SPEC copywriting.
        e. Below items: PosChannelFields component
        f. Separator
        g. Subtotal row: label + Geist Mono price
        h. Channel modifier note: "Channel pricing applied at checkout" (since server computes)
        i. Total estimate row (bold)
        j. Notes Textarea with placeholder "Any special requests? (optional)" per UI-SPEC
        k. PulsatingButton "Place Order" — active only when cartItems.length > 0 AND channel selected AND required channel fields filled. Disabled state uses regular Button (not pulsating). When isPlacing, show spinner.
      - BorderBeam: Wrap sidebar in relative container. When `showBorderBeam=true`, render `<BorderBeam />` — auto-hides after 3 seconds (parent manages via setTimeout).

    2. **PosCartItemRow** — `frontend/components/ops/pos/PosCartItemRow.tsx`:
      - Props: `item: CartItem`, `onUpdateQuantity: (delta: number) => void`
      - Wrap in AnimatedListItem for enter/exit animation
      - Layout per UI-SPEC: item name (14px/400), then row with "−" button, quantity display, "+" button, line total (Geist Mono 14px/700)
      - Qty buttons: 32x32px minimum (per UI-SPEC touch target). − at qty 1 removes item (calls onUpdateQuantity(-1) which parent handles as remove).
      - Line total = item.unit_price * item.quantity, formatted as "INR {total}"
      - No confirmation on remove — fast workflow per UI-SPEC.

    3. **PosChannelFields** — `frontend/components/ops/pos/PosChannelFields.tsx`:
      - Props: `channel: OrderChannel`, `onChannelChange`, `fields: { table_number, customer_name, customer_phone, delivery_address, delivery_assigned_to }`, `onFieldChange`
      - Channel selector: shadcn Tabs variant="outline" with 3 tabs (Dine-in, Takeaway, Delivery), compact h-8 height per UI-SPEC
      - Conditional fields based on channel:
        - dine_in: Input for table_number (placeholder "Table number" per UI-SPEC)
        - takeaway: Input for customer_phone (placeholder "Customer phone")
        - delivery: Input for customer_phone (placeholder "Customer phone"), Textarea for delivery_address (placeholder "Delivery address"), Input for delivery_assigned_to (placeholder "Rider or staff name")
      - All fields are Input components from shadcn. No animation on field appear/disappear (per UI-SPEC: conditional render, no animation).
      - customer_name Input shown for all channels (optional).

    4. **Sidebar nav update** — `frontend/components/ops/Sidebar.tsx`:
      - Add `posNav` array between `kitchenNav` and `adminNav`:
        ```typescript
        const posNav: NavItem[] = [
          { label: 'Take Order', href: '/pos', icon: <ShoppingCart className="size-4" /> },
          { label: 'Order History', href: '/pos/orders', icon: <ClipboardList className="size-4" /> },
          { label: 'Delivery Queue', href: '/pos/delivery', icon: <Truck className="size-4" /> },
        ];
        ```
      - Import `ShoppingCart`, `ClipboardList` from lucide-react (Truck already imported).
      - Add POS section in the render between Kitchen and Admin sections:
        ```tsx
        <div className="pt-3 pb-1 px-2">
          <span className="text-[11px] font-medium uppercase tracking-wider text-muted-foreground/60">
            POS
          </span>
        </div>
        {posNav.map((item) => (
          <NavLink key={item.label} item={item} active={isActive(item.href)} />
        ))}
        ```
      - Per D-15: POS section appears between Kitchen and Admin (before the `isAdmin && (` block).
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "AnimatePresence" frontend/components/ops/pos/PosCartSidebar.tsx
    - grep -q "AnimatedListItem" frontend/components/ops/pos/PosCartItemRow.tsx
    - grep -q "PulsatingButton" frontend/components/ops/pos/PosCartSidebar.tsx
    - grep -q "Place Order" frontend/components/ops/pos/PosCartSidebar.tsx
    - grep -q "BorderBeam" frontend/components/ops/pos/PosCartSidebar.tsx
    - grep -q "No items yet" frontend/components/ops/pos/PosCartSidebar.tsx
    - grep -q "dine_in" frontend/components/ops/pos/PosChannelFields.tsx
    - grep -q "delivery_address" frontend/components/ops/pos/PosChannelFields.tsx
    - grep -q "Table number" frontend/components/ops/pos/PosChannelFields.tsx
    - grep -q "posNav" frontend/components/ops/Sidebar.tsx
    - grep -q "Take Order" frontend/components/ops/Sidebar.tsx
    - grep -q "Delivery Queue" frontend/components/ops/Sidebar.tsx
    - grep -q "/pos/orders" frontend/components/ops/Sidebar.tsx
  </acceptance_criteria>
  <done>
    Cart sidebar shows animated item rows with +/- quantity controls, channel selector with conditional fields (table number for dine-in, phone for takeaway, full delivery fields for delivery), notes textarea, subtotal/total, and PulsatingButton Place Order CTA. BorderBeam flashes on order confirmation. Sidebar has POS section with 3 nav items. AnimatedListItem used (NOT AnimatedList wrapper).
  </done>
</task>

</tasks>

<verification>
- `cd frontend && npx tsc --noEmit` — no TypeScript errors
- grep confirms all UI-SPEC components used (MagicCard, AnimatedListItem, PulsatingButton, ShimmerButton, BorderBeam)
- grep confirms all UI-SPEC copywriting ("No items yet", "Sold Out", "Place Order", "Terminal Mode", etc.)
- grep confirms 10s availability polling
- grep confirms sidebar POS section with 3 nav items
- Page renders at /pos with split-screen layout
</verification>

<success_criteria>
- /pos page loads with Brand tabs, Category sections, 3-column item grid
- Menu items show servings badges (emerald/amber/red), sold-out items grayed
- Tap adds item to cart with AnimatedListItem animation
- +/- buttons adjust quantity, - at 1 removes item
- Channel selector shows conditional fields for dine-in/takeaway/delivery
- Place Order creates order via API, shows toast, resets cart, flashes BorderBeam
- Full-screen terminal mode toggle works
- Sidebar shows POS section between Kitchen and Admin
</success_criteria>

<output>
After completion, create `.planning/phases/10-pos-orders/10-03-SUMMARY.md`
</output>
